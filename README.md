# tiny-c2ll

a light weight C compiler.


## Pipeline
- Preprocessor: .c/.h -> pure C source text
- Lexer: source text -> token stream
- Parser: token stream -> AST
- SemanticAnalyzer: validates syntax, AST -> HIR
- LLVM IR Builder: HIR -> LLVM IR text (.ll)
- Backend (Clang) .ll -> .o

# Workflow

## 1. Define testset
currently, we have:

- tiny_c2ll, simple testcase and negative case
- c-testsuite, small C core feature testset
- ccc-review-tests, testset generated by code review from ref/claudes-c-compiler
- gcc-c-torture, tricky gcc corner cases

## 2. Run agent for bug fix

add test pattern in ./tests

then run (need claude code)
```bash
./scripts/run_agent.sh
```

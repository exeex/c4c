Status: Active
Source Idea: ideas/open/03_lir_to_backend_ir_refactor.md
Source Plan: plan.md

# Active Queue: LIR To Backend IR Refactor

## Queue
- [x] Step 1: Re-establish the lowering boundary
  - Notes: Moved the production lowering declarations/implementation to `src/backend/lowering/lir_to_backend_ir.*`, kept `src/backend/lir_adapter.hpp` as the legacy shim, and validated `backend_lir_adapter_tests`, `backend_lir_adapter_aarch64_tests`, `backend_lir_adapter_x86_64_tests`, `backend_ir_tests`, plus a monotonic full-suite regression check.
  - Blockers: None.
- [x] Step 2: Isolate LIR syntax decoding
  - Notes: The lowering entrypoint/file ownership now exists. Earlier in Step 2, the remaining x86-local direct-global LIR call decoders in `src/backend/x86/codegen/emit.cpp` were routed through the shared `src/backend/lowering/call_decode.*` helpers, and `backend_lir_adapter_tests` gained a mismatched-callee rejection on that shared LIR-call helper path. A later slice moved the x86 declared-extern typed-argument decoder out of `src/backend/x86/codegen/emit.cpp` into `src/backend/lowering/call_decode.*`, added an inferred-parameter x86 asm-path regression in `backend_lir_adapter_x86_64_tests`, and kept the direct extern-call path behavior unchanged. A later AArch64 slice moved the remaining general-emitter `args_str` and function-signature parsing behind `src/backend/lowering/call_decode.*`, removed the target-local parser from `src/backend/aarch64/codegen/emit.cpp`, and added a spacing-tolerant param-slot direct-call asm-path regression in `backend_lir_adapter_aarch64_tests`. A later slice moved the AArch64 minimal-lowering gate's raw LIR call/signature nonminimal-type detection behind shared helpers in `src/backend/lowering/call_decode.*`, replaced emitter-local string scans with those helpers, and added lowering-helper coverage in `backend_lir_adapter_tests` for i32-vs-i64 call/signature classification plus malformed-suffix fallback behavior. The previous slice moved the remaining AArch64 minimal-lowering gate type/global/return nonminimal-text predicate into shared `src/backend/lowering/call_decode.*` helpers, replaced emitter-local scans in `src/backend/aarch64/codegen/emit.cpp`, and added shared lowering coverage in `backend_lir_adapter_tests` for minimal i32 globals/returns plus floating-init and i64-return rejection. The previous slice moved repeated LIR `main` signature decoding behind new shared `src/backend/lowering/call_decode.*` helpers, replaced raw `signature_text == "define i32 @main()\\n"` and `find("i32 @main(")` checks across `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp`, and added lowering-helper coverage in `backend_lir_adapter_tests` for spaced zero-arg `main`, parameterized `main`, declaration rejection, and non-`i32` rejection. The previous slice replaced the remaining x86 helper-signature exact-text matches for `add_one` and `add_pair` with a shared `backend_lir_signature_matches(...)` helper in `src/backend/lowering/call_decode.*`, added shared lowering coverage in `backend_lir_adapter_tests` for spaced single-arg and two-arg helper signatures plus type-drift rejection, and added x86 asm-path regressions for spacing-tolerant helper signatures in `backend_lir_adapter_x86_64_tests`. The latest slice added a shared `LirCallOp` overload for owned typed-arg decoding in `src/backend/lowering/call_decode.*`, switched the AArch64 general asm call path off direct `p->args_str` parsing, cached `parse_backend_function_signature_params(fn.signature_text)` once per emitted function instead of reparsing it three times, and added lowering-helper coverage in `backend_lir_adapter_tests` for spacing-tolerant signature-param decoding plus owned typed-arg extraction from LIR call ops. Validation: `./build/backend_lir_adapter_tests`, `./build/backend_lir_adapter_x86_64_tests`, `./build/backend_lir_adapter_aarch64_tests`, and `./build/backend_ir_tests` passed; the monotonic full-suite regression guard also passed with `2667 passed / 2 failed` before and after, with the same existing AArch64 object-contract failures and no newly failing tests.
  - Blockers: None.
- [ ] Step 3: Make backend IR more backend-native
  - Notes: Step 2 is now closed. The latest slice records structured scalar-global type metadata alongside existing array metadata in `BackendGlobal`, teaches backend IR printing/validation to keep scalar globals well-formed after raw `llvm_type` text is cleared, and switches the explicit x86/AArch64 backend-module scalar-global load/store-reload/extern-load recognizers off raw `global.llvm_type == "i32"` gates and onto that backend-owned metadata. The same slice adds backend-IR coverage for structured scalar globals without raw type text plus x86/AArch64 asm-path regressions showing scalar global load and store-reload slices still stay on the assembly path when raw scalar global type text is removed. Earlier in Step 3, this slice added backend-owned `BackendGlobal::array_type` metadata during LIR->backend lowering, teaches backend IR printing/validation to keep array globals well-formed after raw `llvm_type` text is cleared, and switches the explicit x86/AArch64 backend-module extern-array and char/int ptrdiff recognizers off ad-hoc `global.llvm_type` string slicing and onto that structured metadata. The same slice adds backend-IR coverage for structured extern-array globals without raw type text plus x86/AArch64 asm-path regressions showing extern-array and char-ptrdiff slices still stay on the assembly path when raw global array type text is removed. Earlier in Step 3, this slice added backend-IR contract coverage in `tests/backend/backend_ir_tests.cpp` for the structured helper signature and direct-call metadata produced by `lower_to_backend_ir(make_typed_direct_call_two_arg_module())`, including preserved helper definition/linkage, structured parameter scalar kinds, direct-global callee shape, and normalized call operands. An attempted explicit x86 backend-module two-argument direct-call seam was intentionally backed out after it overmatched broader production cases and broke the monotonic full-suite guard; the safe result keeps behavior unchanged while locking in the backend-owned contract that a narrower future seam can consume. A later slice records structured `BackendBinaryInst::value_type` during generic LIR->backend lowering, adds backend-IR validation coverage for the normalized single-argument direct-call add-immediate slice after clearing legacy signature/call/binary type text, and updates the x86 backend-module direct-call add-immediate seam to accept that structured binary metadata instead of requiring raw `type_str == "i32"`. A later slice extends the backend-owned call contract so backend IR printing no longer depends on duplicated raw call-argument type text when structured call param metadata is present: `clear_backend_signature_and_call_type_compatibility_shims(...)` now clears typed call operands too, and `src/backend/ir_printer.cpp` renders call operands from structured param metadata instead of legacy arg text. The previous slice removed the remaining raw `signature.return_type == "i32"` gate from the narrowed backend-module global load/store recognizers that already depended on structured backend metadata in `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp`, and added x86/AArch64 scaffold coverage for store-reload, extern scalar load, and extern global-array cases with signature return text cleared alongside the existing memory/global shims. The previous slice removed the same legacy return-type-text dependency from the shared single-function backend-module asm recognizer used by the bounded return-immediate/add/sub seam in `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp`, and added x86/AArch64 scaffold coverage showing the structured return-add slice still stays on the assembly path after `clear_backend_signature_and_call_type_compatibility_shims(...)` clears `signature.return_type`. The previous slice removed the remaining raw `signature.return_type == "i32"` gate from the explicit backend-module countdown-loop recognizer in `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp`, and added x86/AArch64 scaffold coverage showing the lowered countdown asm path still stays active after `clear_backend_signature_and_call_type_compatibility_shims(...)` clears `signature.return_type`. The previous slice removed the same legacy signature return-text gate from the explicit backend-module conditional-phi-join recognizer in `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp`, and added x86/AArch64 scaffold coverage showing the lowered phi-join add asm path still stays active after `clear_backend_signature_and_call_type_compatibility_shims(...)` clears `signature.return_type`. The previous slice removed the same legacy signature return-text gate from the explicit backend-module conditional-return recognizer in `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp`, and added x86/AArch64 scaffold coverage showing the lowered conditional-return asm path still stays active after `clear_backend_signature_and_call_type_compatibility_shims(...)` clears `signature.return_type`. The previous slice adds an AArch64 backend-module scaffold regression for `lower_to_backend_ir(make_direct_call_module())` after `clear_backend_signature_and_call_type_compatibility_shims(...)`, switches the zero-arg direct-call recognizer off raw `main.signature.return_type` text and onto structured signature metadata, and also removes the same raw return-text gate from the adjacent AArch64 global-int-pointer-diff backend-module recognizer. The previous slice adds an AArch64 scaffold regression for `lower_to_backend_ir(make_typed_direct_call_two_arg_folded_const_module())` after clearing signature/call compatibility shims, switches the folded two-argument direct-call recognizer off raw `main.signature.return_type` text, and removes the remaining raw two-argument folded-call dependencies on legacy call/parameter/binary type text by consuming structured backend call metadata plus structured helper param/binary types instead. The previous slice lowers the bounded local-array fast path into structured backend IR by recording backend-owned local stack-slot addresses plus structured store/load/add scalar metadata in `src/backend/lowering/lir_to_backend_ir.cpp`, adds backend-IR contract coverage for that local-array slice in `tests/backend/backend_ir_tests.cpp`, and teaches the x86/AArch64 backend-module emitters to keep the slice on the asm path from explicit lowered input even after `clear_backend_signature_and_call_type_compatibility_shims(...)` and `clear_backend_memory_type_compatibility_shims(...)` remove legacy signature/load/store/binary text. Validation: `./build/backend_ir_tests`, `./build/backend_lir_adapter_tests`, `./build/backend_lir_adapter_x86_64_tests`, and `./build/backend_lir_adapter_aarch64_tests` passed; `ctest --test-dir build -j --output-on-failure > test_fail_after.log` finished with `2667 passed / 2 failed`; `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed; the same existing `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object` failures remained; and no new failures were introduced.
  - Blockers: None.
- [ ] Step 4: Shift target codegen onto backend semantics
  - Notes: Defer until at least one structured operand/call slice exists in backend IR.
  - Blockers: Depends on Step 3.
- [ ] Step 5: Validate positioning for later BIR work
  - Notes: Closeout only after the active structural slice lands and backend validation is stable.
  - Blockers: Depends on prior steps.

Current Step: Step 3
Next Step: Extend backend-owned global metadata to the next bounded non-array scalar surface that still depends on raw global text outside the explicit backend-module seam
Blockers: None

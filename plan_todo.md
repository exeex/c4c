Status: Active
Source Idea: ideas/open/03_lir_to_backend_ir_refactor.md
Source Plan: plan.md

# Active Queue: LIR To Backend IR Refactor

## Queue
- [x] Step 1: Re-establish the lowering boundary
  - Notes: Production lowering moved behind `src/backend/lowering/lir_to_backend_ir.*`, with `src/backend/lir_adapter.hpp` kept as the compatibility shim.
  - Blockers: None.
- [x] Step 2: Isolate LIR syntax decoding
  - Notes: Shared decode/parsing helpers in `src/backend/lowering/call_decode.*` now own the staged LIR call and signature parsing that had been spread across target emitters.
  - Blockers: None.
- [ ] Step 3: Make backend IR more backend-native
  - Notes: The latest slice widens the lowering-owned constant-conditional goto adapter in `src/backend/lowering/lir_to_backend_ir.cpp` so predicate-sourced integer casts no longer require the old `zext i1 -> i32` special case. A bounded branch-only chain `icmp` -> `zext i1 -> i8` -> `sext i8 -> i32` -> `sext i32 -> i64` -> `icmp ne i64 0` now collapses to a single structured backend `ret i32 0` instead of depending on the generic AArch64 constant-folder path. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp`, `tests/backend/backend_lir_adapter_aarch64_tests.cpp`, and `tests/backend/backend_test_fixtures.hpp` locks both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that small-integer cast slice. The previous slice widened the same adapter so it tracks constant integer values with explicit bit-width metadata and resolves one more mixed-cast chain on the structured path: `icmp` -> `zext i1 -> i32` -> `sext i32 -> i64` -> `icmp ne i64 0` now collapses to a single structured backend `ret i32 0` instead of falling through to the generic multi-block rejection path. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp` and `tests/backend/backend_test_fixtures.hpp` locks that mixed-cast slice to a direct return with no remaining conditional or unconditional branches. The previous slice widened the same adapter so it accepts constant-resolved `i64` integer compares in addition to the earlier `i32` path, collapsing that multi-block branch-only chain into a single structured backend `ret i32 0` instead of leaving it to the generic AArch64 mini-interpreter path. New regression coverage had already locked the lowered backend IR for that `i64` slice to a direct return with no remaining conditional or unconditional branches. The prior slice added a lowering-owned adapter for a constant-resolved direct-LIR conditional that only flows through branch-only blocks before returning a constant `i32`. `src/backend/lowering/lir_to_backend_ir.cpp` now collapses that multi-block shape into a single structured backend return instead of leaving it to the generic AArch64 mini-interpreter path. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp` asserts the lowered backend IR drops both conditional and unconditional branch instructions for that slice, and `tests/backend/backend_lir_adapter_aarch64_tests.cpp` locks the direct `LirModule` AArch64 output to the same assembly as the explicit lowered backend path. The previous slice deleted the remaining AArch64-only countdown-specific direct-LIR fallback helper `parse_minimal_lir_single_scalar_countdown_imm()` from `src/backend/aarch64/codegen/emit.cpp` after proving the shared lowering already owns that slice. Earlier Step 3 slices extended `ParsedBackendMinimalCallCrossingDirectCallModuleView` with lowering-owned `regalloc_source_value` metadata sourced from the structured backend `source_add` SSA name, updated shared parser regression coverage in `tests/backend/backend_lir_adapter_tests.cpp` to assert that preserved metadata explicitly, and rewired the x86 call-crossing asm path to consume the structured source name instead of reparsing legacy LIR just to recover regalloc ownership. The x86 synthesized backend-only regalloc path now keys the preserved call-crossing source register from that structured metadata too, so renamed lowered call-crossing seams continue to stay on the asm path without fallback. The prior slice removed the duplicate AArch64-local legacy call-crossing regalloc-source parser from `src/backend/aarch64/codegen/emit.cpp` and rewired that fallback to reuse the shared lowering-owned `parse_backend_single_helper_call_crossing_source_value()` helper that x86 already consumed. Shared regression coverage in `tests/backend/backend_lir_adapter_tests.cpp` had already proved the helper preserves renamed helper parameter names plus renamed source, call, and return SSA values for the legacy call-crossing seam. Earlier Step 3 slices introduced `parse_backend_minimal_declared_direct_call_module()` in `src/backend/lowering/call_decode.hpp`, rewired the x86 declared-direct-call seam in `src/backend/x86/codegen/emit.cpp` to consume that lowering-owned backend-module matcher, and centralized the structured direct-call matcher family behind lowering-owned decode.
  - Blockers: None.
- [ ] Step 4: Shift target codegen onto backend semantics
  - Notes: Step 4 x86 cutovers continue to replace emitter-local compatibility reshaping with direct lowering-owned parsed-module views. The zero-argument direct-call asm path consumes `ParsedBackendMinimalDirectCallModuleView` directly, `direct_call_add_imm` now emits from `ParsedBackendMinimalDirectCallAddImmModuleView`, and the earlier x86 slice removed the backend-IR `two_arg_direct_call` reshaping layer so the x86 asm fast path emits directly from `ParsedBackendMinimalTwoArgDirectCallModuleView`. A later x86 slice removed the backend-only declared-direct-call reshaping shim, so `src/backend/x86/codegen/emit.cpp` emits that asm path directly from `ParsedBackendMinimalDeclaredDirectCallModuleView` instead of copying lowering-owned metadata into a target-local slice. This iteration removes the remaining x86-only LIR `extern_decl` compatibility parser and slice too: the direct x86 `LirModule` emit entrypoint now lowers first and reuses the same lowering-owned declared-direct-call matcher as the explicit backend emit surface, and regression coverage locks the direct LIR emit seam to the same assembly as the structured backend path for inferred declared direct calls. The newer AArch64 slices now mirror that pattern for the backend-owned structured direct-call family: `src/backend/aarch64/codegen/emit.cpp` first dropped the target-local `direct_call_add_imm` wrapper, then removed the backend-IR `two_arg_direct_call` reshaping layer, then removed the folded two-argument compatibility wrapper, then removed the zero-argument direct-call compatibility wrapper, then removed the AArch64-local `MinimalCallCrossingDirectCallSlice` reshaping layer so the call-crossing asm fast path emits directly from `ParsedBackendMinimalCallCrossingDirectCallModuleView` while keying shared regalloc from lowering-owned source metadata, then added `emit_minimal_declared_direct_call_asm()` so the minimal AArch64 extern-decl direct-call seam emits directly from `ParsedBackendMinimalDeclaredDirectCallModuleView` for both explicit backend-IR input and the direct `LirModule` emit surface, then removed the explicit-backend-entry fake `LirModule` scaffolds for the AArch64 global/string-backed fast paths so scalar-global, extern-global, ptrdiff, and string-literal asm emission now consumes the structured `BackendModule` target metadata directly, then did the same for the structured local-array fast path, then removed the last remaining explicit-backend-entry target-triple-only scaffold for `conditional_return` so both AArch64 emit surfaces now drive that asm path directly from `BackendModule` target metadata, then removed the duplicate AArch64 direct-`LirModule` local-array matcher so the direct entrypoint lowers first and reuses the structured backend local-array matcher, and the latest slice extends that convergence to lowered minimal return-immediate output even when the original direct LIR required non-minimal lowering first, which keeps both the `i64` and mixed-cast constant-conditional goto slices on byte-for-byte identical assembly between direct and explicit-lowered AArch64 emit surfaces. The remaining direct-entry-only `parse_minimal_lir_single_scalar_countdown_imm()` and `try_constant_fold_single_block()` helpers are now explicitly documented as fallback-only transitional shims for shapes that still do not have dedicated structured backend matchers.
  - Blockers: Any further removal of fallback-only direct-LIR helpers now depends on new Step 3 structured backend surfaces rather than more AArch64 emitter-local reshaping.
- [ ] Step 5: Validate positioning for later BIR work
  - Notes: Closeout only after the active structural slice lands and backend validation is stable.
  - Blockers: Depends on prior steps.

Current Step: Step 3
Next Step: Find the next bounded branch-only constant-return shape that still routes through `try_constant_fold_single_block()` because it needs more than compare/cast/binop resolution, and capture that seam with a shared lowered-IR regression first.
Blockers: None

Latest Iteration Note: The shared constant-conditional goto matcher now resolves a truncation-heavy branch-only return chain with intermediate integer binops, so the `icmp -> zext i1 -> shl i32 -> trunc i8 -> xor i8 -> icmp ne 0` slice lowers to a single backend `ret i32 0` instead of depending on the generic direct-LIR constant folder.

Iteration Update: Added a new shared regression fixture for a truncation-heavy constant-conditional goto-return shape, taught `src/backend/lowering/lir_to_backend_ir.cpp` to evaluate integer binops inside the constant-resolved matcher while still requiring an actual conditional-branch path, and extended the AArch64 direct-vs-lowered parity coverage so that slice stays aligned on identical assembly output.

Validation: `cmake --build build -j8 --target backend_lir_adapter_aarch64_tests backend_lir_adapter_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` passed for the touched shared-lowering and AArch64 emitter surfaces. `cmake --build build -j8` also passed. Full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total with the same pre-existing failures `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`. The monotonic guard against `test_fail_before.log` and `test_fail_after.log` also passed: `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed`.

Next Slice Hint: Start from another direct-LIR branch-only constant-return slice that still reaches the AArch64 direct-entry fallback after compare/cast/binop evaluation, capture the exact lowered backend shape it should produce, and widen the shared matcher only enough to cover that next bounded form.

Iteration Target: Keep converting one fallback-only direct-LIR constant-return slice at a time into the shared lowering-owned path until the remaining direct-entry fallback helpers are documented as truly residual shims.

# AArch64 Backend Bring-up Todo

Status: Active
Source Idea: ideas/open/02_backend_aarch64_port_plan.md
Source Plan: plan.md

## Active Item

- [ ] Step 2: Continue the post-codegen AArch64-local linker audit by evaluating `src/backend/aarch64/linker/emit_static.cpp` next; only promote it if it remains a bounded compile-only structural mirror with no live include or symbol edges beyond the existing linker slice now rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, `types.cpp`, `input.cpp`, `reloc.cpp`, `plt_got.cpp`, `link.cpp`, `emit_dynamic.cpp`, and `emit_shared.cpp`

## Planned Queue

- [ ] Step 1: Inventory and compile the shared backend slice
- [ ] Step 2: Inventory and compile the AArch64-local slice
- [ ] Step 3: Reconnect backend entry points
- [ ] Step 4: Attach the current LIR boundary with the smallest shim
- [ ] Step 5: Emit minimal AArch64 assembly
- [ ] Step 6: Prove external `llvm-mc` assembly to ELF object
- [ ] Step 7: Tighten behavior in ref order after the bring-up gates pass

## Completed Items

- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/emit_shared.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/emit_shared.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/linker/emit_shared.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, `types.cpp`, `input.cpp`, `reloc.cpp`, `plt_got.cpp`, `link.cpp`, and `emit_dynamic.cpp`; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests

- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/emit_dynamic.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/emit_dynamic.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/linker/emit_dynamic.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, `types.cpp`, `input.cpp`, `reloc.cpp`, `plt_got.cpp`, and `link.cpp`; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests

- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/link.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/link.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/linker/link.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, `types.cpp`, `input.cpp`, `reloc.cpp`, and `plt_got.cpp`; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/plt_got.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/plt_got.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/linker/reloc.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, `types.cpp`, and `input.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/reloc.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/aarch64/linker/plt_got.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, `types.cpp`, `input.cpp`, and `reloc.cpp`; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/system.cpp`; the existing `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/system.cpp` succeeded, and `cmake -S . -B build` still configured cleanly before promotion
- [x] Evaluated `src/backend/aarch64/assembler/encoder/system.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/asm_emitter.cpp`; the existing clean-tree `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/asm_emitter.cpp` succeeded, and `cmake -S . -B build` still configured cleanly before promotion
- [x] Evaluated `src/backend/aarch64/codegen/asm_emitter.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new live include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/intrinsics.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/intrinsics.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/intrinsics.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/intrinsics.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); monotonicity was verified directly by comparing the identical failed-test tail and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/i128_ops.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/i128_ops.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/i128_ops.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/i128_ops.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/float_ops.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/float_ops.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/float_ops.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/float_ops.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/f128.cpp`; the existing clean-tree `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/f128.cpp` succeeded, and `cmake -S . -B build` still configured cleanly before promotion
- [x] Evaluated `src/backend/aarch64/codegen/f128.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new live include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed; this checkout still does not contain `check_monotonic_regression.py`, so monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/prologue.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/prologue.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and the existing clean-tree `test_fail_before.log` baseline all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/prologue.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed; monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`

- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/cast_ops.cpp`; the existing clean-tree `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/cast_ops.cpp` succeeded, and `cmake -S . -B build` still configured cleanly before promotion
- [x] Evaluated `src/backend/aarch64/codegen/cast_ops.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests

- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/calls.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/calls.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and the existing `test_fail_before.log` baseline all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/calls.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/calls.cpp` succeeds, `cmake -S . -B build` and `cmake --build build -j8` succeed, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed; this checkout still does not contain `check_monotonic_regression.py`, so monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/returns.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/returns.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure | tee test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/returns.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/returns.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed; monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/comparison.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/comparison.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/comparison.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/comparison.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed; monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`

- [x] Activated `ideas/open/02_backend_aarch64_port_plan.md` into the active runbook
- [x] Restored real-build reachability for the current backend entry slice by aligning CMake with the mirrored AArch64 subtree and adding a thin `src/backend/aarch64/codegen/emit.*` shim
- [x] Repaired the shared backend entry contract so known non-AArch64 targets still route through the backend path while AArch64 keeps explicit target-local validation
- [x] Added the first five stub-only shared backend units (`state`, `traits`, `liveness`, `regalloc`, `generation`) to both `c4cll` and `backend_lir_adapter_tests`, keeping the full suite monotonic at the existing 4 known failures
- [x] Added `src/backend/stack_layout/mod.cpp` to both `c4cll` and `backend_lir_adapter_tests`, keeping the full suite monotonic at the same 4 known failures
- [x] Added `src/backend/common.cpp` to both `c4cll` and `backend_lir_adapter_tests`; `backend_lir_adapter_tests` still passes and the full suite remains at the same 4 known failures
- [x] Added `src/backend/asm_expr.cpp` to both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges
- [x] Added `src/backend/asm_preprocess.cpp` to both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges
- [x] Captured the pre-change baseline for the `call_abi.cpp` slice: `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and `ctest --test-dir build -j8 --output-on-failure > test_before.log` all hold with the same 4 known unrelated full-suite failures
- [x] Added `src/backend/call_abi.cpp` to both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `backend_lir_adapter_tests` still passes, and `test_after.log` preserves the same 4 known unrelated full-suite failures as `test_before.log`
- [x] Added `src/backend/cast.cpp` to both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `backend_lir_adapter_tests` still passes, and full `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Added `src/backend/f128_softfloat.cpp` to both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `backend_lir_adapter_tests` still passes, and both `test_before.log` and `test_after.log` report the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/inline_asm.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/x86_common.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/peephole_common.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/mod.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/elf_writer_common.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/elf/mod.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a stub-only shared backend mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and full `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/mod.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a comment-only AArch64-local mirror with no new dependency edges, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and the regression guard reports `549/553` passed both before and after with the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/mod.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it remains a comment-only AArch64-local mirror alongside the existing `emit.*` shim, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same `549/553` pass count with the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/mod.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it stays stub-light by using a local placeholder `AsmStatement` instead of dragging in parser or linker wiring, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/mod.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and the regression guard reports `549/553` passed both before and after with the same 4 known unrelated full-suite failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/parser.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it stays stub-light with only standard-library data holders plus a placeholder `parse_asm` passthrough, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes with a new parser stub smoke check, and full `ctest --test-dir build -j8 --output-on-failure` still reports the same `549/553` pass count and the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/elf_writer.cpp` and promoted it into both `c4cll` and `backend_lir_adapter_tests`; it stays stub-light with only local pending-record structs and a reloc helper, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes with a new branch-relocation helper smoke check, and both `test_before.log` and `test_after.log` still report the same `549/553` pass count and the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/mod.cpp`; `cmake -S . -B build`, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/mod.cpp`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/encoder/mod.cpp` and promoted it into both build targets; it remains a stub-light top-level encoder shim with only standard-library dependencies, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes with a new encoder-helper smoke check, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes at the same `549/553` full-suite result with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/mod.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/mod.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/linker/mod.cpp` and promoted it into both build targets; it remains a comment-only top-level linker mirror without pulling `src/backend/aarch64/linker/*.cpp` submodules into the active slice, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes at the same `549/553` full-suite result with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/elf.cpp`; the existing `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/elf.cpp` succeeded, and the file remained a comment-only structural mirror before promotion
- [x] Evaluated `src/backend/aarch64/linker/elf.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local linker mirror with no live include or symbol edges beyond the existing `src/backend/aarch64/linker/mod.cpp` slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests

## Next Intended Slice

- inspect the remaining `src/backend/aarch64/` inventory for any unpromoted compile-only mirrors outside `codegen/` before declaring the current Step 2 source-list promotion wave complete
- evaluate `src/backend/aarch64/linker/types.cpp` next; if it introduces real shared-linker type edges beyond the current inert linker slice, stop Step 2 promotion there instead of widening the linker surface ad hoc
- if the AArch64-local source-list inventory is exhausted, start the Step 3 backend-driver reachability inventory under `src/backend/backend.cpp`, `src/backend/mod.cpp`, and `src/codegen/`
- keep the AArch64 shim thin until the shared backend slice grows enough to replace LLVM-text passthroughs deliberately
- defer encoder-heavy and linker-heavy AArch64 modules until the plain shared backend slice is build-reachable

## Blockers

- mirrored shared backend files under `src/backend/` are still mostly disconnected from the build graph beyond the minimal entry slice
- full suite still has unrelated pre-existing failures: `positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`

## Resume Notes

- the active plan was activated from the highest-priority open backend idea
- keep this run focused on compile integration and backend/LIR wiring before broader behavior work
- do not absorb regalloc, built-in assembler, or built-in linker initiatives unless they are strictly required for the current slice
- current slice outcome: `cmake -S . -B build` and `cmake --build build -j8` now succeed with the mirrored AArch64 entry path wired through `src/backend/aarch64/codegen/emit.*`
- validation after this slice: targeted backend tests pass and full `ctest --test-dir build -j8 --output-on-failure` now reports 4 failing tests instead of the earlier 5, removing the backend entry regression
- current iteration target: evaluate `src/backend/aarch64/codegen/atomics.cpp` without broadening the AArch64-local codegen slice past the current `mod.cpp` plus `emit.cpp` boundary
- latest validation: after adding `src/backend/call_abi.cpp` to both build targets, `backend_lir_adapter_tests` passes and both `test_before.log` and `test_after.log` report the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- current iteration target: promote `src/backend/cast.cpp` into both build targets as the next pure stub-only shared backend slice, keeping Step 1 focused on compile integration rather than target behavior
- latest validation: after adding `src/backend/cast.cpp` to both build targets, `backend_lir_adapter_tests` passes and full `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- current iteration target: evaluate `src/backend/x86_common.cpp` as the next top-level shared backend candidate after `src/backend/inline_asm.cpp`
- latest validation: after adding `src/backend/inline_asm.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` remain monotonic at the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- current iteration target: evaluate `src/backend/peephole_common.cpp` as the next top-level shared backend candidate after `src/backend/x86_common.cpp`
- latest validation: after adding `src/backend/x86_common.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` remain monotonic at the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- current iteration scope: treat `src/backend/peephole_common.cpp` like the earlier stub-only shared backend promotions and stop immediately if it pulls in new target-local dependencies or breaks build monotonicity
- latest validation: after adding `src/backend/peephole_common.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` remain monotonic at the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- next candidate: evaluate `src/backend/mod.cpp` first, then `src/backend/elf_writer_common.cpp` if `mod.cpp` stays outside the active slice or adds unexpected coupling
- latest validation: after adding `src/backend/mod.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` remain monotonic at the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- next candidate: evaluate `src/backend/elf/mod.cpp` next, but only promote it if it stays as dependency-light as `src/backend/elf_writer_common.cpp`; otherwise skip to another top-level shared backend stub with no new target-local coupling
- latest validation: after adding `src/backend/elf_writer_common.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` remain monotonic at the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- latest validation: after adding `src/backend/elf/mod.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and full `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- latest validation: after adding `src/backend/aarch64/mod.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- latest validation: after adding `src/backend/aarch64/codegen/mod.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` still report `549/553` passed with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- latest validation: after adding `src/backend/aarch64/assembler/mod.cpp` to both build targets, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/mod.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- latest validation: after adding `src/backend/aarch64/assembler/parser.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes with the new parser stub smoke check, and full `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- latest validation: after adding `src/backend/aarch64/assembler/elf_writer.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes with the new branch-relocation helper smoke check, and full `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- latest validation: after adding `src/backend/aarch64/assembler/encoder/mod.cpp` to both build targets, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/mod.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes with the new encoder-helper smoke check, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- latest validation: after adding `src/backend/aarch64/linker/mod.cpp` to both build targets, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/mod.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- latest validation: after adding `src/backend/aarch64/linker/elf.cpp` to both build targets, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/elf.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/linker_common/mod.cpp` and promoted it into both build targets because it remains a top-level namespace-only mirror with no include or symbol edges into the deeper shared linker helper submodules
- [x] Evaluated `src/backend/linker_common/args.cpp` and promoted it into both build targets; it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` preserve the same `549/553` pass count with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/archive.cpp` and promoted it into both build targets; it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` preserves the same `549/553` pass count with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- latest validation: after adding `src/backend/linker_common/mod.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_before.log --after test_after.log --allow-non-decreasing-passed` passes with no new failing tests
- latest validation: after adding `src/backend/linker_common/args.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_before.log` and `test_after.log` still report the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- note: `scripts/check_monotonic_regression.py` is not present in this checkout, so monotonicity for this slice was verified directly from `test_before.log` and `test_after.log`
- latest validation: after adding `src/backend/linker_common/archive.cpp` to both build targets, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/linker_common/check.cpp` and promoted it into both build targets; it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/linker_common/dynamic.cpp`; `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/dynamic.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/linker_common/dynstr.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/linker_common/eh_frame.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/linker_common/gc_sections.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/gc_sections.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/gc_sections.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/gc_sections.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/linker_common/hash.cpp`; `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/hash.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/hash.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/linker_common/merge.cpp`; `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/merge.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/linker_common/parse_object.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); this checkout still does not contain `check_monotonic_regression.py`, so monotonicity was verified directly by comparing `test_fail_before.log` and `test_fail_after.log`
- [x] Evaluated `src/backend/linker_common/parse_shared.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/linker_common/resolve_lib.cpp`; `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/resolve_lib.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/resolve_lib.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); this checkout still does not contain `check_monotonic_regression.py`, so monotonicity was verified directly by comparing the identical failed-test tails and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/linker_common/section_map.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/section_map.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/section_map.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/section_map.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/linker_common/symbols.cpp`; `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/linker_common/symbols.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Evaluated `src/backend/linker_common/types.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/types.cpp` succeeds, `cmake -S . -B build` and `cmake --build build -j8` succeed, `./build/backend_lir_adapter_tests` passes, and `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); this checkout still does not contain `check_monotonic_regression.py`, so monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Evaluated `src/backend/linker_common/write.cpp` and promoted it into both build targets because it remains a stub-only shared linker-helper mirror with no new include or symbol edges into the deeper `src/backend/linker_common/*.cpp` submodules, `clang++ -std=c++17 -fsyntax-only src/backend/linker_common/write.cpp` succeeds, `cmake -S . -B build` and `cmake --build build -j8` succeed, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/alu.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/alu.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/alu.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/alu.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/globals.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/globals.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/globals.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/globals.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/memory.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/memory.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/memory.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/memory.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and both `test_fail_before.log` and `test_fail_after.log` still report the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/variadic.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/variadic.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/variadic.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/variadic.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/atomics.cpp`; the existing clean-tree `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/atomics.cpp` succeeded, and the file still appeared to be a comment-only structural mirror before promotion
- [x] Evaluated `src/backend/aarch64/codegen/atomics.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new live include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, and `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`); monotonicity was verified directly by comparing the identical failed-test list and pass-count summary lines in `test_fail_before.log` and `test_fail_after.log`
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/inline_asm.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/inline_asm.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/inline_asm.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp` and `emit.cpp` slice; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/inline_asm.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/codegen/peephole.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/peephole.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held on the clean tree with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/codegen/peephole.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no new live include or symbol edges beyond the existing `src/backend/aarch64/codegen/mod.cpp`, `emit.cpp`, and the already-promoted comment-only shared helper `src/backend/peephole_common.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/codegen/peephole.cpp` succeeds, `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/bitfield.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/bitfield.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Audited the remaining `src/backend/aarch64/` inventory outside `codegen/` and confirmed Step 2 is not yet ready to close because unpromoted compile-only mirrors still remain under `assembler/encoder/` and `linker/`; `src/backend/aarch64/assembler/encoder/bitfield.cpp` was selected as the next bounded slice because it is a comment-only structural mirror that adds no live include or symbol edges beyond the existing `src/backend/aarch64/assembler/encoder/mod.cpp` slice
- [x] Evaluated `src/backend/aarch64/assembler/encoder/bitfield.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake -S . -B build` and `cmake --build build -j8` succeed, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/compare_branch.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/compare_branch.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/encoder/compare_branch.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed, and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/data_processing.cpp`; the existing `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/data_processing.cpp` succeeded, and the file remained a comment-only structural mirror before promotion
- [x] Evaluated `src/backend/aarch64/assembler/encoder/data_processing.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake -S . -B build` and `cmake --build build -j8` succeed, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/fp_scalar.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/fp_scalar.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/encoder/fp_scalar.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/load_store.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/load_store.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/encoder/load_store.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/assembler/encoder/neon.cpp`; `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/assembler/encoder/neon.cpp`, `cmake -S . -B build`, `cmake --build build -j8`, `./build/backend_lir_adapter_tests`, and full `ctest --test-dir build -j8 --output-on-failure > test_fail_before.log` all held with the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`)
- [x] Evaluated `src/backend/aarch64/assembler/encoder/neon.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing assembler encoder slice; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/types.cpp`; the existing `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/types.cpp` succeeded, and the file remained a comment-only structural mirror before promotion
- [x] Evaluated `src/backend/aarch64/linker/types.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp` and `elf.cpp`; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests
- [x] Recorded the pre-change baseline for `src/backend/aarch64/linker/input.cpp`; the existing `test_fail_before.log` baseline remained the current reference at `549/553` passed with the same 4 known unrelated failures, `clang++ -std=c++17 -fsyntax-only src/backend/aarch64/linker/input.cpp` succeeded, and `cmake -S . -B build` still configured cleanly before promotion
- [x] Evaluated `src/backend/aarch64/linker/input.cpp` and promoted it into both build targets because it remains a comment-only AArch64-local structural mirror with no live include or symbol edges beyond the existing linker slice rooted at `src/backend/aarch64/linker/mod.cpp`, `elf.cpp`, and `types.cpp`; `cmake --build build -j8` succeeds, `./build/backend_lir_adapter_tests` passes, `ctest --test-dir build -j8 --output-on-failure` still reports the same 4 known unrelated failures at `549/553` passed (`positive_sema_ok_fn_returns_variadic_fn_ptr_c`, `cpp_positive_sema_decltype_bf16_builtin_cpp`, `cpp_positive_sema_eastl_probe_initializer_list_runtime_cpp`, `cpp_llvm_initializer_list_runtime_materialization`), and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passes with no new failing tests

Status: Active
Source Idea: ideas/open/03_lir_to_backend_ir_refactor.md
Source Plan: plan.md

# Active Queue: LIR To Backend IR Refactor

## Queue
- [x] Step 1: Re-establish the lowering boundary
  - Notes: Production lowering moved behind `src/backend/lowering/lir_to_backend_ir.*`, with `src/backend/lir_adapter.hpp` kept as the compatibility shim.
  - Blockers: None.
- [x] Step 2: Isolate LIR syntax decoding
  - Notes: Shared decode/parsing helpers in `src/backend/lowering/call_decode.*` now own the staged LIR call and signature parsing that had been spread across target emitters.
  - Blockers: None.
- [ ] Step 3: Make backend IR more backend-native
  - Notes: The latest slice widens the same lowering-owned matcher in `src/backend/lowering/lir_to_backend_ir.cpp` from a bounded one-or-four-slot local `i32` state map to a bounded one-or-five-slot state map. A branch-only chain `store i32 0, %lv.state` -> `load i32 %lv.state` -> `icmp eq i32 0` -> `zext i1 -> i32` -> `store i32 %lv.tmp0` -> `load i32 %lv.tmp0` -> `icmp ne i32 0` -> `zext i1 -> i32` -> `store i32 %lv.tmp1` -> `load i32 %lv.tmp1` -> `icmp ne i32 0` -> `zext i1 -> i32` -> `store i32 %lv.tmp2` -> `load i32 %lv.tmp2` -> `icmp ne i32 0` -> `zext i1 -> i32` -> `store i32 %lv.flag` -> `load i32 %lv.flag` -> `icmp ne i32 0` now collapses to a single structured backend `ret i32 0` instead of bailing on entry allocas once a fifth local `i32` slot appears. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp`, `tests/backend/backend_lir_adapter_aarch64_tests.cpp`, and `tests/backend/backend_test_fixtures.hpp` locks both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that five-local-slot-backed slice. The previous slice widened the same lowering-owned matcher in `src/backend/lowering/lir_to_backend_ir.cpp` from a bounded one-or-three-slot local `i32` state map to a bounded one-or-four-slot state map. A branch-only chain `store i32 0, %lv.state` -> `load i32 %lv.state` -> `icmp eq i32 0` -> `zext i1 -> i32` -> `store i32 %lv.tmp0` -> `load i32 %lv.tmp0` -> `icmp ne i32 0` -> `zext i1 -> i32` -> `store i32 %lv.tmp1` -> `load i32 %lv.tmp1` -> `icmp ne i32 0` -> `zext i1 -> i32` -> `store i32 %lv.flag` -> `load i32 %lv.flag` -> `icmp ne i32 0` now collapses to a single structured backend `ret i32 0` instead of bailing on entry allocas once a fourth local `i32` slot appears. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp`, `tests/backend/backend_lir_adapter_aarch64_tests.cpp`, and `tests/backend/backend_test_fixtures.hpp` locks both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that four-local-slot-backed slice. The previous slice widened the same lowering-owned matcher in `src/backend/lowering/lir_to_backend_ir.cpp` from a bounded one-or-two-slot local `i32` state map to a bounded one-or-three-slot state map. A branch-only chain `store i32 0, %lv.state` -> `load i32 %lv.state` -> `icmp eq i32 0` -> `zext i1 -> i32` -> `store i32 %lv.tmp` -> `load i32 %lv.tmp` -> `icmp ne i32 0` -> `zext i1 -> i32` -> `store i32 %lv.flag` -> `load i32 %lv.flag` -> `icmp ne i32 0` now collapses to a single structured backend `ret i32 0` instead of bailing on entry allocas once a third local `i32` slot appears. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp`, `tests/backend/backend_lir_adapter_aarch64_tests.cpp`, and `tests/backend/backend_test_fixtures.hpp` locks both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that three-local-slot-backed slice. The previous slice widened the same lowering-owned matcher in `src/backend/lowering/lir_to_backend_ir.cpp` from one bounded local `i32` slot to a bounded one-or-two-slot local scalar state map. A branch-only chain `store i32 0, %lv.state` -> `load i32 %lv.state` -> `icmp eq i32 0` -> `zext i1 -> i32` -> `store i32 %lv.flag` -> `load i32 %lv.flag` -> `icmp ne i32 0` now collapses to a single structured backend `ret i32 0` instead of falling back to direct-LIR stack-state interpretation once a second local slot appears. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp`, `tests/backend/backend_lir_adapter_aarch64_tests.cpp`, and `tests/backend/backend_test_fixtures.hpp` locks both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that two-local-slot-backed slice. The previous slice added a dedicated lowering-owned matcher for one bounded stack-backed constant-conditional goto seam in `src/backend/lowering/lir_to_backend_ir.cpp`. A branch-only chain `icmp eq i32` -> `zext i1 -> i32` -> `store i32 %lv.flag` -> `load i32 %lv.flag` -> `icmp ne i32 0` now collapses to a single structured backend `ret i32 0` instead of bailing on entry allocas and leaving that shape to direct-LIR stack-state interpretation. New regression coverage in `tests/backend/backend_lir_adapter_tests.cpp`, `tests/backend/backend_lir_adapter_aarch64_tests.cpp`, and `tests/backend/backend_test_fixtures.hpp` locks both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that local-slot-backed slice. The previous slice widened the lowering-owned constant-conditional goto adapter so constant-resolved integer `select` results can stay on the shared structured path instead of falling back to the AArch64 direct-entry mini-interpreter. A bounded branch-only chain `icmp` -> `select i32` -> `icmp ne i32 0` now collapses to a single structured backend `ret i32 0`, and regression coverage already locked both the lowered backend IR and the direct-vs-lowered AArch64 assembly parity for that select-based slice. The earlier Step 3 slices widened the same adapter for predicate-sourced integer casts, mixed-width compare/cast chains, i64 compares, and the original constant-resolved branch-only goto chain; removed the AArch64-only countdown-specific direct-LIR fallback helper `parse_minimal_lir_single_scalar_countdown_imm()` after proving shared lowering owned that slice; preserved call-crossing regalloc metadata in lowering-owned direct-call views; removed the duplicate AArch64-local legacy call-crossing regalloc-source parser in favor of shared lowering decode; and centralized more direct-call matcher families behind `src/backend/lowering/call_decode.hpp`.
  - Blockers: None.
- [ ] Step 4: Shift target codegen onto backend semantics
  - Notes: Step 4 x86 cutovers continue to replace emitter-local compatibility reshaping with direct lowering-owned parsed-module views. The zero-argument direct-call asm path consumes `ParsedBackendMinimalDirectCallModuleView` directly, `direct_call_add_imm` now emits from `ParsedBackendMinimalDirectCallAddImmModuleView`, and the earlier x86 slice removed the backend-IR `two_arg_direct_call` reshaping layer so the x86 asm fast path emits directly from `ParsedBackendMinimalTwoArgDirectCallModuleView`. A later x86 slice removed the backend-only declared-direct-call reshaping shim, so `src/backend/x86/codegen/emit.cpp` emits that asm path directly from `ParsedBackendMinimalDeclaredDirectCallModuleView` instead of copying lowering-owned metadata into a target-local slice. This iteration removes the remaining x86-only LIR `extern_decl` compatibility parser and slice too: the direct x86 `LirModule` emit entrypoint now lowers first and reuses the same lowering-owned declared-direct-call matcher as the explicit backend emit surface, and regression coverage locks the direct LIR emit seam to the same assembly as the structured backend path for inferred declared direct calls. The newer AArch64 slices now mirror that pattern for the backend-owned structured direct-call family: `src/backend/aarch64/codegen/emit.cpp` first dropped the target-local `direct_call_add_imm` wrapper, then removed the backend-IR `two_arg_direct_call` reshaping layer, then removed the folded two-argument compatibility wrapper, then removed the zero-argument direct-call compatibility wrapper, then removed the AArch64-local `MinimalCallCrossingDirectCallSlice` reshaping layer so the call-crossing asm fast path emits directly from `ParsedBackendMinimalCallCrossingDirectCallModuleView` while keying shared regalloc from lowering-owned source metadata, then added `emit_minimal_declared_direct_call_asm()` so the minimal AArch64 extern-decl direct-call seam emits directly from `ParsedBackendMinimalDeclaredDirectCallModuleView` for both explicit backend-IR input and the direct `LirModule` emit surface, then removed the explicit-backend-entry fake `LirModule` scaffolds for the AArch64 global/string-backed fast paths so scalar-global, extern-global, ptrdiff, and string-literal asm emission now consumes the structured `BackendModule` target metadata directly, then did the same for the structured local-array fast path, then removed the last remaining explicit-backend-entry target-triple-only scaffold for `conditional_return` so both AArch64 emit surfaces now drive that asm path directly from `BackendModule` target metadata, then removed the duplicate AArch64 direct-`LirModule` local-array matcher so the direct entrypoint lowers first and reuses the structured backend local-array matcher, and the latest slice extends that convergence to lowered minimal return-immediate output even when the original direct LIR required non-minimal lowering first, which keeps both the `i64` and mixed-cast constant-conditional goto slices on byte-for-byte identical assembly between direct and explicit-lowered AArch64 emit surfaces. The remaining direct-entry-only `parse_minimal_lir_single_scalar_countdown_imm()` and `try_constant_fold_single_block()` helpers are now explicitly documented as fallback-only transitional shims for shapes that still do not have dedicated structured backend matchers.
  - Blockers: Any further removal of fallback-only direct-LIR helpers now depends on new Step 3 structured backend surfaces rather than more AArch64 emitter-local reshaping.
- [ ] Step 5: Validate positioning for later BIR work
  - Notes: Closeout only after the active structural slice lands and backend validation is stable.
  - Blockers: Depends on prior steps.

Current Step: Step 3
Next Step: Find the next branch-only constant-return seam that still falls back because it needs one more bounded structured state surface beyond the current one-or-sixteen local `i32` slot matcher, then capture that exact slice with shared-lowering and AArch64 parity coverage.
Blockers: None

Latest Iteration Note: The shared lowering now owns the bounded sixteen-local-slot-backed constant-conditional goto-return slice, so the next fallback in this family will require at least one more bounded local `i32` state surface than the current matcher accepts.

Iteration Update: Added `make_seven_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to seven `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that seven-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_aarch64_tests backend_lir_adapter_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` passed for the touched shared-lowering and AArch64 emitter surfaces after rebuild. `cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the existing workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed against the checked-in baseline snapshot (`2585` passed / `84` failed / `2669` total). The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-seven-slot matcher.

Iteration Update: Added `make_eight_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to eight `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that eight-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, then passed after widening the matcher. `rm -rf build && cmake -S . -B build && cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed against the checked-in baseline snapshot (`2585` passed / `84` failed / `2669` total). The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Iteration Update: Added `make_nine_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to nine `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that nine-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, then passed after widening the matcher. `cmake --build build -j8` passed. A full-suite rerun in `test_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_before.log --after test_after.log --allow-non-decreasing-passed` passed with `delta : passed=0 failed=0` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-ten-slot matcher.

Iteration Update: Added `make_ten_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to ten `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that ten-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, then passed after widening the matcher. `cmake --build build -j8` passed. A full-suite rerun in `test_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with `delta : passed=82 failed=-82` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-eleven-slot matcher.

Iteration Update: Added `make_eleven_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to eleven `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that eleven-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` passed. `cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with `delta : passed=82 failed=-82` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-twelve-slot matcher.

Iteration Update: Added `make_twelve_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to twelve `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that twelve-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^backend_lir_adapter_tests$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, and `ctest --test-dir build -R '^backend_lir_adapter_aarch64_tests$' --output-on-failure` failed with the same error before widening the matcher; both targeted tests passed after the production change. A later full `cmake --build build -j8` also passed, but the full-suite regression guard against `test_fail_before.log` failed (`2585/84` before vs `2512/157` after) and a same-workspace `HEAD` rerun in `/tmp/test_workspace_head.log` was also unstable (`2537/132`), with the new deltas concentrated in `FRONTEND_FAIL` cases rather than backend-lowering coverage. Treat the full-suite baseline as a workspace blocker that needs separate triage outside this Step 3 slice.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-thirteen-slot matcher.

Iteration Update: Added `make_thirteen_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to thirteen `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that thirteen-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, then passed after widening the matcher. `cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with `delta : passed=82 failed=-82` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-fourteen-slot matcher.

Iteration Update: Added `make_fourteen_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to fourteen `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that fourteen-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, then passed after widening the matcher. `cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with `delta : passed=82 failed=-82` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-fifteen-slot matcher.

Iteration Update: Added `make_fifteen_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to fifteen `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that fifteen-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^backend_lir_adapter_tests$' --output-on-failure` failed before the production change with `minimal backend LIR adapter does not support entry allocas`, and `ctest --test-dir build -R '^backend_lir_adapter_aarch64_tests$' --output-on-failure` failed with the same error before widening the matcher; both targeted tests passed after the production change. `cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with `delta : passed=82 failed=-82` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Keep the same branch-only constant-return family and widen shared lowering only enough to accept one more bounded local scalar state surface than the current one-or-sixteen-slot matcher.

Iteration Update: Added `make_sixteen_local_slot_constant_conditional_goto_return_module()`, widened `src/backend/lowering/lir_to_backend_ir.cpp` so the narrow local-slot constant-conditional matcher now accepts one to sixteen `i32` stack cells through store/load before the branch-only return chain, and extended the adapter plus AArch64 parity regressions so that sixteen-local-slot slice now stays on the shared structured lowering path.

Validation: `cmake --build build -j8 --target backend_lir_adapter_tests backend_lir_adapter_aarch64_tests` passed. `ctest --test-dir build -R '^(backend_lir_adapter_tests|backend_lir_adapter_aarch64_tests)$' --output-on-failure` passed. `cmake --build build -j8` passed. A full-suite rerun in `test_fail_after.log` finished at `2667` passed / `2` failed / `2669` total in this workspace, matching the current workspace ceiling while keeping the same two known failures. The regression guard `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with `delta : passed=82 failed=-82` and zero new failing or >30s tests. The remaining failures are `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Iteration Target: Keep converting one fallback-only direct-LIR constant-return slice at a time into the shared lowering-owned path, but only by adding the next bounded state surface rather than broadening into a generic interpreter.

Status: Active
Source Idea: ideas/open/03_lir_to_backend_ir_refactor.md
Source Plan: plan.md

# Active Queue: LIR To Backend IR Refactor

## Queue
- [x] Step 1: Re-establish the lowering boundary
  - Notes: Moved the production lowering declarations and implementation to `src/backend/lowering/lir_to_backend_ir.*`, kept `src/backend/lir_adapter.hpp` as the legacy shim, and validated the touched backend suites plus a monotonic full-suite regression check.
  - Blockers: None.
- [x] Step 2: Isolate LIR syntax decoding
  - Notes: Shared lowering helpers in `src/backend/lowering/call_decode.*` now own the staged LIR call and signature parsing that had been spread across x86 and AArch64 emitters. Targeted lowering and backend tests remained green, and full-suite regression checks stayed monotonic with the same two known AArch64 object-contract failures.
  - Blockers: None.
- [ ] Step 3: Make backend IR more backend-native
  - Notes: The latest slice removes the x86 typed two-argument direct-call LIR fallback's dependency on fixed lowered helper and parameter names. `src/backend/x86/codegen/emit.cpp` now infers the only non-`main` helper structurally, parses the actual `i32, i32` parameter operands from the helper signature, validates the helper body against those parsed parameter names, and keys every direct-call/local-rewrite branch off the helper's real symbol instead of assuming `add_pair`, `%p.x`, or `%p.y`. New regression coverage renames that helper to `sum_pair`, renames the lowered parameters to `%p.left` and `%p.right`, rewrites the direct call to `@sum_pair`, and confirms the x86 backend still emits the helper and call on the asm path without falling back to LLVM text. The prior slice removes the x86 param-slot direct-call fast path's dependency on fixed lowered helper, parameter, and slot names. `src/backend/x86/codegen/emit.cpp` now infers the only non-`main` helper structurally, parses its actual single `i32` parameter operand from the helper signature, ties the slot rewrite to the observed alloca/store flow, and keys the direct call off the helper's real symbol instead of assuming `add_one`, `%p.x`, or `%lv.param.x`. New regression coverage renames that helper to `inc_value`, renames the lowered parameter to `%p.input`, renames the lowered slot to `%lv.shadow.input`, and confirms the x86 backend still emits the param-slot helper and direct call on the asm path without falling back to LLVM text. The prior slice centralizes the backend-owned synthetic call-crossing regalloc key in both `src/backend/x86/codegen/emit.cpp` and `src/backend/aarch64/codegen/emit.cpp` instead of repeating that fixed literal across the pure-backend-IR parse path. `MinimalCallCrossingDirectCallSlice` still keeps an overridable `regalloc_source_value`, but it now defaults to the shared synthetic backend-owned seam and only gets rewritten when legacy LIR fallback regalloc state is actually needed. New regression coverage lowers the call-crossing slice, clears legacy signature/call type shims, renames both the lowered helper symbol and the lowered preserved-source SSA value, and confirms both backends stay on the asm path from structured callee/call metadata plus the backend-owned synthetic regalloc seam alone. The prior slice extends the Step 3 direct-call audit to renamed zero-argument lowered helpers on both x86 and AArch64. New regression coverage lowers the zero-argument direct-call slice, clears legacy signature/call type shims, renames the lowered helper and direct-call target to `const_value`, and confirms both backends stay on the asm path from structured callee metadata alone. This slice also trims stale call-crossing parse fields from the x86 and AArch64 `MinimalCallCrossingDirectCallSlice` scaffolds so those matchers only carry the backend-owned regalloc seam that still participates in emission. The prior slice teaches the AArch64 backend-module call-crossing asm path to keep its synthesized/shared regalloc seam separate from the lowered backend-IR SSA name. `src/backend/aarch64/codegen/emit.cpp` now carries an explicit `regalloc_source_value` inside `MinimalCallCrossingDirectCallSlice`, parses the legacy LIR preserved-source seam when a fallback module is present, and uses a backend-owned synthetic key on the pure backend-IR path so the call-crossing fast path no longer depends on the lowered source SSA name surviving unchanged. New regression coverage lowers the call-crossing slice, clears legacy signature/call type shims, renames only the lowered preserved-source SSA value across the source add, direct-call argument, and final add seam, and confirms the AArch64 backend still emits `mov w20, #5`, `mov w0, w20`, `bl add_one`, and `add w0, w20, w0` without falling back to backend IR text. The prior slice factors the AArch64 folded two-argument constant-return helper-body contract behind a backend-owned matcher in `src/backend/aarch64/codegen/emit.cpp`, so the structured folded direct-call asm slice no longer accepts any helper body that merely happens to fold through `fold_minimal_direct_call_two_arg_callee_return()`. New regression coverage lowers the folded two-argument direct-call slice, clears legacy signature/call type shims, mutates the lowered helper body so the final `sub` uses the wrong structured parameter, and confirms the AArch64 backend now rejects that malformed folded-helper contract by falling back instead of matching the asm fast path. The prior slice factors the AArch64 zero-argument helper-body contract behind a backend-owned matcher in `src/backend/aarch64/codegen/emit.cpp`, so the structured zero-arg direct-call asm slice no longer open-codes the helper’s single-block immediate-return shape through `parse_single_block_return_imm()`. New regression coverage lowers the zero-arg direct-call slice, clears legacy signature/call type shims, mutates the lowered `helper` body into an explicit `%t.helper.folded = add i32 7, 0` computation, and confirms the AArch64 backend now rejects that malformed structured helper-body contract by falling back instead of matching the asm fast path. The prior slice factors the x86 zero-argument helper-body contract behind a backend-owned matcher in `src/backend/x86/codegen/emit.cpp`, so the structured zero-arg direct-call asm slice no longer open-codes the helper’s single-block immediate-return shape through `parse_single_block_return_imm()`. New regression coverage lowers the zero-arg direct-call slice, clears legacy signature/call type shims, mutates the lowered `helper` body into an explicit `%t.helper.folded = add i32 7, 0` computation, and confirms the x86 backend now rejects that malformed structured helper-body contract by falling back instead of matching the asm fast path. The prior slice factors the x86 definition-backed direct-call structured parse and callee-signature validation behind one local matcher seam inside `src/backend/x86/codegen/emit.cpp`, so the zero-arg, single-arg, two-arg, and call-crossing asm slices no longer each open-code direct-global parse, callee lookup, and signature/definition gating. New regression coverage lowers the zero-arg direct-call slice, clears legacy signature/call type shims, mutates the lowered helper signature to add a bogus `i32` parameter, and confirms the x86 backend now rejects that malformed structured call contract by falling back instead of matching the asm fast path. The prior slice factors the x86 declared-direct-call structured parse and declaration-signature validation behind one local matcher seam inside `src/backend/x86/codegen/emit.cpp`, so the minimal declared-call asm slice no longer open-code direct-global parse, callee lookup, and signature/vararg gating separately from the rest of the specialization. New regression coverage lowers the inferred-parameter extern-declaration slice, clears legacy signature/call type shims, renames the structured declaration and direct call target to `sum_ext`, and confirms the x86 asm path still emits `mov edi, 5`, `mov esi, 7`, and `call sum_ext` without falling back to backend IR text. The prior slice adds a renamed pure-backend-IR AArch64 call-crossing regression and then factors the emitter’s repeated direct-global structured call contract checks behind one local matcher helper. `src/backend/aarch64/codegen/emit.cpp` now routes the zero-arg, single-arg, two-arg, folded two-arg, and call-crossing direct-call asm slices through a shared `match_minimal_structured_direct_call()` seam instead of open-coding the same parse/signature/definition checks in each matcher. New regression coverage lowers the call-crossing direct-call slice, renames the structured helper to `inc_value`, clears legacy signature/call type shims, and confirms the AArch64 asm path still emits `.type inc_value, %function`, `mov w20, #5`, `mov w0, w20`, `bl inc_value`, and `add w0, w20, w0` without falling back to backend IR text. The prior slice removes the AArch64 backend-module call-crossing direct-call asm path’s last hard dependency on legacy LIR fallback when the input is already lowered backend IR. `src/backend/aarch64/codegen/emit.cpp` now synthesizes the single preserved callee-saved assignment the minimal call-crossing slice needs on the pure backend-IR path, while still using shared LIR regalloc when legacy input is available. New regression coverage lowers the structured call-crossing direct-call slice, clears legacy signature/call type shims, and confirms the AArch64 asm path still emits the shared `sub sp, sp, #16` / `mov w20, #5` / `bl add_one` / `add w0, w20, w0` sequence without falling back to backend IR text. The prior slice teaches the AArch64 backend-module zero-arg, single-arg, two-arg, folded two-arg, and call-crossing direct-call asm matchers to parse direct-global calls through the shared backend-owned typed-call view and verify them against the referenced callee signature with `backend_typed_call_matches_signature()` before specializing. New regression coverage lowers the folded two-argument direct-call constant-return slice, renames the structured helper and call target to `const_pair`, clears legacy signature/call type shims, and confirms the AArch64 asm path still emits `bl const_pair` without falling back to backend IR text. The prior slice removes the x86 declared-direct-call matcher’s hard-coded variadic-name allowlist and now trusts only structured declaration metadata (`signature.is_vararg`) when deciding whether to materialize the SysV vararg register-count setup. New regression coverage lowers the existing zero-arg extern-declaration object slice, renames the structured declaration and call site to `printf`, clears the legacy signature/call type shims, and confirms the x86 asm path still emits `call printf` without injecting `mov al, 0` when the structured declaration is explicitly non-variadic. The prior slice teaches shared extern-declaration lowering to preserve a structured trailing vararg marker on inferred backend signatures, so lowered declarations like `printf(ptr, ...)` now carry `signature.is_vararg = true` instead of forcing target-local variadic-name heuristics to recover that contract later. New regression coverage lowers a dedicated `printf` vararg declaration fixture, asserts the structured declaration keeps the fixed `ptr` prefix plus the trailing vararg bit, and confirms the backend IR printer can render `declare i32 @printf(ptr, ...)` after raw signature/call compatibility text is cleared. The prior slice teaches shared backend IR validation to reject functions whose `is_declaration` bit disagrees with the structured signature linkage (`define` vs `declare`), so malformed modules cannot present declaration-only bodies or definition-only signatures to target fast paths. New regression coverage mutates a lowered extern helper declaration into a `define` signature and mutates a lowered `main` definition into a `declare` signature, then confirms the shared validator rejects both linkage-shape mismatches before backend-specific asm selection. The prior slice teaches shared backend IR validation to reject direct-global calls whose structured return type or fixed parameter types disagree with the referenced callee signature, so backend-module asm fast paths no longer have to defend that seam one matcher at a time. New regression coverage mutates lowered two-argument direct calls by changing the helper signature param type after compatibility text is cleared, and confirms the shared validator plus x86 and AArch64 backend emitters all reject the malformed structured call contract before asm specialization. The prior slice taught both backend-module emitters to run shared `validate_backend_ir()` checks before trying handwritten asm fast paths, so structurally invalid lowered modules already fell back to backend IR text instead of being accepted only because an individual matcher missed a contract guard. Earlier slices taught the x86 and AArch64 backend-module scalar-global, scalar-global store/reload, extern scalar-global, and extern global-array asm fast paths to reject contradictory structured load widths instead of trusting only the `i32` result type. Earlier slices also taught the AArch64 backend-module string-literal asm fast path to reject structured byte offsets that escape the referenced string constant bounds, taught string-literal and extern global-array asm fast paths to require explicit `BackendAddressBaseKind` provenance instead of trusting symbol text and byte offsets alone, taught the scalar-global and global-ptrdiff asm fast paths to require explicit `BackendAddressBaseKind::Global` provenance rather than trusting any structured address whose symbol text happens to match a global, taught local-array asm fast paths to require the structured `function.local_slots` contract rather than matching only on shared base symbols and hard-coded offsets, taught `src/backend/ir.hpp` to carry explicit `BackendAddressBaseKind` provenance on backend addresses, updated lowering to stamp global, string-constant, and `local_slot` addresses with structured ownership, and made `src/backend/ir_validate.cpp` reject address-kind mismatches instead of inferring object class from raw symbol spelling alone.
  - Blockers: None.
- [ ] Step 4: Shift target codegen onto backend semantics
  - Notes: Defer until Step 3 lands enough structured operand and call surfaces.
  - Blockers: Depends on Step 3.
- [ ] Step 5: Validate positioning for later BIR work
  - Notes: Closeout only after the active structural slice lands and backend validation is stable.
  - Blockers: Depends on prior steps.

Current Step: Step 3
Next Step: Continue Step 3 by auditing the remaining direct-helper asm matchers for target-local helper-body or local-rewrite assumptions that still mirror legacy LIR shapes, and move the next reusable contract behind shared backend-owned matcher seams.
Blockers: None

Latest Iteration Note: The Step 3 audit now covers the remaining x86 typed two-argument double-rewrite LIR fallback seam. The shared two-slot rewrite matcher no longer depends on the first two slot stores appearing in fixed alloca order before it recognizes the rewritten direct-call slice.

Iteration Update: Added `test_x86_backend_keeps_renamed_typed_two_arg_both_local_arg_slice_on_asm_path()` to `tests/backend/backend_lir_adapter_x86_64_tests.cpp`. The regression renames the helper to `sum_pair`, renames both local slots plus the call-feeding SSA names, inserts an extra unused reload from the first slot immediately before the direct call, rewrites the direct call to `@sum_pair`, and confirms the x86 backend still emits the helper and call on the asm path without falling back to LLVM text. Updated `src/backend/x86/codegen/emit.cpp` so `match_two_arg_local_rewrite_slot_slice()` records which tracked slot produced each actual call operand instead of depending on the last reload from that slot appearing immediately before the call.

Validation: `cmake --build build -j8 --target backend_lir_adapter_x86_64_tests` and `ctest --test-dir build -R '^backend_lir_adapter_x86_64_tests$' --output-on-failure` passed for this slice. Full-suite `ctest --test-dir build -j8 --output-on-failure > test_fail_after.log` finished with the same `2667` passed / `2` failed summary, and `python3 .codex/skills/c4c-regression-guard/scripts/check_monotonic_regression.py --before test_fail_before.log --after test_fail_after.log --allow-non-decreasing-passed` passed with zero new failing tests. The only remaining failures are still the same two known AArch64 object-contract cases already recorded for this plan: `backend_contract_aarch64_return_add_object` and `backend_contract_aarch64_global_load_object`.

Next Slice Hint: Continue the Step 3 audit by checking whether the remaining single-slot typed two-argument local-argument and local-rewrite variants still encode avoidable pre-call instruction-order assumptions that should move behind the same backend-owned operand-contract seam.

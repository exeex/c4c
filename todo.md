# Current Packet

Status: Active
Source Idea Path: ideas/open/51_aarch64_alu_prepared_authority_repair.md
Source Plan Path: plan.md
Current Step ID: Step 7
Current Step Title: Classify remaining stale-home ownership

## Just Finished

Step 6 routed `lower_scalar_select_publication` through shared prepared
select-chain authority.

The direct-global select-chain scalar publication path now asks
`find_prepared_direct_global_select_chain_dependency` for the prepared root and
passes that dependency into `emit_select_chain_value_to_register` instead of
using the ALU-owned `select_chain_contains_direct_global_load` shortcut.

The delegated proof preserved the Step 5 behavior split: both focused ALU
probes and `00176`/`00181` pass, while the known `00164`/`00204` stale-home
runtime failures remain.

The Step 7 route review in `review/alu-step7-route-review.md` found the
Steps 1-6 route aligned with the ALU prepared-authority source idea, but
drifting if treated as closure or more blind ALU implementation. The listed
ALU duplicate-authority items have been consumed, so the remaining failure
family needs ownership classification before more code changes.

## Suggested Next

Execute Step 7 as an ownership-classification packet, not implementation and
not closure.

First reduction target: trace the `00164` post-`%t106` ordinary ALU chain:
`%t113 = %t111 & %t112`, `%t114 = %t110 ^ %t113`, and
`%t115 = %t109 | %t114`.

Trace `%t109`, `%t110`, `%t111`, and `%t112` from prepared value homes through
scalar publication, call clobber or call-boundary publication, and ALU operand
materialization. Classify the stale-home owner as one of:

- ALU operand/source materialization still selecting a stale prepared home.
- Call-boundary or call-argument publication invalidating or failing to
  republish a prepared home.
- A missing shared prepared value-home/source-producer query needed by multiple
  consumers.

If the trace lands in call-boundary or call-argument publication, record that
classification and ask the supervisor to route through
`ideas/open/52_aarch64_calls_prepared_authority_repair.md`. Do not switch
immediately without that trace evidence.

If the trace lands in a missing shared query, define the smallest shared
authority packet and proof subset before implementation.

## Watchouts

The Step 6 proof was monotonic against the delegated Step 5 baseline, not
fully green:
`c_testsuite_aarch64_backend_src_00164_c` and
`c_testsuite_aarch64_backend_src_00204_c` still fail, while the two focused
probes plus `00176`/`00181` pass.

The remaining stale-home behavior is not fixed by moving ALU select-chain
publication into shared prepared authority. Step 7 should not add ALU-local
fallbacks for this path.

The `00204` failure still starts in the stdarg output after otherwise-correct
argument and return-value sections; keep it as a secondary signal until the
smaller `00164` ALU chain is reduced.

Do not widen directly into `calls.cpp` under this plan. If the trace proves the
remaining stale-home decision is owned by call argument or call boundary
materialization, stop and route through
`ideas/open/52_aarch64_calls_prepared_authority_repair.md`.

Do not resume `dispatch_value_materialization.cpp` close-readiness work until
the ALU-owned stale-home baseline is reduced or reclassified.

The reviewer reported that root `test_after.log` is currently absent even
though this file previously referenced it. Regenerate the supervisor-selected
canonical `test_after.log` before any closure or acceptance decision; this
lifecycle repair does not create or update proof logs.

## Proof

Last delegated Step 6 proof command:

`cmake --build --preset default && ctest --test-dir build -j --output-on-failure -R '^(backend_codegen_route_aarch64_alu_unpublished_load_local_(after_call|call_boundary)|c_testsuite_aarch64_backend_src_00164_c|c_testsuite_aarch64_backend_src_00176_c|c_testsuite_aarch64_backend_src_00181_c|c_testsuite_aarch64_backend_src_00204_c)$'`

Result: build succeeded; CTest returned the delegated baseline split, 4/6
passed. Passing tests:
`backend_codegen_route_aarch64_alu_unpublished_load_local_after_call`,
`backend_codegen_route_aarch64_alu_unpublished_load_local_call_boundary`,
`c_testsuite_aarch64_backend_src_00176_c`, and
`c_testsuite_aarch64_backend_src_00181_c`. Failing tests:
`c_testsuite_aarch64_backend_src_00164_c` and
`c_testsuite_aarch64_backend_src_00204_c`.

Canonical log status for this lifecycle repair: root `test_before.log` exists;
root `test_after.log` is absent and was intentionally not regenerated by this
plan-owner packet.

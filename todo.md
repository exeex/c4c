Status: Active
Source Idea Path: ideas/open/226_phase_e3_prepared_diagnostic_oracle_replacement_readiness.md
Source Plan Path: plan.md
Current Step ID: 2
Current Step Title: Classify Candidate Rows

# Current Packet

## Just Finished

Completed Step 2 - Classify Candidate Rows for Phase E3. The table below
classifies each candidate row or tightly scoped row family from the Step 1
inventory against the E3 readiness categories. No durable E3 output document,
source idea, implementation, test, baseline, or log file was changed.

## Evidence Inventory

| Source | Proven semantic owner or boundary | Diagnostic/oracle row families E3 can support | Retained authority to preserve |
| --- | --- | --- | --- |
| `ideas/open/226_phase_e3_prepared_diagnostic_oracle_replacement_readiness.md` | Defines E3 as analysis-only readiness classification after E1/E2 proved selected branch-target and fused-compare identity plus private pass-context boundaries. | Starting candidate set: branch-target helper-oracle rows, fused-compare helper-oracle/branch-control/machine-printer rows, Route 3 memory/source rows, Route 4 block-entry publication rows, Route 5 edge/join rows, Route 6 x86 scalar source route-debug rows, Route 7 fused-compare/materialized-condition rows, and any E0/E1 row-scoped candidate with proven semantic owner plus fallback boundary. | No direct row replacement, baseline refresh, expected-string rewrite, unsupported downgrade, broad prepared printer/debug/helper-oracle replacement, target-policy migration, draft 155, E5, or aggregate prepared retirement. |
| `ideas/closed/217_phase_e0_prepared_bir_merge_readiness_and_ownership_boundary_audit.md` and `docs/bir_prealloc_fusion/phase_e0_prepared_bir_merge_readiness.md` | E0 accepts the target end state: BIR owns target-neutral semantics; prepared owns target/ABI/layout/register/stack/address/move/call/branch/emission policy; prepared APIs may shrink only toward private pass context; duplicate semantic helpers are the code-size-positive target. | E0 supports row-scoped diagnostic/oracle helper candidates only when route-native or BIR-native facts explain one positive row and every non-agreement path remains prepared-authoritative. It also maps route source/publication/join/call/comparison facts as possible semantic evidence for Route 3 through Route 7 rows. | Prepared fallback/oracle, diagnostic/string authority, expected strings, wrappers, public compatibility APIs, aggregate `PreparedBirModule`, aggregate `PreparedFunctionLookups`, target policy, cross-target compatibility, and draft 155/E5 remain retained. |
| `ideas/closed/218_phase_e1_semantic_duplicate_candidate_triage.md` and `docs/bir_prealloc_fusion/phase_e1_semantic_duplicate_candidate_triage.md` | E1 triage accepts only two semantic helper follow-ups: control-flow identity helpers and route source/publication/join/call/comparison identity helpers. Row-scoped diagnostic/oracle helpers are explicitly E3-owned unless they are only proof harnesses for a named E1 semantic reader. | Supports E3 classification for control-flow branch-target rows and Route 1 through Route 7 source/publication/join/call/comparison row families where a positive BIR/route identity fact is already proven or can be tied to a prior route closure. | Aggregate forwarding, liveness, intrinsic metadata, target policy, fallback/oracle, diagnostic/string, cross-target compatibility, baseline/string authority, Route 8, and draft 155/E5 stay deferred or no-action. |
| `ideas/closed/219_phase_e1_control_flow_identity_helper_contraction.md` | Proved the selected `find_prepared_control_flow_branch_target_labels(...)` path can use an agreement-gated BIR structured successor id path for branch-target label identity. | Supports branch-target helper-oracle or control-flow diagnostic rows tied to the selected branch-target labels helper. | Aggregate `PreparedControlFlow`, public prepared APIs, branch/output policy, edge-copy scheduling, move policy, wrapper/printer/debug/helper-oracle behavior, non-agreement fallback, and expected strings remain prepared/target-owned. |
| `ideas/closed/220_phase_e1_route_identity_helper_contraction.md` | Proved Route 7/prepared agreement for the selected AArch64 `find_prepared_fused_compare_operand_producer_facts(...)` comparison-provenance identity read consumed by conditional branch lowering. | Supports fused-compare operand producer helper-oracle rows and branch-control/machine-printer rows only where the selected Route 7 comparison provenance fact explains the positive row. | Broad Route 1 through Route 7 migration, route facades, aggregate route view replacement, prepared aggregate retirement, target-policy movement, wrapper/printer/debug/helper-oracle changes, fallback, and expected strings remain retained. |
| `ideas/closed/223_phase_e2_prepared_lookup_api_private_pass_context_readiness.md` and `docs/bir_prealloc_fusion/phase_e2_prepared_lookup_api_private_pass_context_readiness.md` | E2 classifies public API/private pass-context readiness and accepts only the two proven E1 helper families for follow-up. Lookup groups and mixed helper/printer/oracle rows stay retained, deferred, or no-action. | Supports E3 as the owner for mixed Route 1/2/5/6/7 source-producer helper or printer/oracle rows, plus broad diagnostic/oracle prerequisites around call/memory/publication/edge/fused-compare surfaces. | `call_plans`, `memory_accesses`, `edge_publications`, `edge_publication_source_producers`, `move_bundles`, `value_homes`, aggregate lookup construction, target policy, fallback/oracle, wrappers, printer/debug, expected strings, E4 wrapper work, Route 8, E5, and draft 155 remain retained or deferred. |
| `ideas/closed/224_phase_e2_control_flow_branch_target_helper_private_pass_context.md` | Moved the selected agreement-proven BIR structured successor label read behind `prepare::detail::BranchTargetIdentityPassContext` / `read_agreeing_bir_branch_target_labels(...)` for the safe AArch64 selected-identity consumer. | Supports E3 readiness analysis for branch-target helper-oracle rows where private BIR successor identity is positive evidence and the public prepared helper remains fallback/oracle authority. | Public prepared helper fallback remains for absent context, invalid IDs, mismatch/conflict, non-conditional BIR, and non-agreement. Branch spelling, edge-copy scheduling, wrapper output, printer/debug, helper-oracle strings/statuses, expected strings, and aggregate APIs remain retained. |
| `ideas/closed/225_phase_e2_fused_compare_operand_producer_helper_private_pass_context.md` | Extracted a private Route 7/prepared agreement reader for the selected fused-compare operand producer identity read in AArch64 comparison lowering. | Supports E3 readiness analysis for fused-compare helper-oracle, branch-control, and machine-printer row families where private Route 7 comparison provenance is positive evidence. | Public prepared fallback, absent-route/invalid/duplicate/conflict/mismatch/unfused/non-agreement behavior, branch targets, suffix mapping, fused legality, hazards, final assembler rows, printer/debug, wrappers, helper-oracle strings/statuses, expected strings, aggregate route views, and prepared aggregate ownership remain retained. |
| `ideas/closed/208_route3_memory_source_oracle_printer_row.md` | Route 3 memory/source agreement can explain one stored-value helper-oracle success row. | Supports one Route 3 memory/source helper-oracle status row or one prepared addressing printer row, including memory access/source identity, non-memory negatives, alias/address ambiguity, and mismatch cases. | Prepared diagnostics remain authoritative for fallback and non-agreement; address formation, relocation, materialization, addressing legality, final operands, target-addressing fallback, target wrappers, row text, and expected strings remain retained. |
| `ideas/closed/210_route4_block_entry_publication_printer_debug_row.md` | Route 4 attribution can reproduce one available-register `block_entry_publication` prepared value-location printer row under prepared agreement and fail-closed fallback. | Supports one block-entry publication printer/debug row with positive agreement, absent route evidence, wrong-reference, mismatch, duplicate-reference fallback, and wrapper no-change proof. | Prepared publication mechanics, block-order/output policy, wrapper behavior, CLI dump or broad printer/debug sections, fallback, row text, x86/riscv wrapper output, and expected strings remain retained. |
| `ideas/closed/212_route5_edge_join_oracle_printer_row.md` | Route 5 evidence is diagnostic metadata for one `PreparedCurrentBlockJoinParallelCopySourceFact` current-block join-source success row after agreement with prepared edge/join semantics. | Supports one Route 5 edge/join helper-oracle or prepared printer row covering join transfer, parallel copy, move bundle, edge publication, or source producer. | Prepared behavior remains authoritative for absent, invalid, duplicate/conflicting, memory-source, mismatch, unsupported, branch/parallel-copy, printer, wrapper, and expected-string paths. |
| `ideas/closed/214_route6_x86_scalar_source_route_debug_row.md` | Route 6 x86 scalar `i32` argument-source route-debug row can use route-native scalar source agreement while keeping `ConsumedPlans` compatibility. | Supports one x86 Route 6 scalar source agreement route-debug row, including absent, invalid, duplicate/conflict, mismatch, and compatibility cases. | `ConsumedPlans`, x86 wrapper behavior, ABI/call wrapper policy, prepared call printer/debug, direct-call/helper-oracle families, public fallback, expected strings, and broad x86 call wrapper migration remain retained. |
| `ideas/closed/216_route7_comparison_oracle_row.md` | Route 7-native evidence augmented one materialized-condition helper-oracle row in `verify_prepared_bir_comparison_condition_producer_equivalence`. | Supports one Route 7 fused-compare or materialized-condition helper-oracle row with positive, absent-route, invalid-reference, duplicate/conflict, mismatch, unfused fallback, prepared fallback, helper-oracle, branch-control, and machine-printer proof. | Prepared oracle assertion, branch policy, branch-control output, wrappers, final assembler behavior, expected strings, and broad printer/debug or route-index migration remain retained. |
| `docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md` | Establishes the diagnostic/oracle readiness rule: route-native replacement must name owned route facts, keep prepared expected output and oracle strength intact, cover positive/negative/ambiguous/mismatch/fallback cases, name retained compatibility adapters, and keep target/formatting/ABI/storage/emission policy outside route diagnostic ownership. | Supports candidate blockers and proof shapes for prepared printer, prepared CLI dump, x86 route-debug summary/trace, prepared lookup helper oracle, AArch64 lookup threading, target-wrapper compatibility, and Route 1 through Route 7 row families. | Production lowering success, full-suite greenness, section-name matching, printer label rewrites, baseline refreshes, helper renames, unsupported downgrades, output relabeling, target-policy migration, and broad prepared API deletion are not readiness evidence. |
| `docs/bir_prealloc_fusion/phase_d2_retained_surface_consumer_switch_analysis.md` | D2 maps retained surfaces and accepts only one-surface or one-row follow-up shapes for Route 3 through Route 7; selected adapters did not remove public prepared helper groups, aggregate lookup fields, target wrappers, printer/debug rows, oracle families, or target policy. | Supports Route 3 memory/source, Route 4 publication, Route 5 edge/join, Route 6 call-use source or wrapper/debug, and Route 7 comparison provenance diagnostic/oracle rows as one-row candidates only. Also supports no-action classifications for aggregates, AArch64 lookup threading as a contraction target, cross-target wrappers, baseline/string authority, and return-chain. | `PreparedFunctionLookups`, `PreparedBirModule`, AArch64 lookup threading, cross-target wrappers, baseline/string authority, return-chain, draft 155, broad aggregate retirement, public fallback removal, and expected-output rewrites remain blocked or retained. |
| Current accepted baseline state: `test_before.log`, `test_after.log`, and prior E0/E1/E2/D2 closure notes | Current root canonical logs are matching backend subset logs: both `test_before.log` and `test_after.log` report 180/180 backend tests passing with 0 failures. Prior accepted baseline evidence records recovered full-suite 3428/3428 and string-authority guard discipline; E0/E1/E2 closures used matching canonical logs with non-decreasing regression guard policy. | Baseline state can support E3 only as non-regression/string-authority context. It does not by itself classify any diagnostic/oracle row as ready. | Do not refresh baselines, rewrite expected strings, weaken helper-oracle names/statuses, downgrade supported paths, mask timeouts, or treat green production/backend proof as diagnostic/oracle replacement evidence. |
| Hook review state snapshot from Step 1: `.plan_review_state.json` and `todo.md` metadata | At Step 1 inventory time, hook-backed review state recorded `current_step_id: "1"`, `current_step_title: "Assemble Evidence Inventory"`, `counter: 0`, `review_limit: 5`, `baseline_review_pending: false`, `code_review_pending: false`, `test_baseline_counter: 3`, `test_baseline_limit: 8`, and no hook reminder line was present in `todo.md`. | Supports Step 1 context only: no plan-review or baseline-review reminder was active for the inventory packet. | Hook state is local automation state; do not add synthetic reminder lines. Keep `Current Step ID` and `Current Step Title` aligned in `todo.md` through the hook utility. |

## Candidate Classification

| Candidate row or row family | Classification category | Positive BIR/route fact | Retained prepared, oracle, target, wrapper, expected-string, or baseline authority | Blocker or deferral owner | Why this is not broader than E3 permits |
| --- | --- | --- | --- | --- | --- |
| `find_prepared_control_flow_branch_target_labels(...)` helper-oracle branch-target label row for the selected AArch64 conditional branch identity consumer | Ready to draft one implementation idea | E1 proved agreement-gated BIR structured successor label identity; E2 moved the selected read behind `BranchTargetIdentityPassContext` / `read_agreeing_bir_branch_target_labels(...)`. | Public prepared helper remains fallback/oracle for absent context, invalid ids, mismatch/conflict, non-conditional BIR, and non-agreement; branch spelling, edge-copy scheduling, wrapper output, printer/debug text, helper-oracle statuses, expected strings, and aggregate APIs remain prepared/target-owned. | None for analysis; later implementation idea must prove positive plus all fail-closed paths. | It names one helper-oracle row family tied to the selected private identity reader, not broad control-flow, public API, branch policy, printer, wrapper, or output migration. |
| Branch-target printer/debug, wrapper, branch-control, edge-copy, or emitted-output rows adjacent to the selected helper | Retained target/prepared policy or emitted-output authority | Same BIR successor identity fact may explain a diagnostic agreement, but it does not own output spelling or scheduling. | Branch emission, branch spelling, edge-copy scheduling, wrapper output, printer/debug rows, and expected strings stay prepared/target-authoritative. | Deferred outside E3 unless a later one-row diagnostic idea avoids output-policy movement. | E3 can classify these surfaces but cannot move target/emitted-output policy or claim broad branch-control replacement from one identity helper. |
| `find_prepared_fused_compare_operand_producer_facts(...)` helper-oracle positive operand-producer row for the selected AArch64 comparison lowering consumer | Ready to draft one implementation idea | E1 proved Route 7/prepared agreement for the selected comparison provenance identity read; E2 extracted the private Route 7 agreement reader for the selected fused-compare operand producer fact. | Public prepared fallback remains for absent route, invalid reference, duplicate/conflict, mismatch, unfused/non-agreement, and prepared-only cases; helper-oracle names/statuses, expected strings, printer/debug, wrappers, aggregate route views, and prepared aggregate ownership remain retained. | None for analysis; later implementation idea must keep fail-closed prepared authority. | It is limited to the selected helper-oracle operand-producer row family and does not replace broad Route 7, aggregate route views, branch policy, or prepared lookup groups. |
| Fused-compare branch-control and machine-printer rows whose positive text is explained only by the selected Route 7 comparison provenance fact | Proof harness only | Route 7 comparison provenance can corroborate the selected operand-producer identity used by AArch64 comparison lowering. | Branch target choice, suffix mapping, fused legality, hazard checks, final assembler rows, machine-printer formatting, wrapper output, expected strings, and prepared fallback remain retained. | Implementation deferral until a follow-up names one exact diagnostic row and proves no target-policy or output-format ownership change. | These rows are evidence/proof surfaces for the Route 7 identity helper; E3 cannot broaden them into machine-printer or branch-control replacement. |
| Route 3 memory/source stored-value helper-oracle success row | Ready to draft one implementation idea | Route 3 memory/source agreement can explain one stored-value helper-oracle success row. | Prepared diagnostics remain authoritative for non-memory negatives, alias/address ambiguity, mismatch, and fallback; address formation, relocation, materialization, addressing legality, final operands, target wrappers, row text, and expected strings remain retained. | None for analysis; later idea must include positive, negative, ambiguous, mismatch, and fallback cases. | The scope is one memory/source helper-oracle success row, not broad memory addressing, target-addressing printers, or address-emission policy. |
| Route 3 prepared addressing printer row | Retained target/prepared policy or emitted-output authority | Route 3 can provide memory access/source identity agreement, but the inventory does not prove route ownership of final addressing text. | Address formation, relocation, materialization, addressing legality, final operands, target-addressing fallback, target wrappers, printer text, and expected strings remain prepared/target-owned. | Deferred unless a future one-row idea proves a diagnostic-only augmentation that leaves address/output policy unchanged. | E3 can note the row as adjacent evidence, but replacing addressing printer authority would exceed one diagnostic/oracle row readiness. |
| Route 4 `block_entry_publication` available-register printer/debug row | Ready to draft one implementation idea | Route 4 attribution can reproduce one available-register block-entry publication prepared value-location printer row under prepared agreement and fail-closed fallback. | Prepared publication mechanics, block order/output policy, wrapper behavior, CLI dump and broad printer/debug sections, fallback, row text, x86/riscv wrapper output, and expected strings remain retained. | None for analysis; later idea must prove absent route, wrong-reference, mismatch, duplicate-reference fallback, and wrapper no-change paths. | It is one block-entry publication row with agreement gating, not a CLI dump, broad printer/debug, publication-mechanics, or wrapper migration. |
| Route 5 current-block join-source helper-oracle or prepared printer row | Ready to draft one implementation idea | Route 5 evidence is diagnostic metadata for one `PreparedCurrentBlockJoinParallelCopySourceFact` current-block join-source success row after agreement with prepared edge/join semantics. | Prepared behavior remains authoritative for absent, invalid, duplicate/conflicting, memory-source, mismatch, unsupported, branch/parallel-copy, printer, wrapper, and expected-string paths. | None for analysis; later idea must choose either the helper-oracle row or one prepared printer row and prove the fallback matrix. | The candidate is a single join/source row family after prepared agreement, not broad edge publication, move bundle, parallel-copy, or printer replacement. |
| Route 6 x86 scalar `i32` argument-source route-debug row | Ready to draft one implementation idea | Route 6 x86 scalar argument-source agreement can support one route-debug row while keeping `ConsumedPlans` compatibility. | `ConsumedPlans`, x86 wrapper behavior, ABI/call wrapper policy, prepared call printer/debug, direct-call/helper-oracle families, public fallback, expected strings, and broad x86 call wrapper migration remain retained. | None for analysis; later idea must include absent, invalid, duplicate/conflict, mismatch, and compatibility cases. | The row is limited to one x86 scalar source route-debug diagnostic; it does not migrate ABI/call wrappers, call lowering, or broad call printer/debug authority. |
| Route 7 materialized-condition helper-oracle row in `verify_prepared_bir_comparison_condition_producer_equivalence` | Ready to draft one implementation idea | Route 7-native comparison evidence already augmented one materialized-condition helper-oracle row. | Prepared oracle assertion, branch policy, branch-control output, wrappers, final assembler behavior, expected strings, helper-oracle statuses, and broad printer/debug or route-index migration remain retained. | None for analysis; later idea must cover positive, absent-route, invalid-reference, duplicate/conflict, mismatch, unfused, and prepared fallback cases. | It is one materialized-condition helper-oracle row family, not a Route 7-wide comparison, branch-control, machine-printer, or route-index migration. |
| Route 7 fused-compare branch-control or machine-printer rows beyond the selected helper-oracle/materialized-condition row | Proof harness only | Route 7 provenance can help verify comparison identity and materialized-condition agreement. | Branch-control output, suffix mapping, fused legality, hazards, final assembler rows, wrappers, printer/debug strings, expected strings, and target policy remain retained. | Deferred until a later idea names one diagnostic row and proves it is not output-policy movement. | E3 keeps these rows as evidence surfaces only; using them to replace branch-control or machine-printer behavior would be broader than a row-scoped readiness classification. |
| Mixed Route 1/2/5/6/7 source-producer helper, printer, or oracle rows named by E1/E2 but lacking a completed row-specific route closure | Retained prepared oracle/fallback authority | No row-specific positive BIR/route fact is proven in the Step 1 inventory beyond the named branch-target, fused-compare, and Route 3 through Route 7 closures. | Prepared fallback/oracle, diagnostic/string authority, public compatibility APIs, wrappers, expected strings, and aggregate lookups remain retained. | Blocked until a future source idea proves one route fact plus fallback boundary for a concrete row. | E3 rejects broad route-family claims and accepts only row-scoped candidates with existing evidence. |
| AArch64 lookup threading or private pass-context plumbing around retained lookup groups | Proof harness only | E2 proved private pass-context readiness for selected helper families, not ownership of lookup-threading output. | Lookup groups, aggregate lookup construction, public prepared helper compatibility, wrappers, diagnostics, expected strings, and target policy remain retained. | E2/private-pass-context follow-up owner for API contraction; E3 may cite it only as proof harness. | This is infrastructure evidence, not a diagnostic/oracle row replacement. |
| Cross-target wrapper output and wrapper compatibility rows | Cross-target wrapper prerequisite owned by E4 | No E3-positive route/BIR fact owns cross-target wrapper formatting or compatibility. | x86/riscv/AArch64 wrapper behavior, target ABI/layout/call policy, expected output, compatibility adapters, and emitted strings remain retained. | E4 owns wrapper prerequisite analysis and any wrapper migration. | E3 is row-readiness analysis; cross-target wrapper migration is explicitly outside its phase boundary. |
| Baseline files, expected strings, supported/unsupported markers, and helper-oracle names/status labels | Blocked by expected-string/baseline/unsupported authority | Green backend/full-suite logs and string-authority guardrails are non-regression context only, not row ownership evidence. | Baselines, expected strings, helper names/status labels, supported-path contracts, timeout behavior, and oracle strength remain authoritative. | Blocked unless a later implementation idea proves semantic behavior without baseline refresh, unsupported downgrade, or expectation rewrite. | E3 can preserve these as authority but cannot use baseline edits as readiness proof. |
| `PreparedFunctionLookups`, `PreparedBirModule`, aggregate prepared lookup construction, aggregate route views, route facades, Route 8, draft 155, E5, and broad prepared diagnostic/oracle retirement | No-action | No row-scoped positive fact in the Step 1 inventory transfers aggregate or retirement authority. | Aggregate prepared APIs, route facades, target policy, compatibility, fallback/oracle, diagnostics, and source-idea phase ownership remain retained. | Route 8, draft 155, E5, or separate lifecycle initiatives own those topics if reopened. | These are aggregate or future-phase surfaces, not one diagnostic/oracle row or tightly scoped row family for Phase E3. |

## Suggested Next

Proceed to Step 3 - Draft The E3 Readiness Document. Write
`docs/bir_prealloc_fusion/phase_e3_prepared_diagnostic_oracle_replacement_readiness.md`
from the Step 1 inventory and Step 2 classification table, without creating
follow-up ideas or changing implementation/test/baseline files in the same
packet.

## Watchouts

- E3 is still analysis-only; Step 2 did not write
  `docs/bir_prealloc_fusion/phase_e3_prepared_diagnostic_oracle_replacement_readiness.md`.
- Ready classifications are draft-readiness only, not implementation approval.
- Proof-harness rows must not be promoted into branch-control, machine-printer,
  wrapper, or emitted-output ownership without a separate one-row idea.
- Route 3 through Route 7 closures remain row-scoped evidence, not permission
  for broad printer/debug/helper-oracle replacement.
- Green backend logs are guardrail context only; they are not replacement
  proof for any diagnostic/oracle row.
- Do not mutate hook state, baselines, expected strings, source ideas, or
  follow-up ideas during Step 3 document drafting unless separately delegated.

## Proof

Analysis-only lifecycle packet; no build or test required by supervisor.
Delegated proof run and passed: `git diff --check -- todo.md`.
Final status check run: `git status --short` reported only `M todo.md`.

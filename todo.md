# Current Packet

Status: Active
Source Idea Path: ideas/open/127_typed_parser_record_identity_bridge.md
Source Plan Path: plan.md
Current Step ID: Step 4
Current Step Title: Propagate Typed Record Identity Through Template Records

## Just Finished

Plan-owner review accepted the Step 3 exhaustion finding and split the
remaining template work into explicit `plan.md` Step 4 before compatibility
mirror demotion.

Step 3 is complete for bounded non-template parser consumer conversions:

- Already converted TypeSpec-backed consumers with explicit compatibility
  fallback: `support.cpp::resolve_record_type_spec()` and layout helpers,
  `declarations.cpp` incomplete-object checks, `expressions.cpp` `offsetof`
  folding through `eval_const_int`, `types/declarator.cpp` dependent typename
  owner traversal, and `types/base.cpp` member typedef owner lookup.
- Const-eval bridge callers, not direct semantic record lookup authority:
  declaration constexpr binding, attribute/vector-size expressions, array
  bounds, bitfield widths, and static member initializer evaluation. These pass
  the compatibility map into `eval_const_int`, whose record layout path now
  resolves `TypeSpec::record_def` first.
- Compatibility/final-spelling mirrors and testing hooks:
  `register_record_definition_for_testing()`, record registration in
  `types/struct.cpp`, direct template instantiation map population, and tests
  that intentionally mutate stale rendered tags to prove typed identity wins.

## Suggested Next

Execute Step 4 with a first bounded template packet:

- Inventory `types/template.cpp` and adjacent template helpers for producers
  that create or forward struct/union `TypeSpec` without preserving
  `record_def`.
- Focus first on `ensure_template_struct_instantiated_from_args()`, injected
  template instantiation fallback, template base `TypeSpec` construction,
  deferred member typedef propagation, and template static-member base
  recursion.
- Implement the smallest propagation path that preserves `record_def` when the
  instantiated record `Node*` is known while keeping rendered instantiation
  keys and compatibility map writes unchanged.
- Add focused parser coverage where a stale rendered template record tag cannot
  override the instantiated record identity.

Suggested proof command:
`cmake --build --preset default && ctest --test-dir build -j --output-on-failure -R '^(frontend_parser_tests|frontend_hir_tests)$' > test_after.log 2>&1`

## Watchouts

Preserve `TypeSpec::tag` as final spelling, diagnostics, emitted text, and
compatibility payload. Do not treat `TextId` alone as semantic record identity.
Do not delete `struct_tag_def_map` while tag-only compatibility consumers
remain.

Template instantiation paths are now the active Step 4 contract, not Step 3
leftovers. Preserve rendered instantiation keys for compatibility and dedup;
the semantic improvement is carrying typed record identity alongside those
keys, not replacing them with another spelling authority.

## Proof

Lifecycle-only plan review/update. No build or tests were run.

Local validation: `git diff --check -- plan.md todo.md`

Proof log: no new `test_after.log` generated by this packet.

Status: Active
Source Idea Path: ideas/open/844_lir_global_extern_initializer_family_facts.md
Source Plan Path: plan.md
Current Step ID: 1
Current Step Title: Inventory extern fixed parameter type fact carriers

# Current Packet

## Just Finished

Plan-owner repaired the exhausted idea 844 runbook after the selected extern
aggregate return route concluded with no safe selected mirror deletion. Closure
is rejected because extern fixed parameter/signature-store type facts remain
in-scope durable source criteria.

## Suggested Next

Execute plan.md Step 1: inventory extern fixed parameter facts through
`record_extern_decl`, `ExternDeclInfo`, nominal signature-store entries,
verifier, printer, and call signature checks. Select one bounded parameter
target and record accepted/rejected authority inputs plus focused proof needs.

## Watchouts

Keep the route inside idea 844 global/extern type facts. Do not absorb
initializer payload semantics, global policy identity, collector-only migration,
Raw-BIR receiver work, non-type string routing, or varargs policy work. Do not
invent direct `LirExternDecl` parameter fields unless the Step 1 inventory
proves the signature store cannot serve as the selected producer-owned carrier.

## Proof

Lifecycle-only repair. Run `git diff --check` before handing back.

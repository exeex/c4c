# Current Packet

Status: Active
Source Idea Path: ideas/open/127_typed_parser_record_identity_bridge.md
Source Plan Path: plan.md
Current Step ID: Step 4
Current Step Title: Propagate Typed Record Identity Through Template Records

## Just Finished

Completed the final Step 4 inventory pass after template direct propagation
and substitution-copy propagation.

`types/template.cpp` template instantiation helpers are already in the expected
shape: `ensure_template_struct_instantiated_from_args()` carries `record_def`
for explicit full-specialization reuse and injected-parse reconstruction, while
the remaining rendered keys and `struct_tag_def_map` writes are compatibility,
dedup, or final-spelling mirrors. The static member lookup evaluator still
uses rendered map lookup for instantiated records and base traversal, but that
path is value lookup after instantiation rather than a `TypeSpec` propagation
producer.

`types/base.cpp` direct template emission now carries `record_def` for created
instantiation `TypeSpec`s, template-base instantiation/reconstruction,
instantiation `template_arg_types`, cloned member typedefs, fields, method
returns, and method parameters. One bounded template propagation gap remains:
direct substitution of a template type parameter into `inst->base_types[bi]`
updates `base`, `tag`, pointer, and cv state but does not yet copy
`pts.record_def`.

## Suggested Next

Run one more bounded Step 4 implementation packet in
`src/frontend/parser/impl/types/base.cpp` plus focused parser coverage:
preserve `TypeSpec::record_def` when direct template instantiation substitutes
a record-backed type parameter into `inst->base_types[bi]`, and prove a
template-instantiated derived/base path keeps the typed base owner even when
the rendered `tag` points at stale map state.

## Watchouts

Rendered template instantiation keys, direct-emission dedup sets,
`TypeSpec::tag`, and `struct_tag_def_map` mirror writes were intentionally
left unchanged and should stay compatibility/final-spelling/dedup state.
Direct template-emission reuse should not infer `record_def` from an existing
rendered map entry because that can be stale; producers should carry typed
identity from the selected/generated node or from already-typed `TypeSpec`
inputs.

The remaining base-substitution gap is not the template-base
`tpl_struct_origin` reconstruction path; that path already passes
`&inst->base_types[bi]` into `ensure_template_struct_instantiated_from_args()`
and falls back to a record-backed reconstruction only when needed.

## Proof

Inventory-only packet; no build or test command was delegated or run.

Local validation: `git diff --check -- todo.md`

Proof log: no new `test_after.log` generated by this packet.

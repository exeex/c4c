Status: Active
Source Idea Path: ideas/open/200_phase_a2_residual_prepared_surface_semantic_owner_audit.md
Source Plan Path: plan.md
Current Step ID: Step 4
Current Step Title: Produce Ownership Decisions And Follow-Up Payloads

# Current Packet

## Just Finished

Completed `plan.md` Step 3 by classifying cross-target prepared wrappers plus prepared printer, CLI dump, x86 route-debug, lookup-helper oracle, target-wrapper compatibility, and baseline/string-authority guardrail rows in `docs/bir_prealloc_fusion/phase_a2_residual_semantic_owner_audit.md`.

The Step 3 rows cite AArch64 lowering-context threading, x86 `ConsumedPlans` and `prepared::Query` wrappers, riscv prepared edge-publication emission, prepared printer/dump/debug/helper tests, and prior cross-target/diagnostic maps. They separate production-lowering/pass-context ownership from diagnostic/printer/oracle compatibility, name replacement equivalence proof, and apply idea 190/199 guardrails to fallback, target policy, baseline, and expected-string surfaces.

## Suggested Next

Execute `plan.md` Step 4: turn the row-level classifications into final ownership decisions, rejected retained-surface lists, blocked/unknown lists if any, and follow-up idea payloads.

## Watchouts

- This is analysis-only; do not edit implementation files for the audit.
- Step 3 classifies ownership only; it does not make API contraction, wrapper replacement, diagnostic replacement, or `PreparedBirModule` retirement claims.
- Step 4 should not promote a route candidate unless it names the semantic fact, excluded target/prepared policy, proof route, and reject signals.
- Keep production-lowering rows separate from diagnostic/printer/oracle compatibility rows when drafting final retained-surface decisions.
- Ideas 190 and 199 remain cross-cutting guardrails for Route 3 fallback/target policy, non-regressive baselines, and dump/printer/debug/helper string authority.

## Proof

Analysis-only/docs-only packet. No build required and no root proof logs were modified. Verified with `rg`, focused source/test reads, and `c4c-clang-tool`/`c4c-clang-tool-ccdb` structure queries over AArch64 traversal, x86 wrapper/debug, riscv prepared emission, prepared printer, CLI dump, route-debug, lookup-helper, and target-wrapper oracle surfaces. The audit table now has Step 3 cross-target coverage for AArch64, x86, and riscv; separates production-lowering ownership from diagnostic/printer/oracle compatibility; names equivalence proof needed for replacement; and applies idea 190/199 fallback, target-policy, baseline, and string-authority guardrails.

Status: Active
Source Idea Path: ideas/open/184_direct_call_signature_metadata_structured_boundary.md
Source Plan Path: plan.md
Current Step ID: Step 2
Current Step Title: Fence generated metadata-rich direct calls

# Current Packet

## Just Finished

Step 2 implementation completed: generated direct calls to known HIR functions
now populate `LirCallOp::callee_signature` with structured return, fixed
parameter, variadic, void-parameter-list, byval, and aggregate type-ref mirrors.

BIR direct-call lowering now consumes `callee_signature` before rendered
`callee_type_suffix` parsing, uses the structured `LirTypeRef` return mirror for
metadata-rich direct-call return lowering, and fails closed when a direct global
call carries semantic callee metadata without the structured callee signature.
The rendered-signature parser remains reachable for explicit raw/no-metadata
compatibility calls.

Focused tests added or updated:

- Frontend LIR call type-ref coverage now asserts direct-call structured callee
  signatures, direct variadic state, and stale rendered suffix tolerance when
  structured metadata is present.
- Backend notes coverage now proves structured direct calls ignore stale
  rendered suffixes, reject missing structured direct signatures, and keeps
  existing direct-call LinkNameId fixtures on the explicit structured path.

## Suggested Next

Execute Step 3 from `plan.md`: add any remaining focused direct-call tests the
supervisor wants for aggregate-sensitive success, stale or missing structured
metadata rejection, and explicit raw/no-metadata compatibility.

## Watchouts

- Direct calls with `direct_callee_link_name_id` and no `callee_signature` now
  fail closed; raw/no-metadata compatibility should keep `direct_callee_link_name_id`
  invalid if it intentionally relies on rendered call text.
- Generated byval direct calls should get structured byval facts from the
  function-derived `callee_signature` plus existing `arg_type_refs`; manual
  pointer-identity fixtures with byval-looking argument text were kept on
  explicit pointer signatures to preserve their existing contract.
- `lower_extern_decl` still prefers `return_type_str` before `return_type`; this
  is adjacent compatibility behavior, not necessarily the direct-call Step 2
  repair point.
- Backend freeze closure remains owned by idea 188.

## Proof

Supervisor-selected proof passed:
`cmake --build --preset default && ctest --test-dir build -j --output-on-failure -R '^(frontend_hir_tests|backend_)' > test_after.log 2>&1`

Proof log: `test_after.log`

Status: Active
Source Idea Path: ideas/open/195_frontend_to_bir_legacy_string_lookup_closure_gate.md
Source Plan Path: plan.md
Current Step ID: Step 2
Current Step Title: Produce the Closure Ledger

# Current Packet

## Just Finished

Step 2 - Produce the Closure Ledger: classified retained frontend-to-BIR string surfaces and recorded owner/removal conditions for explicit compatibility paths.

Closure ledger:

| Surface | Classification | Authority / Compatibility Boundary | Owner | Removal Condition | Step 3 Status |
| --- | --- | --- | --- | --- | --- |
| Parser `TextId`, `ParserSymbolId`, `ParserQualifiedNameRef`, namespace display/canonical names | structured-authority mirror plus display/output text | Parser identity uses `TextId` and parser symbol ids; spelled strings are parse-time projection/display carriers. | Parser name-binding and AST projection. | No removal for display spelling; semantic users should continue to prefer `TextId`/symbol/qualified-key fields. | Closed for Step 2. |
| Parser record resolution, `resolve_record_type_spec_with_parser_tag_map_compatibility` | explicit no-metadata compatibility | Ordinary `resolve_record_type_spec` uses direct `record_def`; rendered tag map requires the compatibility API. | Parser-local record probes and declaration checks. | Delete when all parser-local callers carry `record_def` or structured record keys instead of rendered tag maps. | Closed as compatibility; Step 3 should only revisit if generated metadata-rich callers still enter this bridge. |
| Parser const-int/typedef helpers | explicit no-metadata compatibility | `eval_const_int` and typedef resolution prefer `TextId`; `eval_const_int_with_rendered_named_const_compatibility` is an opt-in rendered bridge. | Legacy/HIR template paths that still pass rendered NTTP or constant names. | Delete when forwarded constants/NTTPs carry `TextId` or structured binding metadata end to end. | Closed as compatibility. |
| Parser/LLVM type spelling helpers such as final aggregate spelling and `llvm_ty` output names | ABI/final spelling and display/output text | Rendered aggregate tags are final LLVM/type spelling, not record identity when owner metadata is present. | HIR-to-LIR type emission. | Not removable while textual LLVM/LIR output needs final type names. | Closed for Step 2. |
| Sema enum/static int lookup rendered maps | explicit no-metadata compatibility | `StaticEvalIntEnumLookupInput` checks structured/TextId enum maps first; rendered enum maps are retained only when no structured key is available. | Sema static evaluation and legacy enum handoff. | Delete when enum/const AST and HIR handoffs always pass structured enum/value keys. | Closed as compatibility. |
| Sema rendered type-name equality | explicit no-metadata compatibility | `same_rendered_type_name_compatibility` is a last path for carriers without owner/key/type metadata; structured metadata blocks rendered-name recovery. | Sema type utility compatibility. | Delete after all comparable `TypeSpec`/canonical type carriers have structured identity. | Closed as compatibility. |
| Sema consteval environment rendered type/NTTP maps | explicit no-metadata compatibility | Text/key/structured maps are authority; rendered `type_bindings` and `nttp_bindings` are mirrored for legacy call-env substitutions only. | Consteval call-env construction and legacy template substitution. | Delete once all template type and NTTP substitutions are keyed by `TextId`, `QualifiedNameKey`, `HirTemplate*Bindings`, or specialization keys. | Closed as compatibility. |
| Sema/HIR struct layout compatibility through rendered `struct_defs` | explicit no-metadata compatibility | Owner-index lookup is authority; rendered tag maps bridge legacy layout handoffs and no-owner TypeSpecs. | Sema consteval layout lookup and HIR aggregate layout compatibility. | Delete when all TypeSpecs reaching layout code carry `record_def` or complete `HirRecordOwnerKey`. | Closed as compatibility; generated/member owner paths remain Step 3 audit targets below. |
| HIR compile-time registry keys | structured-authority mirror | `CompileTimeRegistryKey`, `primary_def`, specialization keys, and pending consteval callee identity are structured authority. Rendered names remain mirrors for preservation/progress/display. | HIR compile-time engine. | No removal for display/progress spelling; rendered public lookup APIs remain removable once callers use declaration/key APIs. | Partially closed; see blocker rows. |
| HIR direct consteval lookup and pending consteval materialization | explicit incomplete-identity compatibility | Direct calls use declaration-aware `find_consteval_def(n->left, n->left->name)`; pending consteval carries `callee_identity` and falls back to rendered `fn_name` only when identity metadata is incomplete. | HIR consteval lowering and compile-time engine. | Delete rendered fallback when every pending consteval expression carries complete callee identity. | Closed by ideas 192 and 196 evidence. |
| HIR rendered template registry calls in call/deduction/retry paths | unclassified high-risk metadata-rich path | Remaining `ct_state_->find_template_def(n->left->name)`, `find_template_def(call_node->left->name)`, and compile-time retry rendered preservation lookups may still be legitimate compatibility, but Step 2 did not prove complete structured misses fail closed for generated template-call metadata. | HIR template call lowering, template deduction, and compile-time retry. | Convert to declaration/key lookup or prove rendered use is no-metadata/preservation-only with complete structured misses closed. | Blocker for Step 3 audit, not closed by this ledger. |
| HIR object/member/operator/builtin owner lookup | explicit no-owner/no-decl compatibility | Direct constructor owner route uses structured owner metadata; operator/builtin helpers fail closed on complete owner/decl-key misses before consulting rendered no-owner maps. | HIR object/member/operator/builtin lowering. | Delete rendered owner/member fallback after all member, builtin layout, and operator queries carry complete owner/member keys. | Closed as compatibility except generated-member payload row below. |
| HIR generated member payload extraction from rendered qualified names | unclassified high-risk metadata-rich path | `scalar_control.cpp` derives member names from rendered qualified spellings after owner-key work; this may be payload extraction rather than semantic owner recovery, but Step 2 did not prove stale rendered generated names cannot repair a complete structured member miss. | HIR scalar/member lowering. | Replace with structured member `TextId`/owner payload or prove extraction is gated to no-member-metadata compatibility and fails closed on complete structured misses. | Blocker for Step 3 audit. |
| HIR route-local rendered names in `FunctionCtx` (`rendered_compat_local_*`, `rendered_compat_param_*`, static-global mirrors) | route-local handle plus explicit compatibility | Local/param maps are function-local LIR/HIR storage handles; rendered compatibility sets gate legacy lookups while TextId-local/param keys are preferred. | HIR function lowering. | Delete rendered mirror sets once all local/param references carry TextId or direct local/param ids. | Closed as route-local compatibility. |
| HIR-to-LIR call metadata (`LirCallSignature`, `LirCallArg`, `arg_type_refs`, `direct_callee_link_name_id`) | structured-authority mirror | Generated calls carry structured args, callee signatures, argument type refs, and link-name ids; verifier checks retained rendered text as a mirror. | HIR-to-LIR call lowering and LIR verifier. | No removal for verifier/output mirrors; semantic users should keep consuming structured payloads. | Closed by idea 190 evidence. |
| LIR call rendered `callee_type_suffix` / `args_str` parsing | explicit no-metadata compatibility and mirror validation | `parse_typed_call` prefers `structured_args` and `callee_signature`; rendered suffix/args parsing remains for raw hand-built LIR and for cross-checking structured payload consistency. | BIR call lowering. | Delete raw parsing after all accepted LIR producers provide structured call args/signatures, or keep only verifier-mode mirror checks. | Closed as compatibility. |
| LIR/BIR direct callee spelling | ABI/final spelling plus structured authority mirror | `direct_callee_link_name_id` is authority for metadata-rich direct calls; fallback spelling is final/import ABI spelling for no-id inputs and output. | HIR-to-LIR call lowering and BIR call lowering. | Raw fallback can be deleted when direct call imports always carry `LinkNameId`; final ABI spelling remains needed for output. | Closed as compatibility. |
| LIR function `signature_text` | ABI/final spelling and explicit no-metadata compatibility | `signature_params`, `signature_param_type_refs`, and `LirSignatureParam::is_byval` are backend authority when present; `signature_text` is final LLVM header spelling and legacy hand-built LIR parser input. | HIR-to-LIR function emission, LIR printer/verifier, BIR ABI lowering. | Delete parsing fallback after hand-built/no-structured LIR fixtures are retired; retain final output spelling. | Closed by idea 191 evidence. |
| LIR/BIR byval aggregate type text | structured-authority mirror plus explicit no-metadata compatibility | `arg_type_refs`/signature param type refs carry struct ids; rendered byval text is accepted only when structured refs are absent or as consistency check. | BIR call ABI and aggregate parameter lowering. | Delete rendered byval authority once all byval call/signature producers carry `LirTypeRef`/structured param metadata. | Closed as compatibility. |
| BIR global raw maps (`GlobalTypes`, `TypeDeclMap`, function raw symbol maps) | explicit raw/no-id compatibility | `LinkNameId` is authority when present; raw spelling maps support textual LIR/import/no-id paths and must not repair a present-id miss. | BIR module, globals, memory/provenance lowering. | Delete raw lookup paths after all accepted global/function symbols carry `LinkNameId`; retain final spelling for ABI/output. | Closed by idea 194 evidence; Step 3 should spot-check no present-id miss falls through raw maps. |
| BIR memory/provenance route-local strings (`LocalSlotTypes`, local pointer slots, aggregate leaf slots, SSA names) | route-local handle | Strings name local slots, SSA values, offsets, and function-local pointer provenance buckets; they are not module-level semantic identity. | BIR memory/addressing/provenance/local-slot lowering. | Not a closure blocker; may be replaced by ids in a later cleanup, but backend restart does not require it. | Closed for Step 2. |
| BIR inline asm, diagnostics, inspectors, printers, and prepared output names | display/output text or diagnostic text | Rendered strings are emitted text, diagnostics, inspection labels, or final ABI/output spelling. | Printers, diagnostics, MIR/prealloc output. | Not removable while text output and diagnostics exist. | Closed for Step 2. |

Step 2 blocker records for Step 3:

- HIR rendered template registry calls remain unclosed until Step 3 proves the remaining `find_template_def(string)`/rendered retry paths are no-metadata or preservation-only, or converts/splits them.
- HIR generated member payload extraction from rendered qualified names remains unclosed until Step 3 proves complete structured owner/member misses fail closed before rendered payload extraction can recover identity.
- BIR raw global maps are classified as explicit raw/no-id compatibility, but Step 3 should spot-check representative present-`LinkNameId` misses across call/memory/provenance paths before restart closure.

## Suggested Next

Execute `plan.md` Step 3: audit the high-risk generated paths named in the ledger. Start with HIR rendered template registry calls and generated member payload extraction, then spot-check BIR present-id fail-closed behavior around raw global maps.

## Watchouts

- Do not treat the two HIR blocker rows as closed based on comments alone; Step 3 needs source-path evidence that complete structured metadata misses fail closed.
- Parser, sema, LIR call payload, signature/byval, and BIR raw global spelling paths are ledger-classified as compatibility with explicit owners/removal conditions, not implementation work for this gate.
- Backend restart work remains out of scope until Step 4 records validation and a restart-readiness decision.

## Proof

Audit/todo-only packet. No validation was run because no code changed, and no `test_after.log` was produced for this no-build proof.

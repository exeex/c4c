Status: Active
Source Idea Path: ideas/open/239_phase_e5_prepared_bir_module_demotion_or_retirement_gate.md
Source Plan Path: plan.md
Current Step ID: Step 3
Current Step Title: Draft the E5 Gate Artifact

# Current Packet

## Just Finished

Step 3 drafted the durable E5 gate artifact at
`docs/bir_prealloc_fusion/phase_e5_prepared_bir_module_demotion_or_retirement_gate.md`
from the Step 1 inventory and Step 2 field-group matrix. No implementation
files, `plan.md`, source ideas, draft 155, expectation files, or baseline files
were changed.

The E5 gate conclusion is conservative:

- draft 155 should remain blocked as a broad aggregate-retirement plan and
  should not be opened as-is;
- idea 238 is closed, but only as prerequisite-complete evidence for the x86
  Route 6 scalar `i32` route-debug / `ConsumedPlans` compatibility boundary;
- no whole `PreparedBirModule` field group is ready for deletion,
  privatization, or aggregate retirement;
- no whole `PreparedFunctionLookups` group is ready for deletion,
  privatization, or aggregate replacement;
- selected future work may only be one proven semantic reader, one
  public/private adapter, one diagnostic/oracle row, or one wrapper
  compatibility boundary at a time;
- target policy, private pass context, public fallback/oracle surfaces,
  diagnostic/string authority, cross-target wrappers, expected strings, and
  baselines remain retained until a later single-boundary proof says
  otherwise.

## Suggested Next

Execute Step 4 only as safe follow-up idea creation from the E5 gate. The
recommended Step 4 packet should not open draft 155 as-is and should not
create broad aggregate-retirement work; it should create only narrow
field-group, adapter, row, or wrapper-compatibility ideas that the E5 document
explicitly permits, or record that no safe follow-up idea is warranted.

## Watchouts

- Keep idea 238 narrow: closed, prerequisite-complete, but only for x86 Route 6
  scalar `i32` route-debug / `ConsumedPlans` compatibility. It is not broad
  x86 call-wrapper, riscv, route-wide wrapper, or aggregate retirement proof.
- Step 3 should not claim readiness for any field group that lacks
  cross-target, diagnostic/oracle, fallback, public-consumer, baseline, or
  expected-string proof.
- Keep target policy out of target-neutral BIR: ABI, frame, registers, stack,
  addressing, move scheduling, call/helper protocols, carrier/runtime-helper
  protocol, branch/output spelling, final assembler, emission order, and
  wrapper output remain target/prepared-owned unless a later source proves a
  narrower owner.
- Preserve helper-oracle names/status labels, expected strings,
  supported-path status, prepared printer/debug output, route-debug output,
  wrapper output, fallback behavior, and baselines. Expectation rewrites,
  helper renames, unsupported downgrades, baseline refreshes, facade-only
  moves, and named-case-only shortcuts remain reject signals.

## Proof

Docs/lifecycle-only Step 3 gate artifact packet. No implementation files
changed, so no build or CTest proof was required and no `test_after.log` was
generated for this packet. Local proof was limited to source/lifecycle and
durable-artifact inspection with `git status --short`, `plan.md`, `todo.md`,
the source idea, E0-E4 documents, and idea 238 closure notes.

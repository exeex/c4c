# Current Packet

Status: Active
Source Idea Path: ideas/open/732_bir_stage_document_convergence_umbrella.md
Source Plan Path: plan.md
Current Step ID: 7
Current Step Title: Plan phase F machine construction and emission

## Just Finished

- Completed plan Step 6: converged E1-E4. E4 consumes only the private
  post-stable-E3 D5-resolved candidate, materializes the bounded frame-action
  vocabulary before final projection/E1, then uses non-mutating E2/E3 checks
  and atomically publishes Allocated ownership plus a distinct borrowing
  MIR-readiness capability/view bound to the same revision and products.

## Suggested Next

- Execute plan Step 7: converge F1-F3 around apply-only machine construction,
  machine verification, and emission/serialization ownership boundaries.

## Watchouts

- F1 must apply exactly one declared mapping per admitted MIR-ready node and
  must not repair allocation, spilling, copy resolution, or frame state.
- Machine identity is distinct; BIR identity may survive only as explicit
  provenance rather than storage or ownership reuse.
- F2 verifies the exact machine product and F3 owns only the declared emission
  boundary; neither may backfill phase-E work.
- Markdown-only output; proposed code belongs only in fenced Markdown blocks.

## Proof

- Changed-path suffix audit: every changed path ends in `.md`.
- Structural checks confirm E4 contains the exact adapted 17-section spine and
  a closed NodeKind/tag matrix.
- Searches confirm bounded frame actions precede final projection/E1, E2/E3
  are non-mutating final validators, no allocation repair/retry is admitted,
  no transitional readiness alias exists, and both E4 outputs bind the same
  revision and products.
- `git diff --check` passes. No build/test run is applicable to this
  documentation-only packet; no `test_after.log` is created or modified.

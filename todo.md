# Current Packet

Status: Active
Source Idea Path: ideas/open/127_typed_parser_record_identity_bridge.md
Source Plan Path: plan.md
Current Step ID: Step 3
Current Step Title: Convert Parser Record Consumers To Typed Identity

## Just Finished

Refreshed Step 3 remaining-consumer inventory after the
`types/base.cpp::lookup_struct_member_typedef_recursive` conversion.

Remaining `struct_tag_def_map` uses now classify as:

- Already converted TypeSpec-backed consumers with explicit compatibility
  fallback: `support.cpp::resolve_record_type_spec()` and layout helpers,
  `declarations.cpp` incomplete-object checks, `expressions.cpp` `offsetof`
  folding through `eval_const_int`, `types/declarator.cpp` dependent typename
  owner traversal, and `types/base.cpp` member typedef owner lookup.
- Const-eval bridge callers, not direct semantic record lookup authority:
  declaration constexpr binding, attribute/vector-size expressions, array
  bounds, bitfield widths, and static member initializer evaluation. These pass
  the compatibility map into `eval_const_int`, whose record layout path now
  resolves `TypeSpec::record_def` first.
- Compatibility/final-spelling mirrors and testing hooks:
  `register_record_definition_for_testing()`, record registration in
  `types/struct.cpp`, direct template instantiation map population, and tests
  that intentionally mutate stale rendered tags to prove typed identity wins.
- Template propagation and template-only lookup paths:
  `ensure_template_struct_instantiated_from_args()`, injected template
  instantiation fallback, template base `TypeSpec` construction, deferred member
  typedef propagation, and template static-member base recursion.

## Suggested Next

Step 3 should go to plan-owner review. I do not see another bounded
non-template parser consumer conversion remaining in Step 3; the remaining
work is either Step 4 compatibility mirror cleanup/documentation or a broader
template propagation packet, not a small consumer conversion.

## Watchouts

Preserve `TypeSpec::tag` as final spelling, diagnostics, emitted text, and
compatibility payload. Do not treat `TextId` alone as semantic record identity.
Do not delete `struct_tag_def_map` while tag-only compatibility consumers
remain.

Template instantiation paths remain a larger propagation family. A future
template packet would need to carry `record_def` through instantiated
`TypeSpec` results and base types while preserving rendered instantiation keys
for compatibility and dedup; it should not be treated as a routine Step 3
non-template consumer conversion.

## Proof

Inventory-only packet. No build or tests were run, per delegation.

Local validation: `git diff --check -- todo.md`

Proof log: no new `test_after.log` generated by this packet.

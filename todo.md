Status: Active
Source Idea Path: ideas/open/152_phase_b_bir_annotation_schema_candidate_audit.md
Source Plan Path: plan.md
Current Step ID: 4
Current Step Title: Create Follow-Up Schema Ideas

# Current Packet

## Just Finished

Step 4 from `plan.md`: created follow-up schema idea files for every
Phase A-justified schema/prototype/blocker domain recorded in
`docs/bir_prealloc_fusion/phase_b_annotation_schema_candidates.md`.
The artifact now links route-level prototype ideas for producer identity,
select-chain/direct-global dependency, memory/access identity, publication
availability, edge/join-source identity, call-use source facts, comparison and
condition provenance, plus a cross-route lookup index idea. It explicitly
records that module-level lowering metadata and return-chain helper facts do
not have justified follow-up ideas under the Phase A evidence set.

## Suggested Next

Execute Step 5 from `plan.md`: finalize the Phase B artifact for closure by
adding migration constraints, checking that the required route coverage table,
schema map, non-schema classifications, target-facing list, follow-up ideas,
and traceability are present, and preparing the closure summary.

## Watchouts

- Step 4 created source ideas only for Phase A-proven relationships and Step 3
  blockers. It did not create implementation/schema code.
- New follow-up ideas preserve old prepared queries as bridge/oracle data until
  BIR answers prove equivalence, and reject whole mixed `Prepared*` shapes as
  schema inputs.
- Function-level placement remains limited to lookup/index structures for
  cheap prepared-query-shaped access; durable payloads remain on value,
  instruction, terminator, block, or edge annotations.

## Proof

Lifecycle/docs-only packet. No build or test proof was run because no code or
schema implementation changed, per the delegated proof contract. No
`test_after.log` was generated for this docs-only slice.

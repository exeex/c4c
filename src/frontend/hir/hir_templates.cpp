// Template-lowering split unit placeholder.
//
// The previous disabled draft had drifted into a broad shadow copy of active
// owners from ast_to_hir.cpp, hir_build.cpp, and hir_expr.cpp. Keep this file
// in the staged source layout, but require future extraction slices to move
// template-owned bodies over from the live owners intentionally instead of
// reviving stale draft copies.

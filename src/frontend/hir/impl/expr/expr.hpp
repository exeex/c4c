#pragma once

// Private expression lowering implementation index.
//
// Expression lowering translation units include this subdomain header instead
// of reaching directly into the full Lowerer engine index.

#include "../lowerer.hpp"

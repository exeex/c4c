#pragma once

// Private template and dependent lowering implementation index.
//
// Template-family lowering translation units include this subdomain header
// instead of reaching directly into the full Lowerer engine index.

#include "../lowerer.hpp"

# CFG Analysis

Status: scaffold. Derives successors exclusively from terminators and computes
predecessors, reachability, edge identity, and analysis-local dense numbering.
It never publishes persistent CFG authority back into BIR.

Legacy coverage: control-flow views, route edge snapshots, label lookups, and
prepared control-flow queries.

# Comparison and Select Analysis

Status: scaffold. Derives comparison producers, consumers, select chains, and
condition relationships from canonical values and instructions. It cannot use
rendered instruction text or persistent route records.

Legacy coverage: comparison views, `prealloc/comparison.*`, select dependency
views, select-chain lookups, and target consumers that currently reconstruct
the same relationship.

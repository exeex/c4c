# AArch64 Target Contract

Status: scaffold. Review source: existing `src/backend/mir/aarch64/` ABI,
module, codegen, assembler, linker, object, allocation, inline-asm, variadic,
i128/f128, atomic, and runtime-helper paths.

Completion requires an exhaustive operation/ABI/frame/encoding matrix, not only
the currently supported testcase set.

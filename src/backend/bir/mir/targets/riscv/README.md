# RISC-V Target Contract

Status: scaffold. Review source: existing `src/backend/mir/riscv/` codegen,
assembler, linker, prepared emit adapters, calls, frame, variadic, atomics,
vectors, compressed instructions, inline assembly, and object emission.

Completion requires an exhaustive operation/ABI/frame/encoding matrix, not only
the currently supported testcase set.

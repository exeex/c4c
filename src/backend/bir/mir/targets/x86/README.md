# x86 Target Contract

Status: scaffold. Review source: existing `src/backend/mir/x86/` ABI, lowering,
prepared adapters, codegen, peephole, assembler, linker, register constraints,
frame, variadic, inline assembly, and object emission.

Completion requires an exhaustive operation/ABI/frame/encoding matrix, not only
the currently supported testcase set.

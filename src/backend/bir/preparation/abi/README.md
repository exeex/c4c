# ABI Plan

Status: scaffold. Classifies parameters, returns, byval/sret/HFA-style values,
calling conventions, and target register/stack classes. It produces placement
requirements, not executable moves.

Legacy coverage: call-return ABI classification, target register profiles,
formal publications, value locations, special carriers, and aggregate ABI.

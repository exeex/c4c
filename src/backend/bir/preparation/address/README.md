# Address Plan

Status: scaffold. Chooses target address-materialization strategies from
canonical address semantics without changing those semantics. MIR instruction
selection realizes the selected strategy.

Legacy coverage: addressing, local/global/static object access, dynamic stack,
pointer carriers, decoded home storage, and target relocation requirements.

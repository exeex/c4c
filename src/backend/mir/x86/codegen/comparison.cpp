#include "lowering/comparison_lowering.hpp"

namespace c4c::backend::x86 {

// Dormant compatibility surface: canonical compare/branch/select ownership
// moved to lowering/comparison_lowering.cpp during step 2.3. Keep this file as
// an explicit non-owning marker instead of letting the legacy top-level bucket
// silently disappear.

}  // namespace c4c::backend::x86

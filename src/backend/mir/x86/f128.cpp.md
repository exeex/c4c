# `f128.cpp` extraction

## Purpose and current responsibility

This file currently has no executable `F128` lowering logic. Its entire body is a
single include of the x86 codegen umbrella header plus an empty namespace block.

```cpp
#include "x86_codegen.hpp"

namespace c4c::backend::x86 {}
```

Current responsibility is therefore structural, not behavioral:

- participates in the directory's translation-unit layout
- pulls in the shared x86 codegen surface
- contributes no file-local APIs, helpers, data, or registration hooks

## Important APIs and contract surfaces

This `.cpp` defines none.

Its only visible surface is the dependency on the umbrella header:

```cpp
#include "x86_codegen.hpp"
```

That header is where the real `F128` contract surface exists today, including
declarations such as:

```cpp
void emit_f128_cmp_impl(const Value& dest, IrCmpOp op, const Operand& lhs, const Operand& rhs);
void emit_f128_load_to_x87(const Operand& operand);
void emit_f128_to_int_cast(IrType to_ty);
```

The important extraction point is that `f128.cpp` is not the owner of those
contracts; it merely names the shared surface indirectly by inclusion.

## Dependency direction and hidden inputs

Dependency direction is one-way:

- `f128.cpp` depends on `x86_codegen.hpp`
- no code in this file provides a service back to the rest of the backend

Hidden inputs:

- the build graph may still expect a file at this path, even though it contains
  no logic
- the directory-level organization suggests a historical or intended `F128`
  ownership seam that is not actually implemented here
- real `F128` behavior is hidden behind declarations in `x86_codegen.hpp` and
  implementations spread across other translation units

## Responsibility buckets

- Placeholder translation unit: yes
- Public or semi-public contract definition: no
- Core `F128` lowering: no
- ABI or return-value handling: no
- Memory movement or constant materialization: no
- Prepared fast paths: no

## Notable fast paths, compatibility paths, and overfit pressure

- Core lowering: none in this file
- Optional fast path: none in this file
- Legacy compatibility: the file itself is a compatibility placeholder because
  it preserves the path/name without owning behavior
- Overfit pressure: high risk of misleading ownership. The filename suggests a
  dedicated `F128` implementation unit, but the implementation actually lives in
  unrelated buckets elsewhere. Rebuild work should avoid preserving this empty
  seam merely because the old tree had a file with this name.

## Rebuild ownership guidance

This file should not own new `F128` behavior just to justify its existence.

In a rebuild, the `F128` surface should be owned by the actual semantic buckets
that already exist or by a cleaner replacement seam with explicit contracts.
This path may remain only if the build/layout genuinely needs a placeholder;
otherwise it should not own lowering logic, state bookkeeping, ABI glue, or
memory-format special cases.

# LIR Memory, VA, Object, and Lifetime Authority

Status: Closed
Type: first-owner producer/schema handoff
Parent: `ideas/open/734_lir_to_new_bir_container_completeness.md`
Generated By: `ideas/open/866_lir_remaining_authority_owner_triage.md`
Related:
- `docs/lir_remaining_authority_owner_triage/current_evidence.md`
- `docs/lir_remaining_authority_owner_triage/classification.md`
- `docs/lir_remaining_authority_owner_triage/ordering_and_closure.md`

## Goal

Publish one exact typed LIR producer/schema authority route for a remaining
memory, variadic-argument, object, or lifetime fact that is not already covered
by accepted local-object, VLA, or selected memcpy receiver history.

## Why This Exists

Idea 866 classified memory/VA and object/lifetime authority as missing a current
exact successor. 734 must not receive another memory or pointer row until LIR
first publishes structured current-function object, pointer, lifetime, or
variadic-argument authority that can be verified without display-text recovery.

## In Scope

- Select one bounded memory/VA/object/lifetime family with a concrete current
  LIR fact gap.
- Publish native LIR fields or schema carriers for that fact, plus producer
  population at the exact owned seam.
- Add verifier checks proving current-function ownership, validity, type or
  object/lifetime suitability, and malformed rejection for the selected fact.
- Write a handoff document naming the exact typed field set and the one future
  734 receiver row it authorizes.

## Out Of Scope

- Raw-BIR receiver implementation under 734.
- Reopening accepted local-object, VLA stack-save/restore, or selected memcpy
  rows.
- Broad pointer, memory, stack, lifetime, or VA sweeps.
- Recovering pointer, object, lifetime, or variadic state from operand spelling,
  printer output, LLVM text, rendered names, or testcase identity.
- Aggregate/vector, CFG/PHI, body-parameter, inline-assembly template, global
  policy, or terminal 797 convergence work.

## Acceptance Criteria

- Fresh build and focused producer/verifier proof cover the selected native
  memory/VA/object/lifetime fact and malformed rejection boundary.
- The handoff document identifies the exact LIR fields, owner checks, accepted
  positive case, rejected malformed cases, and one future 734 receiver row.
- Existing accepted local-object, VLA, and selected memcpy receipt tests remain
  valid and are not weakened or repurposed.
- No Raw-BIR receiver work lands in this idea.

## Exact 734 Return Condition

Return to `ideas/open/734_lir_to_new_bir_container_completeness.md` only after
this idea accepts a handoff document and proof for one exact typed memory/VA,
object, or lifetime row. 734 must then receive only that handed-off row into a
typed Raw-BIR container with transactional malformed rejection. All other
memory/VA/object/lifetime rows remain separately scoped.

## Reviewer Reject Signals

- Reject any Raw-BIR receiver implementation or 734 repair inside this idea.
- Reject reopening accepted local-object, VLA, or selected memcpy rows.
- Reject broad memory/pointer/VA sweeps, generic residual coverage, or multiple
  unrelated facts in one handoff.
- Reject deriving authority from operand text, printer output, LLVM rendering,
  testcase names, or compatibility-only display mirrors.
- Reject expectation downgrades, unsupported-marker changes, allowlist
  filtering, helper renames, or classification-only changes claimed as producer
  capability progress.

## Closure Record

Status: Closed
Disposition: capability complete for this bounded producer-side handoff.

Closed after the active runbook selected exactly one memory/VA row:
direct-local `LirVaStartOp` destination `va_list` pointer authority. The
runbook confirmed the native LIR carrier and producer route through
`LirVaStartOp.ap_authority`,
`LirVaStartOp.requires_native_memory_va_authority`, and
`StmtEmitter::native_direct_local_va_pointer`, confirmed the verifier boundary
in `verify_native_memory_va_authority`, and accepted the handoff document:

- `docs/lir_memory_va_object_lifetime_authority/handoff_to_734.md`

Accepted focused proof:

```sh
{ cmake --build build && ctest --test-dir build -R '^backend_lir_selected_pointer_authority$' --output-on-failure; } > test_after.log 2>&1
```

Result: `test_after.log` records a successful build/no-op build and 1/1
passing `backend_lir_selected_pointer_authority`.

No Raw-BIR receiver/importer work landed in this idea. Return to
`ideas/open/734_lir_to_new_bir_container_completeness.md` for the single
authorized receiver packet named by the handoff document:
receive selected direct-local `LirVaStartOp` destination `va_list` authority.

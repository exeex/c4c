# LIR String Semantic Authority Completion And Successor-Routing Umbrella

Status: Open
Type: Umbrella semantic completion router and follow-up idea generator
Parent Evidence Classifier: `ideas/closed/812_lir_string_authority_remaining_routes_umbrella.md` (accepted evidence-only closure at `f374ab3f5`)
Handoff Directory: `docs/lir_string_semantic_authority_completion/`
Related Architecture Input:
- `ideas/draft/837_lir_nominal_type_family_architecture.md` (parked,
  non-executable, not an owner or dependency until separately approved and
  promoted)
Existing Completion/Execution Owners:
- `ideas/open/734_lir_to_new_bir_container_completeness.md`
- `ideas/open/795_lir_body_parameter_authority_handoff.md`
- `ideas/open/796_lir_instruction_terminator_residual_authority_handoff.md`
- `ideas/open/797_lir_to_new_bir_final_coverage_convergence.md`
- `ideas/open/829_lir_next_body_parameter_authority_handoff.md`
- `ideas/open/830_lir_direct_call_structured_argument_identity_prerequisite.md`
- `ideas/open/831_preexisting_baseline_failure_family_decomposition_blocker.md`
- `ideas/open/836_lir_remaining_aggregate_owner_rejection_decomposition_blocker.md`

## Goal

Consume 812's current evidence matrix, assign every genuinely unfinished
semantic string-authority route to an exact existing owner or one separately
scoped new successor, and publish a dependency-ordered execution queue. Exclude
intentional opaque/render text and completed bounded rows. Preserve one first
owner per successor and an explicit terminal relationship to 797.

813 owns routing and successor generation. It does not repeat 812's exhaustive
inventory and does not implement any generated successor.

## Why This Exists

An inventory alone does not ensure unfinished semantic authority receives an
executable owner. Conversely, generating successors directly from stale closure
notes creates duplicates and reopens accepted capability. 812 supplies the
field/callsite evidence; 813 converts only its unresolved rows into durable,
ordered lifecycle ownership.

Current evidence already removes several former candidate gaps from generation:
closed 754 completed representative ExtractValue, InsertValue, InsertElement,
ExtractElement, and ShuffleVector rows; closed 762 completed module declaration
and type-shadow precedence; closed 811/814/815 completed their bounded native
vector carrier/coherence prerequisites. Closed 832-835 completed bounded HIR
aggregate-owner lifetime and canonicalization relations, while active 836 owns
the demonstrated residual structured-key/matching-owner/no-owner decomposition.
813 must credit these exact outcomes without generalizing them.

## Current Evidence

813 may execute only from an accepted, current 812 handoff containing:

- the exact evidence revision and complete disposition matrix;
- stable routing keys for every `unowned semantic route` and
  `insufficient evidence` row;
- exact existing-owner scope and return chains;
- the intentional-text registry; and
- closure reconciliation distinguishing satisfied rows from bounded-only work.

At execution, refresh open/closed lifecycle state before generation. If work
closed after 812's baseline, reclassify it against its exact closure record. If
a new open source owns a row, reuse it. Do not trust path/status assumptions
from this promotion snapshot.

Draft 837 is approved architecture direction only as a parked design input. It
may inform successor boundaries for nominal type families, recursive type
shape closure, aggregate canonical ownership, direct HIR-to-LIR construction,
and deletion gates. While it remains under `ideas/draft/`, it is not executable,
cannot satisfy an 812 row, cannot reserve ownership, and cannot block or replace
a required successor. If separately promoted before 813 executes, compare its
exact open scope and reuse it only for rows it fully owns.

## In Scope

- Validate 812's unresolved handoff against current lifecycle state.
- Map rows to exact existing open owners and preserve their return order.
- Group only rows with the same first owning layer, semantic contract,
  prerequisites, and acceptance proof.
- Generate one separately scoped source idea under `ideas/open/` for each
  genuinely unfinished first-owner route with no existing owner.
- Generate a bounded evidence/research successor for an insufficient-evidence
  family when a first owner cannot yet be chosen; do not invent implementation
  scope.
- Publish the dependency-ordered successor queue, row-to-owner map, and final
  handoff to 797.

## Existing Owner And Closure Rules

- 734 owns only Raw-BIR receipt/import/verifier rows after an accepted typed LIR
  handoff; it does not own missing LIR producer/schema authority.
- 795 owns its bounded body-parameter contract and accepted selected handoff,
  not all parameter/ABI forms. Current next-parameter/direct-call work belongs
  to the 829 -> 830 -> 831 -> 836 return chain where its exact contracts apply.
- 796 owns bounded selected residual producer families under its source and
  resumption records; nonselected instruction/intrinsic/terminator families are
  not automatically completed or owned.
- 797 owns terminal matrix/dispatcher/proof convergence only after every
  producer and receiver row has a disposition; it is not a catch-all repair
  owner.
- Active 836 owns only the demonstrated residual aggregate-owner decomposition
  and must return through 831 Step 4, then 830 Step 3 and 829 Step 2. Do not
  duplicate, bypass, or fold generic aggregate architecture into it.
- Closed 754, 762, 811, 814, 815, and 832-835 satisfy only their explicit
  completion records. Completed rows are removed from generation; adjacent
  recursive type, initializer, parameter, expression, or owner families remain
  eligible only when 812 evidence proves them unfinished.

## Required Routing Families

Generate or reuse exact owners for every unresolved 812 key, including where
evidence supports these distinct families:

1. Generic type authority: `LirTypeRef` runtime-text/classification, mutable and
   implicit string escape hatches, and recursive nested type shapes. Consult
   draft 837 as architecture input without treating it as an owner.
2. HIR-to-LIR structured type construction versus `llvm_ty()`-derived semantic
   refs, split by first producer/model owner where needed.
3. Global initializer/reference identity and global policy enums outside closed
   762's declaration/type-shadow scope.
4. Raw/parsed call compatibility and semantic reference collection beyond
   closed 761, coordinated with current 829/830 call-argument ownership.
5. Expression-result/coercion/operand authority loss, split by coherent
   producer family rather than one universal emitter rewrite.
6. Residual body-parameter and ABI-expanded forms beyond bounded 795 and the
   current 829 chain.
7. Residual instruction/intrinsic/terminator/inline-assembly value bindings
   beyond bounded 796; templates and constraints remain intentional text.
8. Semantic name/string-pool/global identity only where 812 proves spelling is
   authority rather than display or opaque bytes.
9. Aggregate/vector recursive type and canonical owner gaps beyond closed
   754/811/814/815 and 832-835, excluding work already owned by 836.
10. Typed 734 receiver rows after producer handoffs, followed by terminal 797
    integration.

These are routing families, not permission to create one idea per bullet or
one mixed idea for a whole bullet. Exact 812 rows and first owners control the
split.

## Dependency And Priority Model

Order by durable dependency and first ownership:

1. Preserve any active blocker/return chain already in progress; 813 does not
   interrupt active 836 or reorder its 836 -> 831 -> 830 -> 829 return.
2. Reuse open owners before generating anything.
3. Resolve insufficient evidence with bounded diagnostic/research sources
   before implementation scope.
4. Establish shared native schema/canonical stores/type families before
   producer migrations that need them.
5. Publish producer authority before verifier/consumer contraction.
6. Complete an exact LIR handoff before a bounded 734 receiver row.
7. Run mirror/removal audits after structured authority exists.
8. Enter 797 only after every valid current-LIR row is structured, a checked
   mirror, intentional text, or owned by a completed dependency.

The queue must identify which successors may run independently and which must
serialize. Source-file proximity, testcase order, and idea number do not define
dependency.

## Generated Successor Contract

Every generated source idea must include:

- exact 812 row/routing keys and first owning layer;
- one coherent semantic contract and explicit non-goals;
- prerequisite and parent return point;
- producer/schema/verifier/consumer boundaries;
- positive and malformed/foreign/mismatch proof expectations;
- focused build/test proof and broader/full proof proportional to shared
  surface risk;
- compatibility adapter and deletion conditions where applicable;
- `## Reviewer Reject Signals` blocking text recovery, mixed ownership,
  weaker checks/tests, and testcase-shaped fixes.

Do not create a successor for intentional text, a completed row, or a row fully
owned by an existing open source.

## Required Handoff Documents

Create under `docs/lir_string_semantic_authority_completion/`:

- `input_validation.md`: accepted 812 revision and lifecycle refresh;
- `row_to_owner_map.md`: every unresolved key mapped to existing owner, new
  successor, or bounded evidence route;
- `existing_owner_dependencies.md`: exact open scopes and return chains;
- `successor_queue.md`: generated ideas and dependency order;
- `architecture_alignment.md`: how parked/open 837 influenced boundaries,
  without assigning it ownership while draft;
- `handoff_to_734_and_797.md`: producer-to-receiver and terminal proof order;
- `closure_trace.md`: generated paths, excluded rows, and remaining blockers.

## Out Of Scope

- Repeating 812's exhaustive scan except to validate changed evidence.
- Implementing any generated successor or editing LIR/BIR/frontend/backend code
  or tests inside 813.
- Activating, switching, repairing, or closing active/open plans as part of
  umbrella execution.
- Promoting, revising, or activating draft 837.
- Creating duplicate successors for existing owners or accepted closed rows.
- Treating all strings as debt or converting intrinsic text to IDs.
- Parsing rendered text, names, operands, diagnostics, printer output, or
  testcase identity as semantic authority.
- Weaker verifier/tests, expectation changes, unsupported markers, allowlists,
  filtering, or runtime behavior changes as routing progress.

## Acceptance Criteria

- 813 cites one accepted current 812 handoff and validates changed lifecycle
  evidence before routing.
- Every unresolved 812 key maps exactly once to an existing open owner, a new
  one-first-owner successor, or a bounded evidence route; no row is unowned.
- No completed or intentional-text row generates a successor.
- Existing 734/795/796/797/829-831/836 and newer owners are reused only for
  their exact scopes and return points.
- Closed 754/762/811/814/815 and 832-835 remove their exact completed rows from
  generation while their bounded exclusions remain visible.
- Draft 837 is recorded only as non-executable architecture input unless a
  separate lifecycle operation has promoted it before execution.
- Generated ideas are under `ideas/open/`, separately scoped by first owner,
  dependency ordered, and map back to exact 812 keys.
- The queue preserves producer-before-receiver and receiver-before-797 order,
  including active return chains.
- Classification or successor creation is not claimed as semantic capability;
  each generated source retains its own implementation and proof gate.
- 813 changes only handoff docs and generated source ideas, not implementation,
  tests, runtime behavior, active plan, `plan.md`, or `todo.md`.

## Closure Note Requirements

The closure note must name the accepted 812 evidence revision, lifecycle refresh
revision, every reused open owner and exact return point, every generated idea
and 812 keys, dependency order, rows excluded as completed or intentional text,
draft/open disposition of 837, and the final 734/797 handoff. Closure is
rejected while any unfinished semantic route lacks an exact executable owner
or evidence route.

## Reviewer Reject Signals

- Reject implementation, tests, active lifecycle changes, or draft-837
  promotion/revision inside 813.
- Reject duplicate successors for work owned by 734/795/796/797/829-831/836 or
  newer open ideas.
- Reject reopening closed 754/762/811/814/815 or 832-835 without direct evidence
  outside their bounded completion records.
- Reject treating parked draft 837 as an executable owner, dependency, or
  substitute for a generated successor.
- Reject one broad `remove LIR strings` successor, mixed producer/receiver/proof
  ownership, or a giant type/expression/module rewrite unsupported by 812 rows.
- Reject every-string-is-debt reasoning or successors for opaque/render text.
- Reject semantic recovery from rendered type text, names, operands, printer
  output, diagnostics, or testcase spelling.
- Reject weaker verifier/runtime checks, test reductions, expectation
  downgrades, unsupported markers, allowlists, filtering, or named-case fixes.
- Reject classification, docs, wrappers, helper renames, or successor creation
  claimed as implemented semantic capability.
- Reject closure while any 812 unresolved key lacks one owner, exact dependency
  order, first-owner scope, or terminal 734/797 disposition.

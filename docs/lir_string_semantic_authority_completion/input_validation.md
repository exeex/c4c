# 813 input validation

## Accepted 812 evidence

The accepted 812 evidence revision is
`d58b8d44c9b64d2005d2b3760a0592b1b47ebd03`, selected and closed through
`f374ab3f5`. Its handoff contains exactly the three stable routing keys:

1. `global.policy-identity-evidence`
2. `instruction.intrinsic-binding-evidence`
3. `cfg.phi-raw-bindings-evidence`

The 29-row disposition reconciliation remains complete: these are the three
`insufficient evidence` rows; there are no `unowned semantic route` rows. No
`existing open owner`, completed, structured-authoritative, checked-mirror, or
intentional opaque/render-text row enters this routing input.

## Lifecycle refresh

Lifecycle state was refreshed at `ce1984a661b79c40f21e5ec203e5fe4d8521585b`
(`plan: close 812 evidence inventory and activate 813 routing`). The current
open sources retain their recorded bounded scopes. The closed 812 record and
the current sources introduce no exact closure that supersedes a qualifying
key.

| Stable key | Current owner / closure comparison | Validation result |
| --- | --- | --- |
| `global.policy-identity-evidence` | Open 844 owns global/extern **type**-fact migration and explicitly excludes 812/813 non-type string routing and initializer-text semantic redesign. Closed 762 remains only the declaration/type-shadow completion credited by 812. Neither proves producer-to-BIR-receiver coverage for linkage, visibility, qualifier, and symbol-identity policy fields. | Still insufficient evidence; no exact current owner. |
| `instruction.intrinsic-binding-evidence` | Open 796 owns one selected residual instruction/terminator/inline-assembly value-or-type handoff at a time; its accepted work is bounded cast-result and pointer-subtraction authority, while nonselected families remain fail-closed. It excludes a combined residual sweep and opaque template/constraint parsing. No current source proves the complete `LirInlineAsmValueBinding` producer/lowerer audit or malformed-binding boundary. | Still insufficient evidence; no exact current owner. |
| `cfg.phi-raw-bindings-evidence` | Open 734 has accepted bounded typed CFG and PHI receiver work, including selected PHI incoming value/predecessor and successor-occurrence handoffs. Its source remains a Raw-BIR receiver route and records other unreceived instruction/terminator families separately; it does not establish the requested every-`LirPhi`/terminator producer-to-verifier-to-BIR seam map, including malformed predecessor/value pairs. Closed 751/786/787/788 are selected prerequisites, not a family-wide closure. | Still insufficient evidence; no exact current owner. |

## Preserved ownership order

This validation does not activate, interrupt, reinterpret, or credit the
active return chain: `836 -> 831 Step 4 -> 830 Step 3 -> 829 Step 2`. The
forward blocked-parent relation remains `829 -> 830 -> 831 -> 836`. Draft 837
remains parked architecture input, not an owner.

## Step 1 conclusion

All three accepted keys remain the complete valid 813 routing input and remain
insufficient-evidence routes. Step 2 must assign each key once to an exact
existing owner or a bounded evidence route; it must not infer implementation
scope from this validation.

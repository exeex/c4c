# 813 Architecture Alignment

## Draft 837

Draft 837 remains parked architecture input. It can inform future source
boundaries for nominal type families, recursive type shapes, aggregate
ownership, and deletion gates, but it is not an executable owner while it stays
under `ideas/draft/`. 813 Step 3 does not promote it, depend on it, or use it
to reserve ownership.

## Alignment With 812

Closed 812 provided the accepted input revision and classified all rows except
three stable keys. 813 preserves that boundary:

- no completed row from 812 is reopened;
- no intentional opaque/render-text row is converted into implementation work;
- no current open owner is duplicated;
- every remaining stable key receives one completed-evidence disposition.

## Alignment With Evidence Routes

Closed 848, 849, and 850 are evidence routes, not implementation closures.
They answer the three insufficient-evidence keys and preserve downstream owner
order:

- 848 separates global non-type policy evidence from closed global/extern type
  work and from 734 receiver ownership.
- 849 separates selected structured inline-assembly bindings from opaque
  templates/constraints and from future 796/846 family work.
- 850 separates modeled CFG/PHI native authority and accepted bounded 734
  receipts from any future raw CFG/PHI producer seam.

## Alignment With 847

Closed 847 contributes terminal deletion-route evidence to 797. It deleted or
fenced universal-model and semantic string escape-hatch surfaces in its scope,
but it does not claim final 797 convergence and does not absorb residual
non-type/string routes, switch selector routes, or receiver dependencies.

## Rejected Architecture Moves

813 Step 3 rejects these moves:

- treating 797 as a catch-all owner for missing producer, verifier, or receiver
  work;
- deriving authority from rendered text, names, diagnostics, printer output,
  or testcase spelling;
- creating a broad "remove LIR strings" successor;
- merging producer, receiver, verifier, and terminal proof ownership into one
  successor;
- rerouting 821/822 switch selector work through 847 or 850.

# Phase A2 Residual Prepared Surface Semantic-Owner Audit

Status: Step 4 ownership decisions and follow-up payloads drafted.

Source idea: `ideas/open/200_phase_a2_residual_prepared_surface_semantic_owner_audit.md`

## Scope

This artifact records the Phase A2 ownership audit for residual
prepared/prealloc surfaces not already owned by Routes 1-8. The audit is
analysis-only: it does not add route schemas, migrate consumers, contract
prepared APIs, open draft 155, or claim `PreparedBirModule` retirement
readiness.

Rows below are seeded from the source idea and prior Phase A-D artifacts. Steps
1-3 classified aggregate, lookup, cross-target wrapper, prepared
printer/debug/dump/helper, and oracle compatibility surfaces. Step 4 converts
those row-level classifications into ownership decisions and follow-up payloads
without changing schemas, consumers, prepared APIs, tests, or lifecycle state.

## Evidence Inventory

| Required input | Status | Step 1 impact |
| --- | --- | --- |
| `ideas/open/200_phase_a2_residual_prepared_surface_semantic_owner_audit.md` | read | Source intent, ownership categories, in-scope residual groups, output contract, and reject signals captured. |
| `docs/bir_prealloc_fusion/phase_a_normalization_candidates.md` | read | Phase A candidate/reject boundaries captured for producer, select-chain, publication, call, memory, comparison, value-home, storage, frame, helper, carrier, and target-policy surfaces. |
| `docs/bir_prealloc_fusion/phase_b_annotation_schema_candidates.md` | read | Prior annotation-schema candidate boundaries captured for route-owned semantic facts versus retained prepared/target policy. |
| `docs/bir_prealloc_fusion/phase_c_private_cache_contraction.md` | read | Private-cache and pass-context contraction constraints captured for later aggregate classification. |
| `docs/bir_prealloc_fusion/phase_d_mir_consumer_switch_plan.md` | read | Route 1-7 selected-consumer ladder, prepared fallback/oracle boundaries, x86/riscv wrapper constraints, non-migration surfaces, and return-chain blocker/import notes captured. |
| `docs/bir_prealloc_fusion/phase_d_followup_pre_phase_e_readiness.md` | read | Phase D follow-up status, selected-reader limits, target-policy boundaries, cross-target breadth limits, Route 3 hard boundary, and draft 155 blockers captured. |
| `docs/bir_prealloc_fusion/residual_route_view_consumer_migration_map.md` | read | Residual Route 1-7 production, printer/debug, target-wrapper, oracle, fallback, and future selected-migration surfaces captured. |
| `docs/bir_prealloc_fusion/prepared_function_lookups_ownership_readiness_map.md` | read | `PreparedFunctionLookups` lookup-group rows captured as required residual audit seeds. |
| `docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md` | read | Prepared printer, CLI dump, route-debug, lookup-helper, AArch64 threading, target-wrapper, and oracle retention prerequisites captured. |
| `docs/bir_prealloc_fusion/cross_target_route_view_reuse_map.md` | read | x86 and riscv wrapper boundaries, ready/blocked/unknown route-view reuse status, and retained target-local policies captured. |
| `docs/bir_prealloc_fusion/return_chain_import_and_naming.md` | read | Route 8 return-chain naming, target-neutral fact shape, target-local policy exclusions, and removed prepared helper status captured. |
| `ideas/closed/190_full_suite_baseline_99_percent_regression_attribution.md` | read | Route 3 regression lesson captured: semantic memory/source identity can be correct while prepared target-addressing fallback or target policy is still required. |
| `ideas/closed/199_full_suite_baseline_string_authority_timeout_attribution.md` | read | String-authority and baseline discipline captured: do not weaken guards, baselines, or expected strings to make route/readiness docs appear clean. |
| `ideas/draft/155_phase_e_prepared_bir_module_retirement_plan.md` | read | Draft retirement questions and required field-by-field map captured as future constraints only; draft 155 remains unopened. |

Missing required inputs: none.

## Residual Ownership Table

| Residual surface group | Current files and APIs to inspect | Seed evidence from inputs | Current consumers to verify | Provisional audit status | Classification owner | Follow-up payload need |
| --- | --- | --- | --- | --- | --- | --- |
| `PreparedBirModule` aggregate fields and wrapper state | `src/backend/prealloc/module.hpp` defines the aggregate with BIR module, target profile, names/control-flow, value locations, stack/frame/dynamic-stack, addressing, liveness, regalloc, call, publication, variadic, storage, i128/f128 carrier, atomic, intrinsic, inline-asm, runtime-helper, phase, and note fields; the same header exposes `BirPreAlloc::prepared()` and function helpers such as `find_prepared_call_plans(...)`, `find_prepared_atomic_operations(...)`, and `prepare_*_bir_module_with_options(...)`. | Source idea and draft 155 require a field-by-field map before retirement; Phase D readiness says no current map proves deletion or demotion. Source evidence confirms the aggregate mixes target-neutral BIR/name/control-flow state with target profile and many target/prealloc policy plans, not one semantic owner. | AArch64 traversal constructs per-function contexts from `PreparedBirModule` and lookup caches in `src/backend/mir/aarch64/codegen/traversal.cpp`; x86 `ConsumedPlans` stores module-derived prepared lookups in `src/backend/mir/x86/x86.hpp`; riscv `emit_prepared_module(...)` rebuilds function lookups and emits from edge publications in `src/backend/mir/riscv/codegen/emit.cpp`; prepared printer/dump/debug/oracle readers remain Step 3 evidence. | Classified by source inspection. Not a new BIR route candidate as a whole; not a deletion, demotion, facade-rename, or retirement-readiness proof. | Mixed aggregate: existing route facts own only their narrow semantic records; target/prepared policy remains with prepared/prealloc owners; pass context/cache state remains transient; diagnostics/oracles remain compatibility surfaces. | Follow-up payload should remain field/group-specific. Any future `PreparedBirModule` retirement idea needs a field-by-field owner map plus fallback/oracle strategy, not whole-aggregate BIR cloning. |
| `PreparedFunctionLookups` aggregate compatibility surface | `src/backend/prealloc/prepared_lookups.hpp` defines seven fields: `call_plans`, `address_materializations`, `memory_accesses`, `move_bundles`, `value_homes`, `edge_publications`, and `edge_publication_source_producers`; `make_prepared_function_lookups(...)` is declared there and defined in `src/backend/prealloc/prepared_lookups.cpp` by rebuilding those typed caches from `PreparedBirModule`, control-flow, value locations, addressing, and source-producer/publication helpers. | Idea 196 map says aggregate-wide replacement, deletion, privatization, or BIR-owned cloning is not ready. Phase C classifies this as aggregate per-function lookup wiring and a construction/cache bundle, not a semantic fact owner. | AArch64 traversal stores a stack-local aggregate plus separate call/address/move/value-home lookup pointers in `FunctionLoweringContext`; x86 `ConsumedPlans` owns `std::optional<PreparedFunctionLookups>` and projects `shared_call_plan_lookups()`; riscv rebuilds lookups locally for prepared emission. | Classified by source plus prior map. The aggregate is retained as a compatibility/cache surface until individual fields have route-owned or target-local owners and consumers no longer need the public facade. | Transient pass context or private construction cache as the primary owner category, with mixed field-level owners below. | Field/group-specific follow-up only. Reject aggregate-wide BIR clone, rename-only facade, deletion, or privatization without reducing one real consumer boundary and preserving prepared diagnostics/oracles. |
| `call_plans` lookup group | `PreparedCallPlanLookups`, indexed call plan helpers, argument/result plan helpers, prior preserved-value lookup helpers, outgoing stack area helpers; `make_prepared_function_lookups(...)` fills this field from `find_prepared_call_plans(prepared, function.function_name)` and `make_prepared_call_plan_lookups(...)`. | Idea 196 marks whole call plans as target/prepared policy with one Route 6 semantic subfact. Phase C says Route 6 covers semantic call-use source records only, while ABI placement, clobbers, outgoing stack size, byval lanes, variadic FPR count, helper/carrier protocols, aggregate transport, and publication routing keep call plans public and out of BIR. | AArch64 calls and call-boundary helpers; x86 `ConsumedPlans::shared_call_plan_lookups()` and direct call wrappers; prepared call tests and lookup-helper oracles. | Classified mixed/retained. | Target/prepared policy retained outside BIR, with a narrow existing Route 6 extension/adapter candidate for call-use source identity only. | Future payload may target one call instruction and one argument/result role, proving route/prepared source agreement, absent/mismatched/ambiguous fallback, and unchanged ABI/policy. No whole-plan replacement or contraction readiness. |
| `address_materializations` lookup group | `PreparedAddressMaterializationLookups`, frame-address offset/address materialization helpers; `make_prepared_function_lookups(...)` fills this field via `make_prepared_address_materialization_lookups(prepared, function.function_name)`. | Idea 196 retains target-addressing policy; no group-level route replacement ready. Phase C says address/materialization maps are caches while `PreparedAddress` and `PreparedMemoryAccess` carry target/layout policy. | AArch64 globals, memory, calls, address materialization, frame-address helpers, prepared-address tests, lookup-helper coverage. | Classified retained. | Target/prepared policy retained outside BIR. No exact route-native subfact proven for this group in Step 2. | Future audit can inspect one reader that might ask only a semantic address/source question; otherwise retain target-address policy and prepared fallback. No contraction readiness. |
| `memory_accesses` lookup group | `PreparedMemoryAccessLookups`, `find_prepared_memory_access(...)`, indexed result-name/result-id helpers, same-block global-load and load-local source helpers; `make_prepared_function_lookups(...)` builds it from prepared addressing and value-home lookups. | Idea 196 and idea 190 mark Route 3 semantic memory/source identity as mixed with retained target-addressing policy. Phase C says Route 3 covers semantic memory/access identity, same-block global load access, and load-local stored-value source, while target/layout fields stay outside BIR. | AArch64 memory/calls/dispatch/store-source paths; x86 local-slot and compare-load candidates; memory/frame/stack tests and prepared lookup-helper oracles. | Classified mixed. | Existing Route 3 extension candidate for selected memory access id/source identity; retained prepared/target policy for address formation, frame/TLS/global relocation, volatile/address-space behavior, materialization, and final operands. | Future payload should target one Route 3 memory/source consumer with mismatch/fallback proof and explicit target-policy exclusion. No group deletion or target-wrapper reuse without AArch64 proof. |
| `move_bundles` lookup group | `PreparedMoveBundleLookups`, `make_prepared_move_bundle_lookups(...)`, before-call, after-call, before-return, edge-copy, and move-bundle lookup helpers; construction also publishes after-call result lane bindings from BIR calls plus value-home and move-bundle state. | Idea 196 marks move bundles as target/prepared policy and transient pass context. Phase C says reverse indexes are caches, but move records themselves are target/codegen allocation state. | AArch64 edge-copy, call, return, dispatch, publication, prologue, and value-materialization paths; x86 move/return/instruction wrappers; move-bundle tests and helper oracles. | Classified retained. | Target/prepared policy plus transient pass context retained outside BIR. Adjacent Route 5 or Route 6 source facts do not own move ordering, storage, ABI move phases, cycle temporaries, or execution sites. | Future payload only if one reader proves it consumes pure source identity; otherwise keep move scheduling/storage policy prepared-owned. No contraction readiness. |
| `value_homes` lookup group | `PreparedValueHomeLookups`, `make_prepared_value_home_lookups(...)`, `find_indexed_prepared_value_home(...)`, decoded-home/formal-publication inputs, and value-id reverse maps; `make_prepared_function_lookups(...)` builds this before memory, edge-publication, and move-derived lookups. | Idea 196 retains storage authority, register choice, stack slot/offset, home rendering, and rematerialization spelling outside routes. Phase C says Routes 1/4/5 can absorb only semantic producer/publication identity, not homes or storage. | AArch64 operands, prologue, dispatch, publication, memory, calls, select/materialization readers; x86 decoded-home/formal-publication/ABI helpers; riscv operand rendering; value-home oracles. | Classified retained. | Target/prepared policy retained outside BIR. No route-native contraction boundary is proven; x86 decoded-home use with null value-home lookups is not deletion evidence per prior map. | Possible future semantic value-id/source-id adapter only if storage/home policy remains prepared-owned and fallback/oracle coverage is preserved. No field contraction readiness. |
| `edge_publications` lookup group | `PreparedEdgePublicationLookups`, indexed/unique edge-publication helpers, block-entry/current-block publication helpers; `make_prepared_function_lookups(...)` builds it from prepared module, function, value locations, and value-home lookups. | Idea 196 marks mixed Route 4 publication identity and Route 5 edge/join-source identity with retained construction/emission policy. Phase C says publication maps are caches but publication facts still encode target-facing move/publication policy and stack-source handling. | AArch64 dispatch edge copies, memory, calls, and publication lowering; x86 edge-publication wrapper paths; riscv prepared edge-publication setup/emission; prepared publication tests and helper oracles. | Classified mixed. | Existing Route 4 publication identity and Route 5 edge/join-source identity are route-native subfacts; publication record construction, block-order emission, move/home/storage policy, stack-source extension, and final edge-copy records remain target/prepared-owned. | Future payload can target one edge-publication consumer or wrapper boundary with predecessor/successor, destination/source, duplicate/ambiguous/conflict, mismatch, and fallback proof. Not deletion-ready. |
| `edge_publication_source_producers` lookup group | `PreparedEdgePublicationSourceProducerLookups`, source-producer helpers, same-block producer/integer-constant/select-chain/current-block publication consumption helpers, and store-source/publication-source inputs; `make_prepared_function_lookups(...)` fills this field through `make_prepared_edge_publication_source_producer_lookups(prepared, function)`. | Idea 196 marks mixed Route 1/2/5/6/7 semantic subfacts plus retained production fallback and printer/debug oracle. Phase C says this is a construction cache, while same-block/select-chain query helpers remain public oracle/compatibility facades. | AArch64 dispatch producers, dispatch/publication/memory/calls, comparison, FP/value materialization; prepared printer `select_chains` output; source-producer helper tests; x86/riscv wrapper-adjacent paths and route-debug/helper oracles. | Classified mixed. | Route-native subfacts are Route 1 producer/constant identity, Route 2 select-chain/direct-global identity, Route 5 join-source identity, Route 6 call-use source identity, and Route 7 comparison provenance. Retained prepared policy covers production fallback, materialization sequence choice, context-coupled direct-global policy, storage/home/move policy, call/publication routing policy, final records, and printer/debug oracle compatibility. | Future payload should choose one route family and one consumer or diagnostic row, with route/prepared agreement, absent/mismatched/ambiguous fallback, and policy exclusion. No whole-group replacement or diagnostic contraction readiness. |
| Route 1 producer/constant residuals | Same-block producer, integer-constant, scalar operand, value-publication helpers; diagnostic readers include `src/backend/prealloc/prepared_printer.cpp` and `src/backend/prealloc/prepared_printer/select_chains.cpp` through `append_select_chain_materializations(...)`. | Residual map keeps prepared helpers as public fallback/oracles for broad scalar materialization, ALU, memory/call operands, printer, and tests. Prepared printer evidence shows source-producer and select-chain rows are rendered from prepared lookup helpers, not from a route-native diagnostic view. | AArch64 ALU/materialization/publication paths; prepared printer and `backend_prepared_lookup_helper` oracle rows. | Classified for Step 3 as residual production plus diagnostic/oracle compatibility. | Existing Route 1 owns producer/constant identity only. Prepared fallback/printer/helper compatibility remains outside BIR until route-native diagnostics prove same visible rows for same-block producer, constant, no-producer, and non-constant cases without weakening expected strings. | Selected-consumer or diagnostic replacement payload must pair route/prepared agreement with prepared-printer/helper expected-output equivalence. |
| Route 2 select-chain/direct-global residuals | Select-chain materialization and direct-global dependency helpers; `append_select_chain_materializations(...)` rebuilds `PreparedEdgePublicationSourceProducerLookups` and prints select-chain/direct-global rows. | Residual map keeps prepared select-chain fallbacks for publication, call, memory, ALU, FP, producer, edge-copy, printer/test paths. | AArch64 calls, memory, ALU, FP, producer, edge-copy; prepared printer and route/helper oracles. | Classified for Step 3 as residual production plus diagnostic/oracle compatibility. | Existing Route 2 owns select-root/direct-global dependency identity only. Prepared materialization, publication/call/memory context, and printer/oracle rendering stay retained compatibility surfaces. | Replacement proof must show route-native diagnostic rows match prepared select-chain/direct-global output for direct-global present/absent, scalar eligibility, nested select, source-producer, and fallback cases. |
| Route 3 memory/source residuals | Prepared memory/access/global-load/same-block/load-local source helpers, x86 local-slot/compare-load candidates, and dump/oracle rows that expose memory source behavior. | Idea 190 makes prepared fallback and target-addressing preservation mandatory. | AArch64 memory/globals/frame-slot/fp/materialization/comparison; x86 module/local-slot wrappers; prealloc call/publication/variadic; prepared helper oracles. | Classified for Step 3 as residual production plus guarded fallback/oracle compatibility. | Existing Route 3 may own selected memory/source identity. Target address formation, frame/global/TLS relocation, materialization, and prepared fallback remain target/prepared policy. | Any Route 3 diagnostic/oracle replacement must preserve prepared fallback and target policy, prove absent/mismatch behavior, and respect idea 190 by rejecting baseline refreshes or expected-output changes that hide a target-addressing regression. |
| Route 4 publication residuals | Current/block-entry publication helpers and edge-publication lookup surfaces; x86 and riscv edge-publication wrappers still consume `PreparedEdgePublicationLookups`. | Phase D follow-up says only one selected AArch64 publication reader moved; x86 wrappers and route-oracle tests still retain prepared surfaces. | AArch64 dispatch/publication/call planning; prepared printer; x86 `consume_edge_publication_move_intent(...)`; riscv `consume_edge_publication_move_intent(...)`; oracle tests. | Classified for Step 3 as mixed production and target-wrapper compatibility. | Existing Route 4 owns publication identity only. Edge-copy emission, move/home/storage policy, wrapper formatting, and prepared edge-publication lookup authority remain outside BIR. | Future wrapper payload must prove predecessor/successor, destination/source, unique match, duplicate/ambiguous rejection, prepared fallback, and unchanged emitted move policy before any x86/riscv replacement. |
| Route 5 edge/join-source residuals | Edge publication, source-producer, current-block join-source, move-bundle helpers; x86 compare/join and riscv edge-publication tests retain prepared edge/move policy. | Residual map keeps parallel-copy scheduling, homes, move bundles, branch spelling, and final edge-copy records outside BIR. | AArch64 edge/publication/memory/call/producer paths; x86 joined-branch tests; riscv prepared edge-publication tests; prepared printer. | Classified for Step 3 as mixed production and target-wrapper compatibility. | Existing Route 5 owns join-source identity only. Parallel-copy scheduling, source/destination homes, move bundles, branch spelling, scratch policy, and final records remain target/prepared-owned. | Replacement proof must cover join-edge/source agreement, conflict rejection, prepared fallback, and adjacent branch/parallel-copy sanity; wrapper tests remain compatibility oracles until then. |
| Route 6 call-use source residuals | Call-plan, argument/result, publication-source routing, value-home, move-bundle helpers; x86 `ConsumedPlans` route gate in `src/backend/mir/x86/x86.hpp`. | Residual map and cross-target map accept only narrow Route 6 semantic source reuse, not whole call-plan or wrapper migration. Source evidence shows `find_consumed_scalar_i32_call_argument_source(...)` returns a Route 6 answer only when the prepared argument source id agrees; otherwise prepared call-plan fallback remains. | AArch64 call argument/result readers; x86 scalar helper and direct extern/same-module wrapper tests; prepared call printer/debug surfaces. | Classified for Step 3 as one narrow existing route-extension boundary plus retained wrapper/oracle compatibility. | Existing Route 6 owns selected call-use source identity. ABI placement, call wrapper kind, value homes, move bundles, variadic counts, helper/carrier protocol, and final call records remain target/prepared-owned. | One call-role adapter payload at a time. Proof must include route/prepared source-id agreement, absent Route 6 fail-closed, mismatched source-id fail-closed, preserved prepared call-plan fallback, and unchanged x86/AArch64 output. |
| Route 7 comparison/condition residuals | Fused-compare, materialized-condition, branch operand, scalar comparison helpers; x86 compare/join debug and handoff tests keep prepared/target branch policy visible. | Residual map keeps branch spelling, fused legality, hazards, emitted-register state, and final branch/compare ordering target-owned. | AArch64 comparison/branch/ALU paths; x86 joined-branch and route-debug tests; fused-compare and materialized-condition oracles. | Classified for Step 3 as residual production plus diagnostic/debug compatibility. | Existing Route 7 owns selected comparison provenance only. Branch spelling, condition-code choice, fused legality, hazards, emitted registers, and final instruction order remain target/prepared-owned. | Replacement proof must show provenance equality for fused, materialized, unfused fallback, absent-route, and invalid-reference cases while keeping branch policy outside BIR. |
| Route 8 return-chain import | `route8_*` BIR records/index/lookups; removed historical prepared return-chain helpers. | Return-chain import note says old public prepared helper names are absent after idea 180; Route 8 owns only target-neutral same-block return-chain identity. | AArch64 return-chain semantic consumers; target-owned return ABI, homes, registers, scratch, ALU records, emission order; `backend_prepared_lookup_helper` Route 8 oracle rows. | Classified for Step 3 as route-owned semantic import with retained target return policy and oracle compatibility. | Existing Route 8 owns target-neutral same-block return-chain identity. Return ABI, home/register/scratch policy, ALU record emission, and helper/oracle expected strings stay outside BIR. | Usually citation/import payload. Diagnostic replacement would need exact helper-oracle equivalence for terminal/next operand, unsupported, unnamed, broken-walk, cross-block, missing-instruction, and duplicate/conflict cases. |
| AArch64 prepared wrapper and lowering context | `src/backend/mir/aarch64/module/module.hpp` exposes `FunctionLoweringContext` with `PreparedBirModule`, target profile, control-flow, BIR function, value locations, storage, regalloc, frame/dynamic-stack/call plans, `PreparedFunctionLookups`, and projected lookup pointers; `src/backend/mir/aarch64/codegen/traversal.cpp` constructs those lookups in `lower_prepared_functions(...)`. | Phase D maps selected consumer progress only; many residual production, fallback, printer/debug, and oracle readers remain. Clang-tool symbol inventory confirms `make_function_lowering_context(...)` and `lower_prepared_functions(...)` as the main threading entry points. | Traversal, dispatch, calls, memory, value materialization, publication, comparison, ALU, prologue, atomics/f128. | Classified for Step 3 as production-lowering pass context, not diagnostic compatibility and not a route candidate as a whole. | Transient pass context/private construction cache plus retained target/prepared policy. Route facts should replace one projected fact consumer at a time; AArch64 context threading is not BIR semantic ownership. | Selected-consumer payloads only. Each must name the single consumed fact, keep prepared fallback/oracle behavior, and avoid claiming aggregate contraction from one reader. |
| x86 prepared wrappers and `ConsumedPlans` | `src/backend/mir/x86/x86.hpp` defines `ConsumedPlans`, `consume_plans(...)`, prepared call-plan selectors, the Route 6 scalar source gate, and debug wrappers; `src/backend/mir/x86/prepared/prepared.hpp` defines the broad `prepared::Query` facade and edge-publication wrapper. | Cross-target map says only x86 Route 6 scalar source sub-boundary is ready; whole plans, facade, ABI, frame, moves, wrappers remain blocked or target-local. Source evidence confirms `prepared::Query` reads addressing, locations, calls, regalloc, storage, BIR lookup, decoded homes, call-boundary moves, formal publication, and block-entry publications. | x86 direct extern/same-module calls, ABI helpers, edge-publication, compare/join, local-slot helpers, debug/handoff tests. | Classified for Step 3 as mixed wrapper compatibility with one narrow Route 6 production sub-read. | Target/prepared policy retained outside BIR, except selected Route 6 source identity when prepared and route agree. Broad `prepared::Query` facade is blocked from route-wide migration. | One wrapper-boundary payload at a time: Route 6 call source, edge-publication, local-slot memory source, compare/join provenance, or one `Query` method. Proof must include route/prepared agreement, mismatch/absent fallback, target-policy exclusion, and unchanged wrapper output. |
| riscv prepared wrapper surfaces | `src/backend/mir/riscv/codegen/emit.cpp` defines `emit_prepared_module(...)`, rebuilds `PreparedFunctionLookups`, scans `lookups.edge_publications.publications`, and renders move intent through `consume_edge_publication_move_intent(...)`. | Cross-target map says no ready riscv route-view reuse point; edge-publication semantic source adapter is only a future blocked candidate. Source evidence shows register naming, stack/immediate handling, extension policy, scratch registers, and fail-closed unsupported status are target-local. | riscv prepared lookup setup, edge-publication move intent, operand rendering, stack-source publication, register naming; `backend_riscv_prepared_edge_publication_test.cpp` keeps emitted string and fail-closed behavior. | Classified for Step 3 as target-wrapper compatibility and production emission, with no accepted route-view reuse. | Target/prepared policy retained outside BIR. Potential Route 4/5 semantic identity is blocked until route/prepared agreement and wrapper fallback are proven. | Possible future riscv edge-publication adapter payload only. Proof must cover predecessor/successor or block-label agreement, source/destination ids, unique/ambiguous/conflict behavior, preserved prepared fallback, unchanged RISC-V register/stack/scratch policy, and no string-authority weakening. |
| Wide-value carriers | I128/F128 carrier surfaces and related lowering helpers; prepared printer includes `append_i128_carriers(...)` and `append_f128_carriers(...)`. | Source idea and Phase D plan list wide carriers as residual non-migration surfaces except for underlying semantic source identities. | AArch64 f128/i128 paths, helper protocols, target records, prepared printer rows. | Classified for Step 3 as retained target/prepared policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR. Route source identities may feed inputs, but carrier protocol, ABI movement, helper records, and printed compatibility rows are not BIR facts. | Retained-policy cleanup payload only if it preserves prepared printer/oracle output and target helper protocol. |
| Runtime helper protocols | Runtime helper plan/helper surfaces; prepared printer includes runtime-helper sections. | Phase D plan excludes helper protocols, scratch resources, helper calls, and target records from BIR ownership. | AArch64 helper emission, call/helper protocol tests, prepared printer/debug rows. | Classified for Step 3 as retained target/prepared policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR. Helper ABI, scratch resources, call spelling, and helper records are not route schema candidates. | Replacement proof would need helper-call protocol equivalence and unchanged diagnostics, not just route source agreement. |
| Variadic entry helpers | Variadic entry and variadic FPR/count surfaces; prepared printer includes variadic entry plans. | Phase D plan excludes variadic entry/helpers and ABI counts from route migration. | Prealloc planning, AArch64/x86 call ABI paths, tests, prepared printer rows. | Classified for Step 3 as retained ABI/target policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR. Variadic register counts, save-area policy, and helper operands remain ABI surfaces. | Future payload should be retained-policy cleanup only unless a pure semantic source read is isolated. |
| Intrinsic helpers | Intrinsic lowering helper surfaces and prepared printer intrinsic carrier rows. | Source idea lists intrinsic helpers as residual prepared/prealloc surfaces needing owner audit. | Target intrinsic lowering and helper/oracle tests. | Classified for Step 3 as retained target/prepared policy pending method-level semantic isolation. | Target/prepared policy retained outside BIR for current audit. No pure route-native subfact was proven by wrapper/diagnostic inspection. | Method-level audit payload only if a future reader proves one target-neutral intrinsic semantic fact and keeps target lowering policy out. |
| Inline-asm helpers | Inline asm helper surfaces and prepared printer inline-asm carrier rows. | Source idea lists inline-asm helpers as residual prepared/prealloc surfaces needing owner audit. | Target inline-asm lowering and diagnostics. | Classified for Step 3 as retained target/prepared policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR. Constraint handling, clobbers, formatting, and diagnostics are target/helper policy, not a BIR route. | Future payload should preserve string-authority expectations for inline-asm diagnostics. |
| Atomic helpers | Atomic helper surfaces and prepared printer atomic operation rows. | Source idea lists atomic helpers as residual prepared/prealloc surfaces needing owner audit. | AArch64 atomics and target-specific lowering/tests; prepared printer rows. | Classified for Step 3 as retained target/prepared policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR unless a future audit isolates a pure semantic memory/source identity already owned by Route 3. | Future payload must preserve target atomic width/order/fallback policy and expected diagnostic strings. |
| Dynamic-stack helpers | Dynamic-stack plan/helpers; AArch64 context stores `dynamic_stack_plan`; x86 `ConsumedPlans` stores `dynamic_stack`; prepared printer includes dynamic-stack plan output. | Phase D and cross-target maps retain dynamic stack, frame layout, and target wrapper policy outside routes. | x86 `ConsumedPlans`, AArch64 prologue/frame paths, prepared printer/debug tests. | Classified for Step 3 as transient pass context plus target/prepared policy. | Target/prepared policy retained outside BIR. Dynamic alloca layout, stack adjustment, and anchor policy are not route facts. | Retained-policy cleanup payload only; no wrapper route-view replacement without target-policy proof. |
| Frame helpers | Frame slots, frame layout, frame-address offset helpers; AArch64 context stores frame plan and address-materialization lookups; x86 `ConsumedPlans` stores frame; prepared printer includes frame plans. | Prior maps retain stack/frame offsets and layout as target/prealloc policy. | AArch64 frame/prologue/memory; x86 frame/local-slot helpers; frame/stack tests; prepared dump rows. | Classified for Step 3 as retained target/prepared policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR. Frame slot ids, offsets, object layout, coalescing, and operand rendering remain prepared/target-owned. | Route 3 memory-source payloads must explicitly exclude frame layout and preserve prepared fallback per idea 190. |
| Special target-entry surfaces | Special carrier/target-entry protocols; prepared printer exposes special carrier sections. | Source idea and Phase D plan list special target entries as residual non-migration surfaces. | Target entry lowering and helper tests. | Classified for Step 3 as retained target/prepared policy plus diagnostic compatibility. | Target/prepared policy retained outside BIR. No route-native semantic fact proven by Step 3 inspection. | Future payload requires method-level source evidence before changing owner. |
| Prepared printer | `src/backend/prealloc/prepared_printer.cpp` defines `prepare::print(const PreparedBirModule&)` and appends BIR, function summaries, control flow, value locations, stack/frame/dynamic-stack, calls, store-source publications, select chains, variadic, regalloc, storage, wide carriers, atomics, intrinsics, inline asm, runtime helpers, and addressing; tests include `backend_prepared_printer_test.cpp`. | Diagnostics plan keeps prepared printer as compatibility oracle until route-native diagnostics prove equivalent content. | Prepared printer expected output, route/prepared comparison tests, x86 route-debug tests that compare generic prepared dump to `prepare::print(prepared)`. | Classified for Step 3 as diagnostic/printer/oracle compatibility surface. | Diagnostic/printer/oracle compatibility surface retained outside production-lowering ownership. The printer can reveal route-native facts, but it also renders target policy and prepared phase products. | Route-native printer replacement must prove exact or intentionally versioned content equivalence across all printed sections it replaces, with expected-string/string-authority guards from idea 199 and no production-only proof shortcut. |
| Prepared CLI dump | Backend dump path for `PreparedBir` stage, `--dump-prepared-bir`, CMake snippet checks in `tests/backend/cmake/run_backend_dump_flag_case.cmake`, and x86 route-debug tests comparing backend prepared dump to `prepare::print(prepared)`. | Diagnostics plan keeps dump as user-facing compatibility stage, not route-native proof. | CLI dump tests and snippets; x86 route-debug generic prepared dump check. | Classified for Step 3 as diagnostic/printer/oracle compatibility surface. | Diagnostic/printer/oracle compatibility surface retained. Dump output is string authority, not BIR semantic ownership. | Route-native dump payload must name exact dump scope, expected snippets, forbidden snippets, focus behavior, and idea 199 non-regressive string-authority proof. |
| x86 route-debug summary/trace | `src/backend/mir/x86/debug/debug.cpp` renders `summarize_prepared_module_routes(...)` and `trace_prepared_module_routes(...)`; output labels owner as `x86/debug` and says structured lane diagnostics are deferred. | Diagnostics plan says compatibility-derived rows must remain labeled until route-native authority owns every assertion. | `backend_x86_route_debug_test.cpp` compares direct calls and backend MIR summary/trace dumps, focus filters, miss summaries, route owner strings, and prepared dump equality. | Classified for Step 3 as diagnostic/debug compatibility surface. | Diagnostic/printer/oracle compatibility surface retained under x86/debug. Public focus strings and rendered rows are debug filters, not semantic identity. | One summary/trace row-family payload at a time. Replacement proof must preserve focus filters, miss/rejection rows, prepared dump equality where applicable, and expected strings under idea 199. |
| Prepared lookup helper oracle | `tests/backend/bir/backend_prepared_lookup_helper_test.cpp` exercises prepared helper families, route/prepared agreement, fail-closed statuses, and x86 `ConsumedPlans` Route 6 agreement/fallback. | Diagnostics plan keeps helper oracle until route-native helper families reproduce success and fallback semantics. | Helper oracle tests for prepared lookup status, route/prepared agreement, Route 6 x86 absent/mismatch fallback, and Route 8 fail-closed cases. | Classified for Step 3 as diagnostic/oracle compatibility surface. | Diagnostic/printer/oracle compatibility surface retained. Individual route facts may own semantic identities, but helper status strings and fallback behavior remain oracle authority until equivalent route-native helpers exist. | Replacement must be route-family-specific and prove success, no-match, absent, mismatched, duplicate/conflict, policy-excluded, and fallback cases without weakening helper messages or expected statuses. |
| AArch64 lookup threading as compatibility surface | `PreparedFunctionLookups` threaded through `FunctionLoweringContext` in `src/backend/mir/aarch64/module/module.hpp` and constructed in `src/backend/mir/aarch64/codegen/traversal.cpp`; tests attach prepared lookups to lowering contexts. | Diagnostics plan treats target-local route-view delivery as unproven for every consumed fact family. | AArch64 route-consuming tests and context construction. | Classified for Step 3 as production pass context with compatibility pressure, not a diagnostic surface by itself. | Transient pass context/private construction cache. Prepared lookup threading should be reduced only by selected fact migrations and retained fallback/oracle proof. | One route fact threading payload at a time, with route/prepared equivalence and no aggregate contraction claim. |
| Target-wrapper compatibility tests | x86 wrapper tests such as direct extern Route 6 fallback and joined-branch handoff, plus riscv prepared edge-publication tests that assert emitted strings and fail-closed behavior. | Diagnostics plan keeps wrapper assertions while adapters still consume prepared lookup state. Source tests show x86 Route 6 fallback preserves prepared call arguments when Route 6 facts are absent/mismatched, and riscv helper tests reject rediscovery after shared lookup authority is removed or signedness/extension authority is missing. | x86/riscv wrapper tests, route-debug/handoff tests. | Classified for Step 3 as target-wrapper compatibility/oracle surface. | Diagnostic/oracle compatibility plus retained target/prepared policy. Wrapper tests are proof obligations, not route ownership. | One target-wrapper route-input payload at a time. Proof must include emitted-output stability, absent/mismatch prepared fallback, fail-closed unsupported policy, and idea 199 string authority. |
| Baseline and string-authority discipline | `test_baseline.log`, `test_baseline.new.log`, string-authority guard rules as cited by ideas 190 and 199. | Idea 190 and 199 reject accepting regressive baselines, weakening expectations, or hiding string-authority failures. | Any future classification touching fallback, target policy, docs authority, baselines, dump/printer/debug output, or expected strings. | Classified for Step 3 as cross-cutting guardrail for all wrapper and diagnostic/oracle rows. | Retained baseline/string-authority discipline outside BIR. It constrains proof quality rather than owning semantic facts. | Every fallback, target-policy, dump/printer/debug, helper/oracle, or expected-output payload must include non-regressive baseline/string-authority proof and must reject expectation rewrites as ownership evidence. |

## Accepted Route Candidates

No accepted brand-new BIR route is justified by this audit. The audit accepts
only narrow existing Route 1-8 extension candidates where the table already
identifies a target-neutral semantic fact and also identifies retained
prepared/target policy.

| Accepted candidate | Semantic fact | Owner category | Excluded target/prepared policy | Proof route | Reject signals |
| --- | --- | --- | --- | --- | --- |
| Route 1 selected producer/constant consumer or diagnostic row | Same-block producer identity, integer constant identity, and scalar producer/constant status for one selected consumer or visible row. | Existing Route 1 extension candidate. | Scalar materialization sequence, target operand selection, publication/call/memory context, prepared fallback, prepared printer/helper status wording, and expected-output authority. | Prove route/prepared agreement for producer, constant, no-producer, and non-constant cases; prove unchanged prepared-printer/helper output for any diagnostic replacement. | Aggregate source-producer lookup replacement; accepting printer diffs as cleanup; weakening no-producer/non-constant messages; claiming AArch64 reader success retires helper/oracle surfaces. |
| Route 2 selected select-chain/direct-global consumer or diagnostic row | Select-root identity and direct-global dependency identity for one selected chain or direct-global use. | Existing Route 2 extension candidate. | Materialization policy, direct-global target policy, publication/call/memory context, scalar eligibility policy, fallback helper behavior, and printer/oracle rendering. | Prove route/prepared agreement for direct-global present/absent, nested select, scalar eligibility, source-producer, and fallback cases before replacing one consumer or one diagnostic row. | Treating select-chain cache removal as semantic proof; broad replacement of prepared select-chain helpers; expected-string refreshes that hide lost fallback rows. |
| Route 3 selected memory/source identity consumer | Memory access identity, same-block global-load access identity, or load-local stored-value source identity for one selected reader. | Existing Route 3 extension candidate. | Address formation, frame/global/TLS relocation, stack/frame offsets, volatile/address-space policy, materialization, final operands, and prepared target-addressing fallback. | Prove route/prepared agreement plus absent/mismatch fallback for the selected reader; include idea 190 baseline discipline so target-addressing regressions cannot be hidden by baseline or string rewrites. | Moving frame/addressing policy into BIR; dropping prepared fallback; proving only a narrow happy path; refreshing expectations to mask target-addressing changes. |
| Route 4 selected publication identity or wrapper adapter | Publication destination/source identity for one current/block-entry or edge-publication use. | Existing Route 4 extension candidate. | Edge-copy emission, move/home/storage policy, block-order emission, wrapper formatting, and prepared edge-publication lookup authority. | Prove predecessor/successor or block-entry agreement, destination/source ids, unique match, duplicate/ambiguous rejection, absent/mismatch fallback, and unchanged emitted/debug strings. | Whole `edge_publications` lookup replacement; wrapper migration without emitted-output proof; weakening x86/riscv compatibility tests or fail-closed behavior. |
| Route 5 selected edge/join-source identity or wrapper adapter | Join-edge/source identity for one edge publication, join, or compare/join wrapper use. | Existing Route 5 extension candidate. | Parallel-copy scheduling, source/destination homes, move bundles, branch spelling, scratch policy, final edge-copy records, and wrapper formatting. | Prove route/prepared agreement for join edge/source, duplicate/conflict rejection, prepared fallback, and adjacent branch/parallel-copy sanity. | Treating route join identity as ownership of move scheduling; changing branch/edge-copy output; accepting wrapper-only proof without conflict and fallback coverage. |
| Route 6 selected call-use source adapter | Call argument or result source identity for one call instruction and one role, including the x86 scalar source gate when route and prepared agree. | Existing Route 6 extension candidate. | ABI placement, call wrapper kind, clobbers, outgoing stack, byval lanes, variadic FPR counts, helper/carrier protocols, value homes, move bundles, publication routing, and final call records. | Prove route/prepared source-id agreement, absent Route 6 fail-closed behavior, mismatched source-id fail-closed behavior, preserved prepared call-plan fallback, and unchanged AArch64/x86 output. | Whole call-plan replacement; claiming prepared fallback is unnecessary; moving ABI policy into BIR; weakening direct-call or helper-oracle messages. |
| Route 7 selected comparison provenance reader | Comparison provenance for one fused, materialized, or branch-condition reader. | Existing Route 7 extension candidate. | Branch spelling, condition-code choice, fused legality, hazards, emitted-register state, and final instruction order. | Prove provenance equality for fused, materialized, unfused fallback, absent-route, and invalid-reference cases while keeping branch policy target-owned. | Treating provenance as ownership of branch emission; replacing debug/handoff rows without expected-string proof; ignoring invalid-reference or fallback paths. |
| Route 8 return-chain citation or diagnostic row | Target-neutral same-block return-chain identity already imported into Route 8. | Existing Route 8 extension/import candidate. | Return ABI, homes, registers, scratch policy, ALU record emission, final return emission order, and helper/oracle expected strings. | Usually citation-only. Any diagnostic replacement must prove helper-oracle equivalence for terminal/next operand, unsupported, unnamed, broken-walk, cross-block, missing-instruction, and duplicate/conflict cases. | Reintroducing deleted prepared helper names; claiming return ABI ownership; replacing helper expected strings without full fail-closed coverage. |

## Rejected Retained Surfaces

These surfaces are rejected as BIR route candidates for this audit. They remain
outside BIR until a later, narrower idea proves one target-neutral semantic
fact and preserves the retained policy or compatibility obligations.

| Rejected retained surface | Retained owner | Rationale |
| --- | --- | --- |
| `PreparedBirModule` as a whole | Mixed prepared/prealloc aggregate plus route-owned subfacts, target/prepared policy, transient pass context, and diagnostic/oracle compatibility. | The aggregate mixes BIR, target profile, names/control-flow, locations, frame/stack/dynamic-stack, calls, publication, storage, carriers, atomics, helpers, inline asm, and notes. Whole-aggregate BIR cloning, facade renaming, deletion, demotion, or retirement readiness would hide field-level ownership and fallback obligations. |
| `PreparedFunctionLookups` as a whole | Transient pass context/private construction cache with field-level mixed owners. | The aggregate is a per-function cache/facade. Replacing it as a unit would conflate route facts with address, memory, move, value-home, edge-publication, and source-producer policy while leaving public target wrappers and oracles unresolved. |
| Address materialization, value homes, frame, dynamic stack, storage, and special target-entry surfaces | Target/prepared policy. | These encode layout, offsets, homes, materialization, stack adjustment, ABI/entry policy, and operand rendering. No row proves a pure route-native fact that can own those policies. |
| Move bundles, call-plan ABI fields, wide carriers, runtime helper protocols, variadic entry helpers, intrinsic helpers, inline-asm helpers, and atomic helper policy | Target/prepared policy plus diagnostic compatibility where printed or tested. | These carry scheduling, ABI, helper protocol, target lowering, constraints, clobbers, width/order, scratch resources, and final record policy. Route source identities may feed inputs but do not own these target decisions. |
| AArch64 lookup threading and lowering context | Transient pass context/private construction cache plus retained target/prepared policy. | Threading `PreparedBirModule` and `PreparedFunctionLookups` through `FunctionLoweringContext` is delivery and pass state, not a semantic BIR fact. It should be reduced only by selected consumer migrations, one consumed fact at a time. |
| x86 broad `prepared::Query`, `ConsumedPlans`, and wrapper facade | Target/prepared policy and wrapper compatibility, except for the accepted narrow Route 6 source identity gate. | The facade reads addressing, locations, calls, regalloc, storage, BIR lookup, decoded homes, call-boundary moves, formal publication, and block-entry publications. Broad migration would move target-local wrapper policy into BIR. |
| riscv prepared wrapper emission | Target/prepared policy and target-wrapper compatibility. | Current evidence shows no accepted route-view reuse point. Register naming, stack/immediate handling, extension policy, scratch registers, emitted strings, and fail-closed unsupported behavior remain target-local. |
| Prepared printer, prepared CLI dump, x86 route-debug summary/trace, and prepared lookup-helper oracle | Diagnostic/printer/oracle compatibility surface. | These are string and behavior authorities, not production ownership. They render route facts alongside target policy and prepared phase products; replacement requires exact or intentionally versioned visible-output proof under idea 199. |
| Target-wrapper compatibility tests | Diagnostic/oracle compatibility plus retained target/prepared policy. | These tests preserve emitted-output stability, absent/mismatch prepared fallback, fail-closed policy, and wrapper behavior. They prove obligations for future adapters but are not route ownership evidence. |
| Baseline and string-authority discipline | Cross-cutting proof guardrail outside BIR. | Ideas 190 and 199 forbid using baseline refreshes, unsupported downgrades, expected-string rewrites, or timeout/string-authority weakening as evidence of ownership progress. |

## Blocked Unknowns

No surface remains blocked or unknown for this Phase A2 classification. The
audit does identify future proof work, but those are follow-up payloads for
separate route-extension, retained-policy cleanup, or diagnostic/oracle
replacement ideas rather than blockers for the ownership decision artifact.

## Follow-Up Payloads

Payloads below are draft idea bodies for a later plan owner to file as separate
`ideas/open/` records if the supervisor chooses. They are intentionally narrow;
none authorizes schema work, consumer migration, prepared API contraction, or
draft 155 retirement by itself.

### Payload: Phase B2 Selected Route Consumer Extensions

Intent: extend existing Routes 1-7 only for selected consumers where the audit
accepts a target-neutral semantic fact and explicitly excludes target/prepared
policy.

Candidate slices:

- Route 1 producer/constant selected consumer or diagnostic row.
- Route 2 select-chain/direct-global selected consumer or diagnostic row.
- Route 3 memory/source selected consumer, with idea 190 fallback discipline.
- Route 4 publication identity selected consumer or wrapper adapter.
- Route 5 edge/join-source selected consumer or wrapper adapter.
- Route 6 call-use source selected call role, especially the x86 scalar gate.
- Route 7 comparison provenance selected reader.
- Route 8 citation/import or diagnostic row only.

Required proof for every slice: route/prepared agreement for success cases;
absent, mismatched, ambiguous, duplicate/conflict, and invalid-reference
fail-closed coverage where applicable; preserved prepared fallback; unchanged
target output or intentionally versioned diagnostic output; no baseline,
unsupported-marker, or expected-string weakening.

Reject signals: aggregate lookup replacement, wrapper-wide migration, facade
renaming, moving ABI/layout/storage/branch/helper policy into BIR, claiming one
production reader retires diagnostics/oracles, or proving only a named testcase
without adjacent fallback and conflict cases.

### Payload: Retained Target/Prepared Policy Cleanup Map

Intent: create a cleanup map for retained target/prepared policy surfaces that
should remain outside BIR while future code can reduce public prepared facade
pressure.

Retained surfaces to map: address materializations, memory target-addressing
policy, value homes, move bundles, frame, dynamic stack, storage, special
target entries, wide carriers, runtime helper protocols, variadic entry,
intrinsics, inline asm, atomics, x86 broad `prepared::Query`, and riscv edge
emission policy.

Required output: one row per retained surface with the public API, current
consumers, target/prepared policy reason, any route facts that may feed it, the
required fallback/oracle tests, and the precise condition under which one API
could be made private or target-local.

Reject signals: declaring private/deletion readiness without a real consumer
boundary reduction; moving storage, layout, ABI, scratch, helper, branch, or
string authority into BIR; using expectation rewrites as cleanup evidence.

### Payload: Prepared Diagnostic And Oracle Replacement Ladder

Intent: replace prepared diagnostic/oracle compatibility one visible row family
at a time only after route-native output authority is proven.

Candidate row families: prepared printer route-owned sections, prepared CLI
dump snippets, x86 route-debug summary/trace rows, prepared lookup-helper route
families, and target-wrapper compatibility rows.

Required proof: exact or explicitly versioned output equivalence for the row
family; focus/filter behavior where applicable; success, no-match, absent,
mismatched, duplicate/conflict, policy-excluded, unsupported, and fallback
cases; no weakening of expected strings or baseline/string-authority guards
from idea 199.

Reject signals: replacing printer/dump/debug output because production lowering
already uses a route fact; removing prepared dump equality before replacement
authority exists; narrowing tests to a single happy-path route row.

### Payload: Prepared Aggregate Retirement Prerequisite Audit

Intent: prepare, but not execute, a later field-by-field prerequisite map for
draft 155 `PreparedBirModule` retirement.

Required scope: every `PreparedBirModule` field and every
`PreparedFunctionLookups` field, mapped to one of: existing route fact,
retained target/prepared policy, transient pass context/private cache,
diagnostic/oracle compatibility, or blocked/unknown.

Required proof: for each field, list current consumers, replacement owner,
fallback/oracle obligations, and whether any public API boundary can be reduced
without schema or behavior changes.

Reject signals: whole-aggregate BIR clone, rename-only facade,
classification-only deletion readiness, or treating this Phase A2 audit as
approval to open draft 155.

### Payload: Focused Blocked-Surface Audits If New Evidence Appears

Intent: reserve a route for later method-level audits when a future source
inspection finds a pure semantic reader inside a currently retained surface.

Eligible surfaces: one intrinsic helper method, one inline-asm helper method,
one atomic helper reader, one runtime-helper record, one riscv edge-publication
adapter method, or one x86 `prepared::Query` method.

Required proof: name the method, the exact semantic fact, the current consumers,
the excluded target/prepared policy, route/prepared agreement and fallback
cases, and diagnostic/oracle strings affected.

Reject signals: broad helper-family migration, route candidates based only on
type names, or reducing wrapper/test coverage before the method-level owner is
proven.

## Closure Notes

Step 1 completion state:

- The audit artifact exists and names the source idea.
- Every required input is recorded as read; none are missing.
- The residual table has placeholder rows for known prepared/prealloc surface
  groups from the source idea and prior Phase A-D artifacts.

Step 2 completion state:

- `PreparedBirModule`, `PreparedFunctionLookups`, and the seven lookup groups
  are classified with source and prior-artifact evidence.
- Mixed rows identify route-native subfacts separately from retained
  prepared/target policy, pass context, and diagnostic/oracle obligations.
- No Step 2 row claims contraction, deletion, demotion, or retirement readiness
  from classification alone.

Step 3 completion state:

- Cross-target coverage is classified for AArch64, x86, and riscv wrappers.
- The table separates production-lowering/pass-context ownership from
  diagnostic, printer, dump, helper, and oracle compatibility surfaces.
- Diagnostic/oracle rows name the equivalence proof needed before replacement,
  including expected-output/string-authority obligations.
- Idea 190 and 199 guardrails are applied to rows touching fallback, target
  policy, baselines, and dump/printer/debug/helper strings.
- No Step 3 row omits x86 or riscv prepared wrapper contracts merely because
  they are not on the AArch64 production path.

Step 4 completion state:

- Accepted candidates are limited to existing Route 1-8 extension/import
  boundaries; no brand-new route or aggregate BIR clone is accepted.
- Each accepted candidate names the semantic fact, owner category, excluded
  target/prepared policy, proof route, and reject signals.
- Rejected retained surfaces are grouped by target/prepared policy, transient
  pass context/cache, diagnostic/printer/oracle compatibility, wrapper
  compatibility, and baseline/string-authority guardrails.
- No blocked or unknown surfaces remain for Phase A2 classification; future
  unknowns are represented as follow-up payload conditions.
- Follow-up payloads are specific enough for later plan-owner filing while
  preserving the analysis-only boundary and rejecting testcase-overfit,
  expectation rewrites, and prepared API contraction claims.

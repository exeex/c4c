# Phase B2 Selected Route Extension Schema Readiness

Status: Step 3 proof and reject coverage recorded.

Source idea:
`ideas/open/201_phase_b2_selected_route_extension_schema_readiness_audit.md`

## Scope

This artifact records the evidence set for the Phase A2 accepted Route 1-8
extension/import candidates. It is limited to the target-neutral semantic fact,
the accepted route boundary, the excluded target/prepared policy, and the
retained fallback or oracle surface for each candidate.

## Sources Read

| Source | Evidence captured |
| --- | --- |
| `docs/bir_prealloc_fusion/phase_a2_residual_semantic_owner_audit.md` | Accepted Route 1-8 candidate set, excluded policies, proof routes, reject signals, and retained surface groups. |
| `docs/bir_prealloc_fusion/phase_a_normalization_candidates.md` | Original Route 1-7 normalization boundaries, dependency order, schema candidates, and target-policy rejects. |
| `docs/bir_prealloc_fusion/phase_b_annotation_schema_candidates.md` | Existing schema/annotation placement evidence, bridge/oracle surfaces, non-schema boundaries, and follow-up schema idea links for Routes 1-7. |
| `docs/bir_prealloc_fusion/return_chain_import_and_naming.md` | Route 8 return-chain identity, fail-closed behavior, target-local policy exclusions, and historical prepared-oracle status. |
| `docs/bir_prealloc_fusion/prepared_function_lookups_ownership_readiness_map.md` | Lookup-group ownership boundaries, retained prepared fallback/oracle surfaces, adapter prerequisites, and no aggregate replacement readiness. |
| `docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md` | Prepared printer, dump, route-debug, helper-oracle, AArch64 threading, and target-wrapper retention requirements. |
| `ideas/closed/190_full_suite_baseline_99_percent_regression_attribution.md` | Route 3 baseline lesson: semantic memory/source identity does not retire prepared target-addressing fallback. |
| `ideas/closed/199_full_suite_baseline_string_authority_timeout_attribution.md` | String-authority guardrail: expected strings, baselines, and guard behavior must not be weakened as ownership evidence. |

Missing required sources: none.

## Evidence Matrix

| Route | A2 accepted candidate | Semantic fact | Accepted boundary | Excluded target/prepared policy | Retained fallback/oracle surface | Step 1 note |
| --- | --- | --- | --- | --- | --- | --- |
| Route 1 | Selected producer/constant consumer or diagnostic row. | Same-block producer identity, integer constant identity, and scalar producer/constant status for one selected consumer or visible row. | Existing Route 1 producer/source identity: value/name/type, target-neutral producer kind, producer instruction/index, materialization availability, immediate constants, and function-local lookup identity. | Scalar materialization sequence, target operand selection, publication/call/memory context, prepared fallback, prepared printer/helper status wording, expected-output authority, homes, registers, storage state, emitted-register availability, spill/reload behavior, frame slots, and final instruction order. | Prepared same-block producer, same-block value materialization, integer-constant, scalar operand, value-publication, prepared printer, prepared lookup-helper, and source-producer diagnostic/oracle rows remain comparison or compatibility surfaces. | Existing Phase B evidence already identifies value/instruction annotation placement and function-local indexes; later readiness must decide whether the selected consumer needs only existing Route 1 records/indexes or a small extension. |
| Route 2 | Selected select-chain/direct-global consumer or diagnostic row. | Select-root identity and direct-global dependency identity for one selected chain or direct-global use. | Existing Route 2 select-chain/direct-global identity: root value/name, root-is-select flag, root instruction index, direct `LoadGlobalInst` dependency, scalar materialization eligibility, and combined lookup identity. | Materialization policy, direct-global target policy, publication/call/memory context, scalar eligibility as target routing policy, fallback helper behavior, printer/oracle rendering, register availability, hazards, call ABI behavior, and final move/branch choices. | Prepared select-chain dependency, direct-global dependency, scalar select-chain materialization, source-producer helper, prepared printer, prepared lookup-helper, and dump/debug oracle rows remain comparison or compatibility surfaces. | Existing Phase B evidence already identifies value/instruction annotations and function-local indexes; later readiness must keep call/memory/publication policy out of any Route 2 schema conclusion. |
| Route 3 | Selected memory/source identity consumer, with idea 190 fallback discipline. | Memory access identity, same-block global-load access identity, or load-local stored-value source identity for one selected reader. | Existing Route 3 memory/access semantic identity: load/store/address node identity, result or stored value, address-space/volatile bits, semantic base kind, local/global/pointer/string source identity, same-block global-load identity, and same-block load-local source identity. | Address formation, frame/global/TLS relocation, stack/frame offsets, concrete size/align layout, base-plus-offset legality, GOT/direct/page-low policy, target addressing-mode choice, volatile/address-space policy when consumed as target lowering policy, materialization, final operands, and prepared target-addressing fallback. | Prepared memory access, same-block global-load, load-local source, store-source, memory/frame/stack oracle, prepared lookup-helper, and target-addressing fallback surfaces remain active. Idea 190 requires non-regressive baseline discipline around target-addressing fallback. | Existing Phase B evidence already identifies instruction/value annotations and function-local indexes; later readiness must preserve the prepared fallback that idea 190 showed is still required for AArch64 target-addressing behavior. |
| Route 4 | Selected publication identity or wrapper adapter. | Publication destination/source identity for one current/block-entry or edge-publication use. | Existing Route 4 block-entry/current-block publication identity: block/program-point availability, produced value/name, consumed value/name, source producer identity, producer instruction/index, and source-producer kind. | Edge-copy emission, move/home/storage policy, hook kind, destination home, storage encoding, stack-source extension policy, register-view conversion, immediate publication payloads, block-order emission, wrapper formatting, and prepared edge-publication lookup authority. | Prepared current-block and block-entry publication reads, edge-publication lookup surfaces, x86/riscv wrapper tests, route-oracle tests, prepared printer/debug rows, and prepared lookup-helper coverage remain comparison or compatibility surfaces. | Existing Phase B evidence already identifies block/value annotation placement; later readiness must separate source/value/producer identity from publication mechanics and wrapper output. |
| Route 5 | Selected edge/join-source identity or wrapper adapter. | Join-edge/source identity for one edge publication, join, or compare/join wrapper use. | Existing Route 5 CFG edge publication and join-source identity: predecessor/successor/destination/source identity, source BIR value/name/kind, source producer block/instruction, optional memory-source identity, no-source marker, and current-block join incoming-expression/source identity. | Parallel-copy scheduling, source/destination homes, move bundles, branch spelling, scratch policy, final edge-copy records, move order, cycle temporary routing, execution-site placement, carrier/phase policy, coalescing/redundancy flags, destination registers, storage-sharing checks, and wrapper formatting. | Prepared edge-publication, source-producer, edge-copy source, current-block join parallel-copy, move-bundle, x86/riscv wrapper, route-debug, and helper-oracle surfaces remain comparison or compatibility surfaces. | Existing Phase B evidence already identifies edge/block annotation placement; later readiness must decide only the selected semantic edge/join source boundary and keep move scheduling downstream. |
| Route 6 | Selected call-use source adapter. | Call argument or result source identity for one call instruction and one role, including the x86 scalar source gate when route and prepared agree. | Existing Route 6 call-boundary semantic source facts: `CallInst` argument/result source value/base value/name, target-neutral pointer delta where representable, materializable same-block producer, direct-global select-chain dependency, memory/source link, publication-source route identity, and call-site ordinal indexes. | ABI placement, call wrapper kind, clobbers, outgoing stack sizing, byval lanes, variadic FPR counts, helper/carrier protocols, destination homes, value homes, move bundles, publication routing when ABI/layout-bound, final call records, preservation/clobber policy, and aggregate transport layout. | Prepared call plans, call argument/result plans, call-boundary tests, prepared lookup-helper, x86 route-debug compatibility rows, AArch64 call oracles, x86 `ConsumedPlans` fallback, and prepared call-plan fallback remain comparison or compatibility surfaces. | Existing Phase B evidence already identifies call-instruction/value annotations and function-local indexes; later readiness must keep the selected source-id adapter narrower than whole call-plan replacement. |
| Route 7 | Selected comparison provenance reader. | Comparison provenance for one fused, materialized, or branch-condition reader. | Existing Route 7 comparison/materialized-condition producer identity: comparison-producing `BinaryInst`, condition value/name, lhs/rhs producer nodes or integer constants, producer instruction index, fused-operand provenance, materialized-condition producer, and branch-condition provenance. | Branch spelling, condition-code choice, fused legality as target lowering policy, hazards, emitted-register state, final instruction order, branch emission strategy, final instruction records/errors, and AArch64 compare/branch instruction selection. | Prepared fused-compare operand, materialized-condition producer, scalar producer/select-chain fallback, route-debug/handoff rows, prepared lookup-helper, prepared printer/debug, and target branch-policy oracle surfaces remain comparison or compatibility surfaces. | Existing Phase B evidence already identifies instruction/terminator annotation placement; later readiness must treat provenance as separate from branch emission and expected-output authority. |
| Route 8 | Return-chain citation or diagnostic row. | Target-neutral same-block return-chain identity already imported into Route 8: terminal return-chain value and immediate next-operand identity in a same-block scalar binary chain ending at a return terminator. | Existing Route 8 return-chain owner/schema line: owning function/block context, stable binary instruction reference or position, named chain value, named terminal return value, optional named next operand, route8 records/indexes/lookups, and fail-closed behavior for unsupported or conflicting cases. | Return ABI, homes, registers, scratch policy, ALU record emission, final return emission order, value-home lookup, missing-home behavior, target register parsing/conversion, alias detection against selected result registers, scalar register view, reserved scratch selection, and helper/oracle expected strings. | Historical prepared return-chain helpers are no longer public after idea 180, but ideas 176-179 remain historical oracle/migration evidence. Current Route 8 source/tests and any diagnostic replacement must preserve route8 fail-closed cases and helper-oracle equivalence expectations where visible. | Existing Route 8 evidence is normally citation-only for this phase; later readiness should avoid treating Route 8 as proof for broader lookup contraction or target return policy. |

## Retained Cross-Cutting Surfaces

| Surface | Retained evidence |
| --- | --- |
| Prepared diagnostic and oracle surfaces | Prepared printer, prepared CLI dump, x86 route-debug summary/trace, prepared lookup-helper, target-wrapper tests, and AArch64 lookup-threading tests remain compatibility surfaces until route-native diagnostics prove equivalent success, negative, ambiguity/conflict, mismatch, and fallback behavior. |
| Prepared aggregate and lookup facades | `PreparedBirModule` and `PreparedFunctionLookups` remain mixed aggregate/pass-context/cache surfaces. The accepted evidence supports only selected route fact boundaries, not aggregate cloning, deletion, privatization, or facade replacement. |
| Baseline and string authority | Ideas 190 and 199 require non-regressive baseline handling and forbid using expected-string rewrites, unsupported downgrades, guard weakening, or baseline refreshes as ownership evidence. |

## Step 2 Schema Sufficiency Classification

| Route | Schema-readiness classification | Existing schema/index evidence | Reason | Retained prepared fallback/oracle note |
| --- | --- | --- | --- | --- |
| Route 1 | Schema sufficient for one selected semantic consumer; schema/index extension only if the selected row needs a new reference-validation or diagnostic wrapper. | `Route1ProducerRecord`, `Route1ProducerIndex`, `Route1SameBlockProducerQuery`, `route1_build_producer_index(...)`, `route1_find_same_block_scalar_producer(...)`, `route1_find_materialization_availability(...)`, and immediate-constant/source-value records. | Existing records cover the Route 1 accepted fact shape: same-block producer identity, source value/name/type, producer kind, producer instruction/index, materialization availability, and integer constants. No new durable schema is needed for a consumer that asks only these facts. | Keep prepared same-block producer, value materialization, integer-constant, source-producer helper, printer/debug, and helper-oracle paths as compatibility surfaces. Prepared fallback remains required for status wording, policy-sensitive scalar materialization, and any diagnostic/oracle replacement until route/prepared success, no-producer, non-constant, mismatch, and fallback cases match. |
| Route 2 | Schema sufficient for one selected select-chain/direct-global semantic consumer; compatibility adapter likely for consumers still coupled to materialization or policy. | `Route2SelectChainProducerRecord`, `Route2SelectChainDirectGlobalDependencyRecord`, `Route2SelectChainValueRecord`, `Route2SelectChainValueIndex`, `route2_build_select_chain_value_index(...)`, and `route2_find_select_chain_value_record(...)`. | Existing records/indexes cover root value identity, root producer/instruction, root-is-select, direct `LoadGlobalInst` dependency, scalar materialization availability as a semantic eligibility fact, and combined lookup identity. They do not justify replacing call, memory, publication, hazard, or target materialization policy. | Keep prepared select-chain dependency, direct-global dependency, scalar select-chain materialization, source-producer helper, printer/debug, and lookup-helper oracle paths. Any adapter must preserve prepared fallback for absent direct-global, nested select, scalar-eligibility mismatch, source-producer mismatch, and policy-excluded cases. |
| Route 3 | Schema sufficient for selected memory/source identity; retained-policy cleanup or compatibility adapter is required for readers that still ask target-addressing policy. | `Route3MemoryAccessRecord`, `Route3MemoryAccessValueRecord`, `Route3MemoryAccessIndex`, `Route3SameBlockGlobalLoadAccessRecord`, `Route3SameBlockLoadLocalSourceRecord`, `Route3SameBlockLoadLocalStoredValueSourceRecord`, `route3_build_memory_access_index(...)`, `route3_find_memory_access_record(...)`, `route3_find_same_block_global_load_access(...)`, and `route3_find_same_block_load_local_stored_value_source(...)`. | Existing records cover access node identity, result/stored value identity, base/source kind, local/global/pointer/string source identity, address-space/volatile flags as BIR facts, same-block global-load identity, and load-local stored-value source. They do not cover frame offsets, relocation choice, addressing-mode legality, materialization, or final operands. | Retain prepared memory access, same-block global-load, load-local source, store-source, memory/frame/stack oracle, lookup-helper, and target-addressing fallback surfaces. Idea 190 makes target-addressing fallback non-retirable from Route 3 schema sufficiency alone; proof must include route/prepared mismatch and non-regressive baseline/string-authority handling. |
| Route 4 | Schema sufficient for current-block and block-entry publication identity; compatibility adapter needed for wrapper or edge-publication consumers. | `Route4CurrentBlockPublicationRecord`, `Route4BlockEntryPublicationRecord`, `Route4PublicationValueRecord`, `Route4PublicationAvailabilityIndex`, `route4_build_publication_availability_index(...)`, `route4_find_current_block_publication(...)`, and route4 reference-validation APIs. | Existing records/indexes cover block/program-point availability, destination/produced/consumed value identity, source producer identity, producer instruction/index, and source-producer kind. They do not own edge-copy emission, homes/storage, stack-source extension, block-order emission, or wrapper formatting. | Keep prepared current-block and block-entry publication reads, edge-publication lookup surfaces, x86/riscv wrappers, route-oracle tests, prepared printer/debug rows, and helper coverage. Adapter proof must retain absent/mismatch/duplicate or wrong-reference fallback and unchanged emitted/debug strings. |
| Route 5 | Schema sufficient for selected CFG edge and current-block join-source identity; compatibility adapter needed for any wrapper or edge-copy use. | `Route5CfgEdgePublicationRecord`, `Route5CurrentBlockJoinSourceRecord`, `Route5PublicationValueRecord`, `Route5EdgeJoinSourceIndex`, `route5_build_edge_join_source_index(...)`, `route5_find_cfg_edge_publication(...)`, and `route5_find_current_block_join_source(...)`. | Existing records/indexes cover predecessor/successor/destination/source identity, source producer block/instruction, no-source and memory-source statuses, and current-block join incoming source identity. They do not cover parallel-copy scheduling, move bundles, storage/homes, branch spelling, execution-site policy, or final edge-copy records. | Keep prepared edge-publication, source-producer, edge-copy source, current-block join parallel-copy, move-bundle, x86/riscv wrapper, route-debug, and helper-oracle surfaces. Any selected adapter must prove route/prepared agreement plus duplicate/conflict, no-source, mismatch, prepared fallback, and emitted-output stability. |
| Route 6 | Schema sufficient for one call-use source identity role; compatibility adapter only for the selected argument/result source read, not a call-plan replacement. | `Route6CallArgumentSourceRecord`, `Route6CallArgumentSourceProducerRecord`, `Route6CallArgumentDirectGlobalDependencyRecord`, `Route6CallArgumentPublicationSourceRecord`, `Route6CallResultSourceRecord`, `Route6CallResultLaneSourceRecord`, `Route6CallUseSourceIndex`, `route6_build_call_use_source_index(...)`, and route6 find APIs for argument, result, producer, direct-global, publication, and lane source records. | Existing records/indexes cover call-site ordinal source identity, argument/result value or base value/name, materializable same-block producer, direct-global dependency, publication-source identity, and selected result-lane identity. They do not cover ABI placement, outgoing stack, clobbers, helper protocols, value homes, move bundles, aggregate lanes as transport policy, or final call records. | Keep prepared call plans, call argument/result plans, call-boundary tests, lookup-helper, x86 route-debug rows, AArch64 call oracles, x86 `ConsumedPlans` fallback, and prepared call-plan fallback. Adapter proof must include source-id agreement, absent/mismatched/ambiguous/ABI-bound fail-closed behavior, and unchanged AArch64/x86 output. |
| Route 7 | Schema sufficient for selected comparison provenance readers; diagnostic/oracle replacement remains separate from branch-policy cleanup. | `Route7ComparisonOperandRecord`, `Route7ComparisonInstructionRecord`, `Route7BranchConditionRecord`, `Route7ComparisonConditionIndex`, `route7_build_comparison_condition_index(...)`, `route7_find_comparison_instruction(...)`, `route7_find_comparison_operand(...)`, `route7_find_materialized_condition(...)`, `route7_find_branch_condition(...)`, and `route7_find_fused_compare_operand_producer_facts(...)`. | Existing records/indexes cover comparison-producing `BinaryInst`, condition value/name, lhs/rhs producer or integer constant provenance, producer instruction index, materialized-condition provenance, and branch-condition provenance. They do not own fused-compare legality, condition-code choice, branch spelling, hazards, emitted-register state, final instruction records, or expected-output authority. | Keep prepared fused-compare operand, materialized-condition producer, scalar producer/select-chain fallback, route-debug/handoff rows, prepared lookup-helper, prepared printer/debug, and target branch-policy oracle surfaces. Replacement proof must cover fused, materialized, unfused fallback, absent-route, invalid-reference, duplicate, and mismatch cases without expected-string weakening. |
| Route 8 | No action for schema/index extension in this phase; diagnostic/oracle replacement only if a visible return-chain row is selected later. | `Route8ReturnChainRecord`, `Route8ReturnChainIndex`, `Route8ReturnChainValueKey`, `route8_build_return_chain_index(...)`, `route8_find_return_chain_record(...)`, `route8_find_return_chain_terminal_value(...)`, and `route8_find_return_chain_next_operand_value(...)`. | Route 8 already has the target-neutral return-chain owner/schema line, records, indexes, lookups, and fail-closed behavior for the imported same-block return-chain identity. Current Phase B2 work should cite it, not use it as proof for broad lookup contraction or target return policy. | Historical prepared return-chain helpers are no longer public after idea 180, but historical oracle/migration evidence and current route8 fail-closed tests remain the compatibility surface. Any diagnostic replacement must preserve terminal/next operand, unsupported, unnamed, broken-walk, cross-block, missing-instruction, duplicate/conflict, and helper-equivalence expectations where visible. |

## Step 3 Proof And Reject Coverage

| Route | Minimum proof recommendation | Concrete reviewer reject signals |
| --- | --- | --- |
| Route 1 | Prove one selected producer/constant consumer or visible diagnostic row with route/prepared agreement for same-block producer identity, integer constant identity, no-producer, non-constant, mismatch, and prepared fallback. If a diagnostic row changes, preserve prepared printer/helper output or add route-native oracle coverage with unchanged string-authority expectations. | Reject any slice that treats scalar materialization, target operand selection, publication/call/memory context, homes/registers/storage, emitted-register availability, spill/reload behavior, final instruction order, prepared status wording, or expected-output strings as BIR schema ownership. Reject aggregate source-producer lookup replacement, prepared fallback removal, no-producer/non-constant message weakening, expected-string refreshes, unsupported downgrades, or a proof that only covers one happy-path testcase. |
| Route 2 | Prove one select-chain/direct-global consumer or diagnostic row with route/prepared agreement for direct-global present/absent, nested select, scalar-eligibility status, source-producer identity, mismatch, and fallback cases. Keep scalar eligibility as semantic eligibility only when the selected reader does not use it as target routing policy. | Reject any slice that converts direct-global target policy, materialization policy, publication/call/memory context, hazards, register availability, final move/branch choices, fallback helper behavior, or printer/oracle strings into BIR schema. Reject broad select-chain helper replacement, cache-removal-as-proof, hidden fallback-row loss, expected-string refreshes, or missing absent/nested-select/scalar-eligibility mismatch coverage. |
| Route 3 | Prove one memory/source identity reader with route/prepared agreement for selected access/source identity plus absent, mismatch, non-memory, alias/address ambiguity, and prepared target-addressing fallback behavior. Include idea 190 baseline discipline so target-addressing regressions cannot be hidden by baseline refreshes or expected-output changes. | Reject any slice that moves address formation, frame/global/TLS relocation, stack/frame offsets, concrete layout, addressing-mode legality, volatile/address-space lowering policy, materialization, final operands, or target-addressing fallback into BIR schema. Reject prepared fallback removal, narrow happy-path-only proof, route/prepared mismatch without fail-closed behavior, baseline refresh as evidence, or string-authority weakening. |
| Route 4 | Prove one current/block-entry publication identity, edge-publication consumer, or wrapper adapter with predecessor/successor or block-entry agreement, destination/source ids, unique match, absent/mismatch fallback, duplicate/ambiguous rejection, and unchanged emitted/debug strings where wrappers or diagnostics are affected. | Reject any slice that treats edge-copy emission, move/home/storage policy, stack-source extension policy, block-order emission, wrapper formatting, immediate publication payloads, prepared edge-publication lookup authority, or emitted strings as BIR schema. Reject whole `edge_publications` replacement, wrapper migration without output proof, duplicate/ambiguous handling gaps, wrong-reference acceptance, or weakened x86/riscv compatibility tests. |
| Route 5 | Prove one CFG edge/join-source consumer or wrapper adapter with route/prepared agreement for predecessor/successor/destination/source identity, no-source and memory-source status, duplicate/conflict rejection, absent/mismatch prepared fallback, and adjacent branch/parallel-copy sanity where wrappers are touched. | Reject any slice that moves parallel-copy scheduling, source/destination homes, move bundles, branch spelling, scratch policy, cycle temporary routing, execution-site placement, coalescing, final edge-copy records, or wrapper formatting into BIR schema. Reject wrapper-only proof without conflict and fallback coverage, output-string changes without string-authority proof, route join identity used as move-scheduling ownership, or broad edge/join lookup contraction. |
| Route 6 | Prove one call instruction and one argument/result role with route/prepared source-id agreement, absent Route 6 fail-closed behavior, mismatched source-id fail-closed behavior, ambiguous or ABI-bound rejection, preserved prepared call-plan fallback, and unchanged AArch64/x86 output. The existing x86 scalar source gate is valid only when route and prepared source ids agree. | Reject any slice that replaces whole call plans, moves ABI placement, call wrapper kind, clobbers, outgoing stack sizing, byval lanes, variadic FPR counts, helper/carrier protocols, value homes, move bundles, aggregate transport layout, publication routing policy, or final call records into BIR schema. Reject claims that prepared fallback is unnecessary, direct-call/helper-oracle message weakening, missing absent/mismatch proof, or output changes hidden by expectation rewrites. |
| Route 7 | Prove one comparison provenance reader with equality for fused compare, materialized condition, branch-condition provenance, unfused fallback, absent-route, invalid-reference, duplicate, and mismatch cases. Keep branch spelling, condition-code choice, fused legality, and expected-output authority target/prepared-owned. | Reject any slice that treats provenance as ownership of branch emission, fused legality as BIR lowering policy, hazards, emitted-register state, final instruction order, final instruction records/errors, or AArch64 compare/branch selection. Reject diagnostic/debug/handoff row replacement without expected-string proof, invalid-reference or fallback gaps, branch-policy cleanup hidden in a provenance adapter, or string-authority weakening. |
| Route 8 | Usually cite the existing Route 8 owner/schema line and fail-closed tests rather than opening schema/index work. If a visible diagnostic row is selected later, prove terminal value, next operand, unsupported opcode, unnamed value, broken walk, cross-block chain, missing instruction, duplicate/conflict, and helper-equivalence behavior. | Reject any slice that reintroduces deleted public prepared return-chain helper names, claims return ABI/home/register/scratch/ALU record/final return emission ownership, uses Route 8 as proof for Route 1/7 or broad lookup contraction, weakens helper expected strings, or lacks full fail-closed coverage. Rejected target/prepared return policy remains outside BIR schema scope. |

## Step 3 Completion Check

- Every Route 1-8 row has a minimum proof recommendation and concrete reviewer
  reject signals.
- Negative, ambiguous, mismatch, invalid-reference, duplicate/conflict,
  policy-sensitive, string-authority, fallback, and oracle cases are named
  where relevant to the selected route boundary.
- Rejected target/prepared policy remains outside BIR schema scope; proof
  expectations require route/prepared agreement or fail-closed fallback rather
  than schema ownership of target policy.
- No follow-up idea files are created in Step 3.

## Step 2 Completion Check

- Every Route 1-8 row has a schema-readiness classification.
- Classifications distinguish existing schema sufficiency from schema/index
  extension, compatibility adapter, diagnostic/oracle replacement,
  retained-policy cleanup, and no-action outcomes.
- Every row preserves the prepared fallback or oracle surface that must remain
  available before any future consumer migration or diagnostic replacement.

## Step 1 Completion Check

- Every A2 accepted Route 1-8 candidate from the source idea is named in the
  matrix.
- Every row records the semantic fact, accepted boundary, excluded
  target/prepared policy, and retained fallback/oracle surface.
- Missing source evidence: none.

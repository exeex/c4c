# Phase A BIR Normalization Candidates

Source idea: `ideas/open/151_phase_a_bir_normalization_candidate_audit.md`
Status: Step 6 close-ready payload assembled.

This artifact is the durable Phase A handoff for classifying which
`Prepared*` facts are target-neutral BIR normalization relationships and which
facts must stay in prealloc, MIR, ABI, stack layout, or target codegen.

## Evidence Inventory

### Inspected Inputs

| Domain | Files or notes inspected | Evidence captured |
| --- | --- | --- |
| Phase contract | `ideas/open/151_phase_a_bir_normalization_candidate_audit.md`, `plan.md`, `docs/bir_prealloc_fusion/README.md` | Phase A is analysis-only and must produce candidate, reject, dependency, follow-up, and proof-route handoff data in this file. |
| Prepared aggregate surface | `src/backend/prealloc/module.hpp`, `src/backend/prealloc/prepared_lookups.hpp` | `PreparedBirModule` aggregates the raw `bir::Module`, target profile, names, control flow, value locations, stack layout, addressing, liveness, regalloc, frame/dynamic stack/call/store-source/variadic/storage/special-carrier/runtime-helper plans, completed phases, and notes. `PreparedFunctionLookups` remains the one-pass aggregate lookup bundle for call plans, address materializations, memory accesses, move bundles, return chains, value homes, edge publications, and source producers. |
| Same-block and select-chain queries | `src/backend/prealloc/select_chain_lookups.hpp`, `src/backend/prealloc/calls.hpp` | Semantic query structs/APIs include `PreparedSameBlockScalarProducer`, `PreparedSameBlockValueMaterializationQuery`, `PreparedCurrentBlockPublicationConsumption`, `PreparedSelectChainDependencyQuery`, `PreparedScalarSelectChainMaterialization`, `PreparedDirectGlobalSelectChainDependency`, and helpers such as `find_prepared_same_block_scalar_producer`, `evaluate_prepared_same_block_integer_constant`, `find_prepared_direct_global_select_chain_dependency`, `find_prepared_select_chain_source_producer`, and `find_prepared_scalar_select_chain_materialization`. |
| Edge publication and current-block source facts | `src/backend/prealloc/publication_plans.hpp` | Semantic-ish fact surfaces include `PreparedEdgePublicationSourceProducer`, `PreparedEdgePublicationSourceProducerLookups`, `PreparedEdgePublication`, `PreparedEdgePublicationLookups`, `PreparedEdgeCopySourceFacts`, `PreparedCurrentBlockJoinParallelCopySourceFact(s)`, and query helpers such as `make_prepared_edge_publication_lookups`, `make_prepared_edge_publication_source_producer_lookups`, `find_indexed_prepared_edge_publication_source_producer`, `prepare_edge_copy_source_facts`, `prepare_block_entry_parallel_copy_edge_source_facts`, and `prepare_current_block_join_parallel_copy_source_facts`. |
| Publication plans with target/layout payloads | `src/backend/prealloc/publication_plans.hpp`, `src/backend/prealloc/formal_publications.hpp` | Mixed surfaces include `PreparedScalarPublicationPlan`, `PreparedStoreSourcePublicationPlan`, `PreparedTypedStackSourcePublication`, `PreparedAggregateStackSourceAuthority`, and `PreparedFormalPublicationPlan`. They combine source/destination identity with home kind, frame slot, stack offset/size/align, register placement/bank, and current-block publication references. |
| Call-boundary facts | `src/backend/prealloc/calls.hpp`, `src/backend/prealloc/call_plans.cpp` | Candidate semantic fields include call-argument source selection, direct-global select-chain dependency, source-producer materialization, and publication source routing. Rejected target/layout payloads include destination register names, occupied register sets, stack offsets/sizes, ABI placements, scratch requirements, and aggregate transport lane layout. |
| Memory/addressing facts | `src/backend/prealloc/addressing.hpp`, `src/backend/prealloc/stack_layout/stack_layout.hpp` | Semantic access surfaces include `PreparedAddress`, `PreparedMemoryAccess`, `PreparedSameBlockGlobalLoadAccess`, `PreparedSameBlockLoadLocalStoredValueSource`, and lookup helpers for same-block load/store source identity. Target/layout payloads include concrete frame slot ids, stack offsets, TLS relocation/register spelling, global materialization policy, and base-plus-offset usability. |
| Comparison facts | `src/backend/prealloc/comparison.hpp` | `PreparedFusedCompareOperandProducer`, `PreparedFusedCompareOperandProducerFacts`, and `PreparedMaterializedConditionProducer` already name comparison-specific producer relationships over same-block/source-producer facts and constants. |
| BIR data surface | `src/backend/bir/bir.hpp`, `src/backend/bir/lir_to_bir/*.cpp`, `src/backend/bir/lir_to_bir/memory/*.cpp` | BIR owns `Module`, `Function`, `Block`, `Inst`, `Terminator`, typed values, `PhiIncoming`, `CallInst`, `LoadLocalInst`, `LoadGlobalInst`, `StoreLocalInst`, `StoreGlobalInst`, `MemoryAddress`, ids for names/slots/blocks/links, intrinsic/atomic metadata, and compare/select/call/address payloads. Current BIR has nodes/blocks/functions but no durable prepared-style annotation surface for producer/publication/edge relationship facts. |
| AArch64 prepared consumers | `src/backend/mir/aarch64/codegen/dispatch_publication.cpp`, `dispatch_producers.cpp`, `dispatch_value_materialization.cpp`, `dispatch_edge_copies.cpp`, `calls.cpp`, `comparison.cpp`, `memory.cpp`, `memory_store_retargeting.cpp`, `operands.cpp`, `instruction.cpp`, `prologue.cpp`, `atomics.cpp`, `f128.cpp` | AArch64 consumes prepared facts for publication, same-block materialization, select-chain dependencies, call materialization/routing, memory retargeting, decoded homes, frame/prologue layout, atomic/f128 carriers, and final instruction records. Emission policy, register view/spelling, scratch selection, ABI lowering, and final instruction order remain target-local consumers, not BIR candidates. |
| Closure notes 130-150 | `ideas/closed/130_*.md` through `ideas/closed/150_*.md` | Recent work moved AArch64 rediscovery into shared prepared query surfaces, then narrowed owners: select-chain in `select_chain_lookups.hpp`, edge publication in `publication_plans.hpp`, call argument materialization in `calls.hpp`, comparison in `comparison.hpp`, addressing load-local source in `addressing.hpp`, stack id helpers in stack-layout ownership, and residual `prepared_lookups.hpp` pressure down to true aggregate/facade use. |

### Prepared Facts Inventory

| Fact group | Current owner | Representative structs/fields/helpers | Initial ownership read |
| --- | --- | --- | --- |
| Same-block scalar producer/materialization | `select_chain_lookups.hpp/.cpp` | `PreparedSameBlockScalarProducer::{producer,instruction,instruction_index,value_name}`, `PreparedSameBlockValueMaterializationQuery`, `find_prepared_same_block_scalar_producer`, `evaluate_prepared_same_block_integer_constant` | Strong BIR-normalization candidate. It describes producer/value identity inside an ordered BIR block before MIR emission. |
| Select-chain dependency | `select_chain_lookups.hpp/.cpp`, with type from `calls.hpp` | `PreparedDirectGlobalSelectChainDependency::{contains_direct_global_load,root_is_select,root_instruction_index}`, `PreparedScalarSelectChainMaterialization`, `find_prepared_direct_global_select_chain_dependency`, `find_prepared_scalar_select_chain_materialization` | Strong BIR-normalization candidate. It relates BIR select/global-load producer structure, not target registers. |
| Current-block publication consumption | `select_chain_lookups.hpp/.cpp`, `value_locations.hpp` | `PreparedCurrentBlockPublicationConsumption::{available,source_producer,instruction,produced_value,instruction_index,value_name,source_producer_kind}` | Candidate with dependency on a BIR publication/entry relationship schema. Keep register-view emission outside BIR. |
| Edge publication and edge-copy source facts | `publication_plans.hpp/.cpp` | `PreparedEdgePublication`, `PreparedEdgePublicationLookups`, `PreparedEdgeCopySourceFacts::{predecessor_label,successor_label,destination_value,source_value,source_producer_*,source_memory_*}`, `prepare_edge_copy_source_facts` | Candidate for edge relationship ownership when limited to predecessor/successor/destination/source/producer identity. Reject move-routing fields that encode parallel-copy execution and homes. |
| Current-block join parallel-copy source facts | `publication_plans.hpp/.cpp` | `PreparedCurrentBlockJoinParallelCopySourceFact(s)`, `prepare_current_block_join_parallel_copy_source_facts` | Mixed. Source/incoming expression identity can become BIR relationship data; AArch64 routing booleans and register-sharing checks should stay target/prealloc-local. |
| Scalar/store publication plans | `publication_plans.hpp/.cpp` | `PreparedScalarPublicationPlan`, `PreparedStoreSourcePublicationPlan`, `plan_prepared_scalar_publication`, `plan_prepared_store_source_publication`, `plan_pending_prepared_store_global_publications` | Mixed. Source/destination value and semantic memory access identity are candidates; concrete homes, stack offsets, frame slots, and publication hook/storage encodings are rejects. |
| Call-boundary semantic facts | `calls.hpp/.cpp` | `PreparedCallArgumentSourceSelection`, `PreparedCallArgumentSourceProducerMaterialization`, `PreparedCallArgumentDirectGlobalSelectChainDependency`, `PreparedCallArgumentPublicationSourceRouting` | Candidate only for source identity, dependency, and materializable-producer relationships. ABI binding, register names, stack area layout, aggregate lane transport, and scratch are rejects. |
| Memory/access identity facts | `addressing.hpp/.cpp`, stack-layout helpers | `PreparedAddress`, `PreparedMemoryAccess`, `PreparedSameBlockGlobalLoadAccess`, `PreparedSameBlockLoadLocalStoredValueSource`, `find_prepared_same_block_load_local_source_producer` | Candidate for semantic access identity, source/stored-value relation, address-space/volatile facts, and local/global/pointer source relationships. Reject concrete frame layout, relocation, target addressing-mode choice, and base-plus-offset legality. |
| Comparison/materialized-condition facts | `comparison.hpp/.cpp` | `PreparedFusedCompareOperandProducer`, `PreparedFusedCompareOperandProducerFacts`, `PreparedMaterializedConditionProducer`, `find_prepared_fused_compare_operand_producer_facts`, `find_prepared_materialized_condition_producer` | Strong BIR-normalization candidate because it names operand/condition producer relationships over BIR compares and same-block producers. |
| Value homes, storage, regalloc, frame, dynamic stack, special carriers | `value_locations.hpp`, `storage.hpp`, `regalloc.hpp`, `frame.hpp`, `dynamic_stack.hpp`, `special_carriers.hpp`, `runtime_helpers.hpp`, `variadic.hpp` | `PreparedValueHome`, `PreparedStoragePlanValue`, `PreparedRegisterPlacement`, `PreparedFrameSlot`, `Prepared*Carrier`, `Prepared*RuntimeHelper` | Mostly rejected from BIR normalization. These encode allocation/layout/ABI/helper emission policy, though some semantic carrier source identity may depend on future annotation phases. |

### BIR Surface Inventory

| Surface | Existing data | Normalization implication |
| --- | --- | --- |
| Node/instruction data | `BinaryInst`, `SelectInst`, `CastInst`, `PhiInst`, `CallInst`, `LoadLocalInst`, `LoadGlobalInst`, `StoreLocalInst`, `StoreGlobalInst` | Enough raw node data exists to compute many producer, select, compare, call, and memory relationships before prealloc. Missing piece is durable relationship storage/query authority. |
| Block/function data | `Block::{label,insts,terminator,label_id}`, `Function::{params,local_slots,blocks,atomic_operations}`, `Terminator` label ids and values | Edge, predecessor/successor, entry-publication, and current-block relationships can be owned next to block/function structure if a schema is added later. |
| Value/name identity | `Value`, `NameTables`, `ValueNameId`, `BlockLabelId`, `SlotNameId`, `LinkNameId` | Semantic identity can be expressed without target registers or frame offsets. |
| Memory/call metadata | `MemoryAddress`, `CallArgAbiInfo`, `CallResultAbiInfo`, `IntrinsicOperation`, `AtomicOperation` | Some call/memory semantic facts already exist in BIR. Target ABI placement and final materialization remain outside BIR. |

### Closure Notes 130-150 Inventory

| Closed idea | Relevant closure signal |
| --- | --- |
| 130 | Dispatch audit found shared-contract gaps around producer, publication, edge-copy, current-block, and select-chain facts; target-local emission should remain AArch64-owned. |
| 131 | Privatized AArch64 edge-copy helper surface while preserving prepared edge-copy/publication facts. |
| 132 | Trimmed AArch64 dispatch lookup surface; retained `make_named_prepared_result_register` and `emitted_scalar_value_available` as real hooks. |
| 133 | Extracted shared prepared value-home query and identified remaining prepared ids, return chains, move bundles, call plans, memory accesses, preserved values, edge producers, select-chain, memory-access, and call-plan opportunities. |
| 134 | Shared same-block materialization and select-chain dependency query facades; kept emission/hazard behavior target-local. |
| 135 | Added shared current-block entry publication query while preserving AArch64 register-view and operand spelling. |
| 136 | Analysis-only audit classified prepared-query surfaces and produced owner-cleanup follow-ups. |
| 137 | Made `select_chain_lookups.hpp` the public owner for select-chain query declarations and types. |
| 138 | Moved call-plan lookup indexes through call-domain ownership while preserving call semantics. |
| 139 | Moved addressing lookup ownership toward addressing/memory ownership. |
| 140 | Split reusable edge publication/source/home/move facts from AArch64-shaped current-block routing conveniences. |
| 141 | Residual audit kept `PreparedFunctionLookups` and return-chain helpers under `prepared_lookups.*`, and created follow-ups 142-149. |
| 142 | Moved value-home, move-bundle, ABI/result-lane/current-block-entry-publication lookup declarations toward value-location ownership. |
| 143 | Moved stack-layout id helpers to stack-layout ownership. |
| 144 | Moved source-producer and same-block materialization APIs to the narrow select-chain/source-producer owner. |
| 145 | Split reusable current-block join source facts from AArch64 instruction-routing convenience. |
| 146 | Moved call-argument source-producer materialization to call ownership, reusing same-block/source-producer facts. |
| 147 | Established `prealloc/comparison.hpp/.cpp` as the owner for fused-compare and materialized-condition producer facts. |
| 148 | Moved same-block load-local stored-value lookup declaration to addressing ownership while reusing source-producer, stack-layout, and memory-access facts. |
| 149 | Cleaned residual broad `prepared_lookups.hpp` includes and left only the edge-publication lookup declaration owner follow-up. |
| 150 | Moved `make_prepared_edge_publication_lookups` declaration to `publication_plans.hpp`, leaving remaining prepared lookup includes justified by true facade/aggregate ownership. |

## BIR-Normalization Candidates

Step 2 classified candidate groups from the Step 1 inventory. "Candidate"
means the named relationship is target-neutral enough for a future BIR-owned
schema/query surface; fields that encode homes, offsets, registers, ABI
placement, target addressing, scratch, or final emission remain Step 3 reject
material.

| Candidate group | Current owner | Current consumers | Proposed BIR owner surface | Semantic rationale | Dependencies / risks |
| --- | --- | --- | --- | --- | --- |
| Same-block producer/materialization | `src/backend/prealloc/select_chain_lookups.hpp/.cpp` owns `PreparedSameBlockScalarProducer`, `PreparedSameBlockValueMaterializationQuery`, `find_prepared_same_block_scalar_producer`, and `evaluate_prepared_same_block_integer_constant`; it reuses `PreparedEdgePublicationSourceProducer` as the producer shape. | AArch64 value/producers paths call these queries from `dispatch_producers.cpp`, `dispatch_value_materialization.cpp`, `fp_value_materialization.cpp`, `select_materialization.cpp`, and `alu.cpp`; call and comparison paths also reuse the same producer relationship. | BIR-owned per-function relationship index over `{block_label, before_instruction_index, value_name/value, type}` to a same-block producer node (`LoadLocal`, `LoadGlobal`, `Cast`, `Binary`, `SelectMaterialization`, or immediate constant) plus materialization availability. Likely lives next to `bir::Function`/`bir::Block` relationship metadata or a BIR analysis cache keyed by stable BIR ids. | The fact answers "which prior BIR instruction or immediate produces this value before this point in the same block?" It depends on BIR value/name/type and instruction order, not target registers, stack layout, or final instruction spelling. | Foundation for select-chain, call argument materialization, memory stored-value recovery, and comparison producer rows. Risk: current producer enum is stored under publication ownership, so a BIR surface should define a target-neutral producer-kind vocabulary instead of importing edge-publication naming. |
| Select-chain dependency/materialization | `src/backend/prealloc/select_chain_lookups.hpp/.cpp` plus `PreparedDirectGlobalSelectChainDependency` in `calls.hpp` own `PreparedSelectChainDependencyQuery`, `PreparedScalarSelectChainMaterialization`, `find_prepared_select_chain_source_producer`, `find_prepared_direct_global_select_chain_dependency`, and `find_prepared_scalar_select_chain_materialization`. | AArch64 `dispatch_producers.cpp` and `calls.cpp` use direct-global select-chain dependency to decide materialization/publication routing; store-source publication planning also queries select-chain source producers. | BIR-owned select-chain analysis keyed by `{block_label, root value, before_instruction_index}` that records root producer value/name, whether the root is a BIR `SelectInst`, root instruction index, direct `LoadGlobalInst` dependency, and scalar materialization eligibility. | The relationship describes BIR dataflow through select nodes and direct global loads. It is independent of where the selected value is allocated or how AArch64 emits the final move/branch. | Depends on same-block producer identity and a stable representation of direct global-load dependency. Risk: do not include target materialization cost, hazard, or register availability decisions in BIR. |
| Current-block entry publication consumption | `src/backend/prealloc/select_chain_lookups.hpp/.cpp` owns `PreparedCurrentBlockPublicationConsumption`; `value_locations.hpp` owns `find_prepared_current_block_entry_publication` and current block entry publication lookup helpers. | AArch64 `dispatch_publication.cpp` looks up current-block entry publication; `calls.cpp` uses `find_prepared_current_block_publication_consumption` for call-boundary publication reuse; scalar publication plans carry `current_block_entry_publication` availability. | BIR-owned block-entry/current-block publication relationship keyed by `{block_label, value_name, before_instruction_index}` that maps a consumed value to its entry publication source producer, produced BIR value, producer instruction, and source-producer kind. | The candidate names that a value available at block entry or earlier in the current block is the semantic source for a later use. That is a CFG/value relationship over BIR block entry state, not an AArch64 register-view or operand spelling rule. | Depends on producer identity and a BIR schema for block-entry publication. Risk: the existing current-block publication consumers are mixed with register-view availability; BIR must only own source/value/producer identity and leave hook emission to prealloc/MIR. |
| Edge-publication and parallel-copy source identity | `src/backend/prealloc/publication_plans.hpp/.cpp` owns `PreparedEdgePublicationSourceProducer`, `PreparedEdgePublication`, `PreparedEdgeCopySourceFacts`, `PreparedCurrentBlockJoinParallelCopySourceFact(s)`, `make_prepared_edge_publication_lookups`, `find_indexed_prepared_edge_publication_source_producer`, `prepare_edge_copy_source_facts`, `prepare_block_entry_parallel_copy_edge_source_facts`, and `prepare_current_block_join_parallel_copy_source_facts`. | AArch64 `dispatch_edge_copies.cpp`, `dispatch_producers.cpp`, `calls.cpp`, and `memory.cpp` consume edge publication/source-producer and edge-copy source facts for edge copy emission, call-boundary reuse, and memory publication decisions. | BIR-owned CFG edge publication surface keyed by `{predecessor_label, successor_label, destination_value_id/name}` that records source BIR value/name/kind, source producer block/instruction, optional source memory-access identity, and current-block join incoming-expression/source identity. | The accepted subset states "on this CFG edge, destination value D receives semantic source S, produced by BIR node P" and "this current-block join copy source corresponds to incoming expression/source value." Those are SSA/CFG value-transfer facts before target copy scheduling. | Depends on producer identity, memory access identity for load-local source cases, and block/function edge ids. Risks: `PreparedEdgePublication` also stores homes, register-sharing, move phases, parallel-copy execution sites, carriers, bundles, and moves; those are target/prealloc policy and must not move with the semantic edge identity. |
| Call-boundary source/dependency identity | `src/backend/prealloc/calls.hpp/.cpp` owns `PreparedCallArgumentSourceSelection`, `PreparedCallArgumentSourceProducerMaterialization`, `PreparedCallArgumentDirectGlobalSelectChainDependency`, `PreparedCallArgumentPublicationSourceRouting`, `PreparedCallArgumentPlan`, `find_prepared_call_argument_source_producer_materialization`, `find_prepared_call_argument_direct_global_select_chain_dependency`, and `find_prepared_call_argument_publication_source_routing`. | AArch64 `calls.cpp` heavily consumes call argument source routing, direct-global select-chain dependency, source-producer materialization, current-block publication consumption, and same-block load-local stored-value source; `select_materialization.cpp` also consumes call argument publication routing. | BIR-owned call-use source relationship attached to `bir::CallInst` arguments/results: source value/base value/name, source pointer delta when it is semantic pointer arithmetic, materializable same-block producer, direct global select-chain dependency, and publication-source route identity. | The accepted subset describes which BIR value or producer is the semantic source for a call-boundary use and whether that source depends on BIR select/global-load structure. It is separate from ABI binding and final call lowering. | Depends on same-block producer, select-chain dependency, current-block publication, and memory/access identity. Risks: `PreparedCallArgumentPlan` and `PreparedCallArgumentSourceSelection` include destination register names, occupied registers, stack offsets/sizes, aggregate transport lanes, scratch requirements, preservation routes, and byval lane details; those must stay prealloc/ABI/AArch64-owned. |
| Memory/access source identity | `src/backend/prealloc/addressing.hpp/.cpp`, `publication_plans.hpp/.cpp`, and stack-layout helpers own `PreparedAddress`, `PreparedMemoryAccess`, `PreparedSameBlockGlobalLoadAccess`, `PreparedSameBlockLoadLocalStoredValueSource`, `PreparedStoreSourcePublicationPlan`, `find_prepared_same_block_global_load_access`, `find_prepared_same_block_load_local_stored_value_source`, and memory-access lookup helpers. | AArch64 `memory.cpp`, `memory_store_retargeting.cpp`, `dispatch_value_materialization.cpp`, `fp_value_materialization.cpp`, `alu.cpp`, `dispatch_edge_copies.cpp`, and `calls.cpp` consume memory access, store-source, same-block global-load, and load-local stored-value relationships. | BIR-owned memory-access identity surface attached to `LoadLocalInst`, `LoadGlobalInst`, `StoreLocalInst`, `StoreGlobalInst`, and address materialization nodes: result/stored value names, address-space/volatile, semantic base kind (`FrameSlot`, `GlobalSymbol`, `PointerValue`, `StringConstant`), pointer/global/local source identity, same-block global-load access, and load-local stored-value source link. | The accepted subset answers "which BIR memory operation/access does this value represent?" and "which earlier store/source value feeds this load or publication?" Those are typed memory/dataflow facts, not final AArch64 addressing forms. | Depends on producer identity and stack-slot/local/global naming. Risks: `PreparedAddress` and `PreparedAddressMaterialization` also store frame slot ids, byte offsets, size/align, `can_use_base_plus_offset`, TLS model/register/relocations, and target global materialization policy; Step 3 should reject those from BIR normalization except where BIR already owns target-neutral address-space/global policy. |
| Comparison/materialized-condition producer identity | `src/backend/prealloc/comparison.hpp/.cpp` owns `PreparedFusedCompareOperandProducer`, `PreparedFusedCompareOperandProducerFacts`, `PreparedMaterializedConditionProducer`, `find_prepared_fused_compare_operand_producer`, `find_prepared_fused_compare_operand_producer_facts`, and `find_prepared_materialized_condition_producer`. | AArch64 `comparison.cpp` consumes fused compare operand producer facts for branch/compare emission and materialized-condition producer lookup for condition-value materialization/reuse. | BIR-owned comparison relationship surface keyed by branch condition or condition value, recording lhs/rhs producer nodes or integer constants, comparison-producing `BinaryInst`, condition value name, and producer instruction index. | The fact identifies that branch operands or a materialized condition value come from prior BIR producers/constants. It describes compare/condition dataflow before target compare/branch instruction selection. | Depends on same-block producer identity and integer constant evaluation. Risk: BIR should own operand/condition producer identity only; AArch64 fused-compare legality, condition-code choice, and branch emission strategy remain target codegen policy. |

## Facts Rejected From BIR Normalization

Step 3 classifies facts that must not move into BIR-owned normalization
schemas. "Rejected" does not mean unused; it means the fact is target, layout,
ABI, or final-emission policy and any BIR candidate must depend on a separate
target-neutral relationship instead.

| Fact group | Current owner and examples | Reject reason | Required non-BIR owner | Candidate dependency cross-check |
| --- | --- | --- | --- | --- |
| ABI register/stack placement | `src/backend/prealloc/calls.hpp` owns `PreparedCallArgumentPlan::{destination_register_name,destination_stack_offset_bytes,destination_stack_size_bytes,destination_register_bank,destination_register_placement}`, `PreparedCallResultPlan::{source_register_name,destination_register_name,destination_slot_id,destination_stack_offset_bytes}`, `PreparedCallPreservedValue`, `PreparedClobberedRegister`, `PreparedMemoryReturnPlan`, `PreparedOutgoingStackArgumentArea`, and `PreparedCallPlan::{variadic_fpr_arg_register_count,outgoing_stack_argument_area,result,preserved_values,clobbered_registers}`. | These fields encode concrete calling-convention lowering, outgoing argument area sizing, result return location, preservation/clobber policy, and physical ABI resources. BIR may know that a call has arguments/results, but not the target ABI placement chosen for them. | Prealloc call plans, target ABI lowering, call-boundary effect planning, and AArch64 call codegen. | The Step 2 call-boundary candidate keeps only source value/base/dependency/publication identity. It must not require destination register names, stack slots, outgoing stack size, variadic FPR count, preservation route, clobbers, or return-lane placement. |
| Stack offsets, frame slots, saved-register placement | `src/backend/prealloc/frame.hpp` owns `PreparedFrameSlot::{slot_id,object_id,offset_bytes,size_bytes,align_bytes,fixed_location}`, `PreparedStackLayout::{frame_slots,frame_size_bytes,frame_alignment_bytes}`, and `PreparedSpillSlotPlacement`; mixed consumers include `PreparedValueHome::{slot_id,offset_bytes,size_bytes,align_bytes}`, `PreparedStoreSourcePublicationPlan::{destination_frame_slot_id,destination_stack_offset_bytes,destination_stack_size_bytes,destination_stack_align_bytes}`, and `PreparedAggregateStackSourceAuthority::{source_slot_id,source_stack_offset_bytes,source_stack_size_bytes,source_stack_align_bytes}`. | Frame-slot ids, byte offsets, stack sizes, alignments, spill slots, fixed locations, and saved-register slots are produced after layout/allocation decisions. Moving them into BIR would make BIR depend on concrete frame layout and prologue/epilogue policy. | Stack layout, frame plan, spill/reload planning, value-location preparation, and AArch64 prologue/epilogue lowering. | The Step 2 memory/access and publication candidates may name semantic local/global/pointer source identity. They must not depend on frame slot ids or final byte offsets; if a future BIR schema needs a local object identity, it should use BIR slot/name identity rather than prepared frame layout. |
| Register spelling, banks, views, occupied registers | `src/backend/prealloc/frame.hpp` owns `PreparedRegisterBank`, `PreparedRegisterSlotPool`, and `PreparedRegisterPlacement`; `src/backend/prealloc/value_locations.hpp` stores `PreparedValueHome::{register_name,target_register_identity}`; call/publication facts store register names, banks, contiguous widths, occupied register names, and placements; AArch64 converts them in `src/backend/mir/aarch64/codegen/operands.cpp` and materialization/dispatch code. | Physical register names, bank/pool assignment, register view conversion, contiguous register width, occupied alias sets, and operand spelling are target allocation/encoding facts. BIR values are typed semantic values and should not embed AArch64 register vocabulary or views. | Regalloc, target register profile/ABI conversion, AArch64 operand construction, and instruction emission. | Same-block, comparison, edge, and call candidates may identify which BIR producer feeds a use. They must not require register names, bank equality, view selection, alias/occupied sets, or current emitted-register availability. |
| Scratch policy and aggregate transport lanes | `src/backend/prealloc/calls.hpp` owns `PreparedAggregateTransportPlan::{chunks,lanes,scratch_requirements}`, `PreparedAggregateTransportLane::{destination_register_name,destination_register_bank,destination_occupied_register_names,destination_register_placement,whole_register}`, and `PreparedAggregateTransportScratchRequirement`; publication/aggregate rows include `PreparedAggregateStackSourceAuthority::{has_destination_lane_mapping,has_lane_widths_and_offsets,partial_copy_allowed,has_abi_layout_reference,has_scratch_ownership}`. | Chunk/lane decomposition, padding/fallback widths, scratch classes, overlap permissions, partial-copy policy, ABI layout references, and lane destination registers are target transport algorithms for aggregate copies or byval lowering. They are not semantic BIR dataflow. | Prealloc aggregate/call planning, ABI aggregate classification, scratch resource policy, and AArch64 aggregate/call codegen. | The Step 2 call and publication candidates may preserve source/base value identity and semantic pointer deltas. They must not require byval lane extents, lane register placement, scratch classes, copy chunk widths, or aggregate stack-copy authority. |
| Target addressing modes, TLS relocations, base-plus-offset legality | `src/backend/prealloc/addressing.hpp` owns `PreparedAddress::{frame_slot_id,byte_offset,size_bytes,align_bytes,can_use_base_plus_offset,global_address_materialization_policy}`, `PreparedAddressMaterialization::{kind,frame_slot_id,address_materialization_policy,is_thread_local,has_tls_address_space,tls_model,tls_thread_pointer_register,tls_high_relocation,tls_low_relocation}`, and `PreparedMemoryAccess::address`; AArch64 records use `AddressMaterializationKind::{DirectPageLow12,GotPageLow12,TlsRelative,...}` and matching record errors. | Addressing-mode selection, base-plus-offset legality, low/high relocation kind, TLS model/thread-pointer register, GOT/direct page-low forms, concrete size/align, and frame slot offsets are target encoding and relocation decisions. BIR may own address-space and semantic base/source identity, but not whether AArch64 can encode the final address form. | Addressing prealloc, target relocation/materialization policy, stack layout, memory operand construction, and AArch64 memory/address-materialization codegen. | The Step 2 memory/access candidate keeps semantic access identity: result/stored values, address space, volatile, global/local/pointer/string base identity, and same-block load/store source links. It must not require `can_use_base_plus_offset`, TLS relocation spelling, page-low/GOT/direct choice, or prepared frame offsets. |
| Parallel-copy execution and publication routing policy | `src/backend/prealloc/control_flow.hpp` owns `PreparedParallelCopyBundle::{execution_site,execution_block_label,steps,has_cycle}` and `PreparedParallelCopyExecutionSite`; `src/backend/prealloc/publication_plans.hpp` embeds `PreparedEdgePublication::{matching_move_coalesced_by_assigned_storage,matching_move_redundant_by_assigned_storage,phase,carrier_kind,parallel_copy_step_index,parallel_copy_step_kind,parallel_copy_step_uses_cycle_temp_source,parallel_copy_bundle_has_cycle,parallel_copy_execution_site,parallel_copy_execution_block_label,move_bundle,move}` and `PreparedCurrentBlockJoinParallelCopySourceFact::{destination_register_name,source_shares_destination_register,source_home_is_stack,immediate_source}`. | Parallel-copy schedule, cycle breaking, move coalescing/redundancy, execution site, carrier choice, phase, and register-sharing checks are out-of-SSA/prealloc routing decisions. BIR may own CFG edge value transfer identity, but not how a target executes the copies. | Control-flow preparation, out-of-SSA move resolution, value-location/move-bundle ownership, and AArch64 edge-copy/dispatch codegen. | The Step 2 edge-publication candidate keeps predecessor/successor/destination/source/producer identity and optional memory-access identity. It must not depend on copy step order, cycle temporaries, execution block placement, coalescing flags, destination register names, or storage-sharing checks. |
| Publication hook, storage encoding, and typed stack-source emission payloads | `src/backend/prealloc/publication_plans.hpp` owns `PreparedScalarPublicationPlan::{hook_kind,destination_home,destination_home_kind,storage_encoding,slot_id,stack_offset_bytes,size_bytes,align_bytes,immediate_*}`, `PreparedTypedStackSourcePublication::{source_slot_id,source_stack_offset_bytes,source_stack_size_bytes,source_stack_align_bytes,destination_register_bank,destination_register_placement,extension_policy}`, and `PreparedStoreSourcePublicationPlan` storage/home fields. | Hook kind, storage encoding, destination home, extension policy, stack-source publication width, and typed stack-source register placement are prepared emission contracts. They choose how an already-identified value is published or materialized for the target. | Publication planning, value-location ownership, typed stack-source emission, and AArch64 publication/materialization codegen. | Current-block publication and store-source candidates may keep semantic source/destination value and memory-access identity. They must not carry hook kinds, stack-source extension policy, concrete destination homes, immediate publication payloads, or storage encodings into BIR. |
| Final instruction routing/order/hazard policy | `src/backend/mir/aarch64/codegen/instruction.hpp` owns final record/error families such as `PreparedBranchRecordError`, `PreparedScalarAluRecordError`, `PreparedScalarCastRecordError`, `PreparedMemoryOperandRecordError`, `PreparedI128TransportRecordError`, `PreparedF128TransportRecordError`, `PreparedAtomicOperationRecordError`, `PreparedAddressMaterializationRecordError`, and final records such as `SpillReloadInstructionRecord`; AArch64 dispatch/memory/producers code records emitted scalar registers, memory retargeting, and instruction payload rewrites. | Instruction-record formation, branch/compare fusion legality, operand storage support, record errors, spill/reload pseudo selection, memory retargeting, hazard avoidance, and final instruction order are MIR/codegen behavior. BIR normalization should provide semantic relationships earlier, not prescribe target instruction sequencing. | AArch64 dispatch, instruction record builders, memory retargeting, scalar/FP/atomic/runtime-helper lowering, and MIR emission. | The Step 2 comparison and same-block candidates can name producer/condition identity. They must not include fused-branch legality, condition-code choice, final record errors, spill/reload scratch, memory-record rewrites, or emitted-register state. |
| Runtime helper, special-carrier, variadic, and dynamic-stack resource policy | `src/backend/prealloc/runtime_helpers.hpp`, `src/backend/prealloc/special_carriers.hpp`, `src/backend/prealloc/variadic.hpp`, and `src/backend/prealloc/dynamic_stack.hpp` own helper families, carrier kinds/lanes, marshal directions, ABI transitions, va-list helper/source classes, and dynamic-stack helper plans; AArch64 consumers lower them through helper/carrying resources and fixed registers. | These facts describe target/helper protocols and multi-register or memory carriers for operations whose semantic operation may be in BIR but whose transport/resources are ABI and target-specific. | Runtime-helper planning, special-carrier planning, variadic lowering, dynamic-stack lowering, ABI policy, and AArch64 helper codegen. | Step 2 candidates should not create BIR dependencies on helper result ownership, carrier lanes, marshal direction, va-list storage class, or dynamic-stack register homes. Future BIR work may reference the original semantic operation only. |
| Mixed publication/call/memory payloads from candidate rows | Mixed surfaces in `PreparedEdgePublication`, `PreparedStoreSourcePublicationPlan`, `PreparedCallArgumentSourceSelection`, `PreparedCallArgumentPlan`, `PreparedCallResultLatePublicationFact`, `PreparedAddress`, and `PreparedMemoryAccess` combine acceptable source/value/access identity with rejected homes, offsets, registers, stack layout, TLS/addressing, and routing fields. | These structs are intentionally mixed aggregation surfaces. Copying them wholesale into BIR would smuggle target-local facts into a BIR schema even when one or two fields are semantic. | Split ownership: BIR or a BIR analysis cache for semantic relationship identity; prealloc/ABI/stack/regalloc/MIR for placement, layout, transport, routing, and final emission. | Every Step 2 candidate row must be implemented by selecting only the semantic subset named in that row. Any proposed follow-up that imports a full `Prepared*Plan`, `Prepared*Publication`, `PreparedAddress`, or `PreparedValueHome` shape into BIR is a reviewer reject signal. |

## Dependency Order

Step 4 orders accepted candidate rows into route groups that can become Phase B
implementation ideas without importing Step 3 reject payloads. Each route
should first add a BIR-owned schema/query surface, then bridge the matching
prepared query to that surface, and only then switch individual MIR consumers.

| Order | Route group | Must exist before this route | BIR-owned schema or annotation decision | Earliest consumer switch point | Step 3 reject boundary |
| --- | --- | --- | --- | --- | --- |
| 1 | Producer/source identity foundation | Raw `bir::Function`, `bir::Block`, instruction order, value/name/type ids, and existing integer-constant evaluation rules. | Add a target-neutral producer-kind vocabulary and per-function or analysis-cache relationship index keyed by `{block_label, before_instruction_index, value_name/value, type}`. The schema may name same-block producer nodes, immediate constants, source value/name, producer instruction index, and materialization availability. Do not reuse publication-owned enum names as the BIR authority. | After the BIR query can answer the same semantic subset as `find_prepared_same_block_scalar_producer` and `evaluate_prepared_same_block_integer_constant`, switch low-risk value/materialization consumers that only need producer/value identity. | Excludes register spelling, register availability, operand views, final instruction order, spill/reload policy, storage encoding, and emitted-register state. |
| 2 | Select-chain and direct-global dependency identity | Route 1 producer/source identity plus BIR `SelectInst`, `LoadGlobalInst`, root value, and instruction index data. | Add select-chain analysis keyed by `{block_label, root_value, before_instruction_index}`. Store root producer value/name, whether the root is a BIR select, root instruction index, direct global-load dependency, and scalar materialization eligibility as semantic dataflow annotations. | After BIR queries match `find_prepared_select_chain_source_producer`, `find_prepared_direct_global_select_chain_dependency`, and `find_prepared_scalar_select_chain_materialization`, switch select-materialization and direct-global dependency consumers before call-specific consumers. | Excludes target materialization cost, hazard decisions, register availability, publication routing policy, and final AArch64 move/branch choices. |
| 3 | Memory/access semantic identity | Route 1 producer/source identity and BIR memory nodes (`LoadLocal`, `LoadGlobal`, `StoreLocal`, `StoreGlobal`, address materialization values), with target-neutral local/global/pointer/string identity. Route 2 is optional only for memory rows that cite direct global select-chain structure. | Add a BIR memory-access identity surface attached to memory/address nodes. Store result/stored value names, address space, volatile flag, semantic base kind, pointer/global/local/string source identity, same-block global-load access, and load-local stored-value source links. Use BIR slot/name identity for locals, not prepared frame slot ids. | After BIR memory queries match same-block global-load and load-local stored-value relationships, switch memory retargeting and store-source consumers that only need access/source identity. Keep target addressing owners in prealloc/AArch64 until later MIR lowering. | Excludes frame slot ids, byte offsets, size/align layout, `can_use_base_plus_offset`, TLS register/relocation spelling, GOT/direct/page-low policy, and AArch64 memory operand legality. |
| 4 | Block-entry and current-block publication identity | Route 1 producer/source identity. Route 3 if publication rows need source memory-access identity. Stable BIR block labels, predecessor/successor ids, entry values, and before-instruction positions. | Add block-entry/current-block publication relationship keyed by `{block_label, value_name, before_instruction_index}`. Store source producer identity, produced BIR value/name, producer instruction/index, and source-producer kind. Model this as a BIR value-availability/publication relationship, not as a hook or home. | After the BIR query matches `PreparedCurrentBlockPublicationConsumption` for source/value/producer identity, switch current-block publication consumers and scalar publication planning reads that only ask "which semantic source is available?". | Excludes hook kind, destination home, storage encoding, stack-source extension policy, register view conversion, immediate publication payloads, and emitted storage availability. |
| 5 | CFG edge publication and join-source identity | Routes 1 and 4, plus Route 3 for edge/load-local source cases. Stable BIR predecessor/successor labels and destination value/name ids. | Add CFG edge publication surface keyed by `{predecessor_label, successor_label, destination_value_id/name}`. Store source BIR value/name/kind, source producer block/instruction, optional source memory-access identity, and current-block join incoming-expression/source identity. | After BIR edge queries match semantic parts of `PreparedEdgePublication`, `PreparedEdgeCopySourceFacts`, and current-block join source facts, switch edge-copy source discovery and publication-source producer lookups. Keep parallel-copy execution planning downstream. | Excludes move bundle/step order, cycle temporary routing, execution site/block placement, phase/carrier policy, coalescing/redundancy flags, destination register names, storage-sharing checks, and prepared move records. |
| 6 | Call-boundary semantic source facts | Routes 1, 2, 3, and 4. Route 5 only for call paths that explicitly reuse edge/publication-source identity. Existing `bir::CallInst` argument/result ids and call metadata. | Add call-use source relationships attached to `bir::CallInst` arguments/results. Store source value/base value/name, semantic pointer delta when target-neutral, materializable same-block producer, direct global select-chain dependency, memory/access source link, and publication-source route identity. | After BIR call queries match `PreparedCallArgumentSourceProducerMaterialization`, `PreparedCallArgumentDirectGlobalSelectChainDependency`, and `PreparedCallArgumentPublicationSourceRouting` for semantic fields, switch call materialization/routing reads one argument/result class at a time. | Excludes ABI register/stack placement, outgoing stack area sizing, variadic FPR count, preservation/clobber sets, byval aggregate lanes, scratch requirements, destination homes, and helper/carrier protocols. |
| 7 | Comparison and materialized-condition producer identity | Route 1 producer/source identity and integer-constant evaluation; Route 2 only when comparison operands depend on select/global-load roots. | Add comparison relationship keyed by branch condition or condition value. Store lhs/rhs producer nodes or integer constants, comparison-producing `BinaryInst`, condition value name, and producer instruction index. | After BIR comparison queries match `find_prepared_fused_compare_operand_producer*` and `find_prepared_materialized_condition_producer`, switch comparison and branch consumers that need operand/condition provenance. | Excludes fused-compare legality, condition-code selection, branch emission strategy, final instruction records/errors, hazard handling, and emitted-register state. |

Switch timing rule: no MIR consumer should switch until the corresponding BIR
query can answer the same semantic subset as the prepared query and the old
prepared surface remains available as a comparison oracle for before/after
proof. Route groups may be implemented as separate ideas, but consumer switches
inside each group should be small and query-equivalence driven.

Schema rule: BIR schemas may record stable BIR ids, value/name/type identity,
block/edge ids, producer instruction indexes, semantic base/source kinds,
address-space/volatile bits, direct global-load dependency, and integer
constants. They must not record homes, stack offsets, frame slots, physical
registers, ABI placements, target addressing legality, relocation spelling,
parallel-copy execution policy, storage hooks, scratch resources, or final
instruction records.

## Follow-Up Idea Payloads

Step 5 converts the accepted route groups into bounded follow-up idea payloads.
The filenames below are proposed `ideas/open/` prefixes; a plan owner can assign
the final sequence number when activating or filing the ideas. Each payload is
intended to add one BIR-owned semantic relationship surface first, bridge the
matching prepared query as an oracle, and defer MIR consumer switches until the
query-equivalence proof exists.

| Proposed idea filename or prefix | Route | Scope | Out of scope | Acceptance criteria | Proof route | Reviewer reject signals |
| --- | --- | --- | --- | --- | --- | --- |
| `NNN_bir_producer_source_identity_foundation.md` | Producer/source identity foundation | Add the target-neutral BIR producer-kind vocabulary and per-function/query-cache relationship index for same-block producer, immediate constant, source value/name, producer instruction index, type, and materialization availability. Bridge `find_prepared_same_block_scalar_producer` and `evaluate_prepared_same_block_integer_constant` to compare BIR answers against existing prepared answers before switching low-risk consumers. | No register names, homes, operand views, emitted-register availability, spill/reload behavior, storage encoding, final instruction order, or publication-owned enum authority in the BIR schema. | BIR query returns the same semantic subset as the prepared same-block and constant queries on representative scalar, cast, binary, load, select, and constant cases; old prepared query remains available as oracle; at most a narrow low-risk consumer switches after equivalence is demonstrated. | Capture matching `test_before.log`/`test_after.log` for the smallest backend/codegen subset that exercises same-block scalar materialization, plus targeted dumps or assertions showing BIR/prepared query equivalence for each supported producer kind. | Imports `PreparedValueHome`, register-view state, publication enum naming as the BIR authority, or switches broad MIR emission before query equivalence is proven. |
| `NNN_bir_select_chain_direct_global_identity.md` | Select-chain and direct-global dependency identity | Add BIR select-chain analysis keyed by block, root value, and before-instruction index. Record root producer value/name, root-is-select, root instruction index, direct `LoadGlobalInst` dependency, and scalar materialization eligibility. Bridge `find_prepared_select_chain_source_producer`, `find_prepared_direct_global_select_chain_dependency`, and `find_prepared_scalar_select_chain_materialization`. | No target materialization cost, hazard decisions, register availability, publication routing policy, call ABI behavior, or final AArch64 move/branch choices. | BIR select-chain queries match existing prepared answers for direct global roots, select roots, scalar materialization, and negative cases; select-materialization/direct-global consumers switch only after the producer foundation is available and equivalence is logged. | Matching before/after backend subset around select materialization and direct-global dependency consumers, with query-equivalence checks covering select root, non-select root, direct global load, and no-dependency paths. | Treats select-chain as a call-specific shortcut, adds AArch64 routing policy to BIR, or proves only one named select/global case while nearby select-chain shapes remain unexamined. |
| `NNN_bir_memory_access_identity.md` | Memory/access semantic identity | Add BIR memory-access identity attached to load/store/address nodes for result/stored values, address space, volatile flag, semantic base kind, pointer/global/local/string source identity, same-block global-load access, and load-local stored-value source links. Use BIR slot/name identity rather than prepared frame slot ids. Bridge same-block global-load and load-local stored-value prepared queries. | No frame slot ids, concrete offsets, size/align layout, base-plus-offset legality, TLS register/relocation spelling, GOT/direct/page-low policy, target addressing-mode choice, or AArch64 memory operand legality. | BIR memory query matches prepared memory/access identity for local, global, pointer, string, volatile, and same-block load/store source cases; store-source and memory-retargeting consumers switch only for semantic source identity and leave target addressing downstream. | Matching before/after backend memory/codegen subset plus explicit equivalence checks for same-block global-load access and load-local stored-value source recovery; escalate to broader backend if address-materialization code is touched. | Copies `PreparedAddress` wholesale, embeds frame layout or relocation policy in BIR, or claims progress by changing memory expectations without preserving target addressing behavior. |
| `NNN_bir_block_entry_publication_identity.md` | Block-entry and current-block publication identity | Add block-entry/current-block BIR value-availability relationship keyed by block, value name, and before-instruction index. Store source producer identity, produced BIR value/name, producer instruction/index, and source-producer kind. Bridge `PreparedCurrentBlockPublicationConsumption` and current-block entry publication reads for semantic source identity. | No hook kind, destination home, storage encoding, stack-source extension policy, register view conversion, immediate publication payloads, emitted storage availability, or scalar publication emission policy. | BIR current-block publication query matches prepared source/value/producer identity for entry and same-block availability cases; scalar publication planning reads switch only where they ask which semantic source is available. | Matching before/after subset covering dispatch publication/current-block publication consumers, with query-equivalence assertions for available and unavailable publication consumption cases. | Turns BIR publication into a storage hook or home model, includes register view conversion, or changes publication ordering before the semantic query is proven equivalent. |
| `NNN_bir_cfg_edge_publication_identity.md` | CFG edge publication and join-source identity | Add CFG edge publication relationship keyed by predecessor label, successor label, and destination value/name. Store source BIR value/name/kind, source producer block/instruction, optional source memory-access identity, and current-block join incoming-expression/source identity. Bridge semantic parts of `PreparedEdgePublication`, `PreparedEdgeCopySourceFacts`, and current-block join source facts. | No move bundle/step order, cycle temporary routing, execution site/block placement, phase/carrier policy, coalescing/redundancy flags, destination register names, storage-sharing checks, or prepared move records. | BIR edge query matches prepared edge-copy/publication source identity across predecessor/successor and join-source cases; edge-copy source discovery switches only after current-block publication identity and required memory identity are available. | Matching before/after subset covering edge copies and dispatch publication, with equivalence checks for normal edge, join-source, optional memory-source, and no-source cases; escalate if parallel-copy planning files change. | Imports parallel-copy scheduling into BIR, encodes register-sharing decisions, or treats edge publication identity as permission to rewrite move execution policy. |
| `NNN_bir_call_boundary_source_facts.md` | Call-boundary semantic source facts | Add call-use source relationships attached to `bir::CallInst` arguments/results. Store source value/base value/name, target-neutral pointer delta, materializable same-block producer, direct global select-chain dependency, memory/access source link, and publication-source route identity. Bridge call argument source-producer materialization, direct-global dependency, and publication-source routing semantic fields. | No ABI register/stack placement, outgoing stack sizing, variadic FPR count, preservation/clobber sets, byval aggregate lanes, scratch requirements, destination homes, helper/carrier protocols, or aggregate transport lane layout. | BIR call queries match prepared semantic call-source answers one argument/result class at a time; consumer switches are limited to materialization/routing reads that no longer need ABI placement; old call plans remain the comparison oracle during migration. | Matching before/after backend call/codegen subset with targeted equivalence for same-block producer, select-chain direct global, memory-source, publication-source, and negative call-argument cases; escalate to broader backend if ABI or aggregate files change. | Copies `PreparedCallArgumentPlan` or aggregate transport shapes into BIR, changes ABI placement while claiming semantic migration, or validates only one named call shape. |
| `NNN_bir_comparison_condition_producer_identity.md` | Comparison and materialized-condition producer identity | Add comparison relationship keyed by branch condition or condition value. Store lhs/rhs producer nodes or integer constants, comparison-producing `BinaryInst`, condition value name, and producer instruction index. Bridge fused-compare operand producer and materialized-condition producer queries. | No fused-compare legality, condition-code selection, branch emission strategy, final instruction records/errors, hazard handling, emitted-register state, or AArch64 compare/branch instruction selection. | BIR comparison queries match prepared fused-operand and materialized-condition producer answers for constants, same-block producers, binary compare values, and unavailable cases; comparison/branch consumers switch only for provenance reads after producer foundation exists. | Matching before/after backend comparison/branch subset with equivalence checks for fused operands and materialized conditions, including integer constants and non-fusable negative paths. | Moves branch-fusion legality into BIR, changes condition-code emission, or proves a single compare testcase without nearby constant/materialized-condition coverage. |

Candidate groups that should not create standalone follow-up ideas: value homes,
storage, regalloc, frame layout, dynamic stack, special carriers, runtime
helpers, variadic plans, final instruction records, ABI placement, target
addressing modes, scratch policy, aggregate transport lanes, storage hooks, and
whole mixed `Prepared*Plan`/`Prepared*Publication`/`PreparedAddress` shapes.
They may appear only as out-of-scope guardrails or downstream consumers of the
semantic BIR relationships above.

## Proof-Route Recommendations

These proof routes are starting points for the future implementation ideas.
Every implementation idea should use matching before/after commands and keep
`test_before.log`/`test_after.log` as the canonical regression artifacts.

| Route | Suggested proof | Escalation trigger |
| --- | --- | --- |
| Producer/source identity foundation | Run the smallest backend/codegen subset that exercises same-block scalar materialization and integer constants, then add direct query-equivalence checks or dumps comparing BIR answers with `find_prepared_same_block_scalar_producer` and `evaluate_prepared_same_block_integer_constant` for each supported producer kind. | Any consumer switch beyond identity reads, any BIR schema field that names homes/registers/storage, or any mismatch between BIR and prepared query answers. |
| Select-chain and direct-global dependency identity | Run targeted select-materialization/direct-global backend coverage and equivalence checks against `find_prepared_select_chain_source_producer`, `find_prepared_direct_global_select_chain_dependency`, and `find_prepared_scalar_select_chain_materialization`. Include positive and negative direct-global/select-root cases. | Any call-specific shortcut, target materialization policy change, or dependency on register availability/publication routing. |
| Memory/access semantic identity | Run targeted backend memory/codegen coverage and compare BIR memory/access answers with prepared same-block global-load and load-local stored-value source queries. Include local, global, pointer/string base, volatile, and no-source cases when available. | Any edit to frame layout, address-materialization policy, TLS/global relocation behavior, base-plus-offset legality, or AArch64 memory operand formation. |
| Block-entry/current-block publication identity | Run dispatch-publication/current-block publication coverage and equivalence checks for available and unavailable publication consumption. Keep scalar publication planning as an oracle until BIR only owns source/value/producer identity. | Any change to hook selection, destination homes, register views, storage encoding, publication ordering, or emitted storage availability. |
| CFG edge publication and join-source identity | Run backend edge-copy/dispatch-publication coverage and compare BIR edge source answers with prepared edge publication, edge-copy source fact, and join-source fact queries. Include normal edge, join-source, optional memory-source, and no-source paths. | Any edit to parallel-copy scheduling, cycle temporary routing, execution site placement, coalescing/redundancy flags, or destination register decisions. |
| Call-boundary semantic source facts | Run targeted backend call/codegen coverage and compare BIR call-source answers with prepared call argument materialization, direct-global dependency, and publication-source routing semantic fields for each switched argument/result class. | Any ABI placement, outgoing stack, aggregate lane, scratch, variadic, preservation/clobber, helper, or carrier behavior change. |
| Comparison/materialized-condition producer identity | Run backend comparison/branch coverage and compare BIR comparison answers with fused-compare operand and materialized-condition producer queries for constants, same-block producers, binary compare values, and unavailable cases. | Any change to fused-compare legality, condition-code selection, branch emission, final instruction record formation, or emitted-register state. |

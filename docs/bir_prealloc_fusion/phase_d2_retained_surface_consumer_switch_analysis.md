# Phase D2 Retained-Surface Consumer Switch Analysis

Status: Step 3 follow-up viability classification complete.

Source idea:
`ideas/open/203_phase_d2_retained_surface_consumer_switch_analysis.md`

## Scope

This Phase D2 artifact starts from the Phase C2 retained-surface decision set.
It is analysis-only. It does not migrate consumers, delete prepared APIs,
privatize caches, open draft 155, or claim `PreparedFunctionLookups` or
`PreparedBirModule` retirement readiness.

Step 1 records the raw evidence set that later steps will use to map current
consumers and classify one-surface follow-up viability. C2 classifications are
preserved unless later D2 steps record new surface-specific evidence.

## Step 1 Sources Read

| Source | Evidence captured for D2 |
| --- | --- |
| `ideas/open/203_phase_d2_retained_surface_consumer_switch_analysis.md` | D2 direction: start from C2 retained facts, classify one surface or one diagnostic/oracle row at a time, and reject broad cache/API migration, draft 155, aggregate retirement, and expectation weakening. |
| `plan.md` | Active runbook Step 1 completion rule: create a working evidence list with route, surface, C2 classification, retained boundary, and source reference. |
| `docs/bir_prealloc_fusion/phase_a2_residual_semantic_owner_audit.md` | Phase A2 accepted only narrow existing Route 1-8 semantic candidates and rejected broad prepared aggregates, target/prepared policy, pass-context, wrappers, diagnostics/oracles, and baseline/string guardrails as BIR route candidates. |
| `docs/bir_prealloc_fusion/phase_b2_selected_route_extension_schema_readiness.md` | Routes 3-7 have sufficient existing schema for selected semantic facts, but retained fallback/oracle and target/prepared policy surfaces remain explicit per route. |
| `docs/bir_prealloc_fusion/phase_c2_selected_adapter_cache_contraction_readiness.md` | No Route 3-7 adapter or diagnostic closure is contraction-ready; touched surfaces remain retained public fallback/oracle, retained target/prepared policy, diagnostic/oracle replacement prerequisite, transient pass-context, or blocked aggregate work. |
| `ideas/closed/201_phase_b2_selected_route_extension_schema_readiness_audit.md` | B2 closure rejected schema/index extensions, retained-policy cleanup, broad wrappers/printers/target-policy/API contraction, and opened only bounded Route 3-6 adapter plus Route 7 diagnostic/oracle follow-ups. |
| `ideas/closed/202_phase_c2_selected_adapter_cache_contraction_readiness_audit.md` | C2 closure instructs D2 to begin from one retained surface or diagnostic/oracle prerequisite, not broad cache/API migration, draft 155, aggregate `PreparedFunctionLookups`, or `PreparedBirModule` retirement. |
| `ideas/closed/202_route3_memory_source_identity_adapter.md` | Route 3 closure: selected load-local adapter landed; `PreparedMemoryAccessLookups` and prepared load-local source-home fallback remain authoritative for address formation, materialization, addressing-mode legality, final operands, and policy-sensitive source-home behavior. |
| `ideas/closed/203_route4_publication_identity_adapter.md` | Route 4 closure: selected AArch64 call-boundary current-block publication source reader moved only for semantic publication identity; publication mechanics, output-sensitive behavior, wrappers, and prepared fallback remain retained. |
| `ideas/closed/204_route5_edge_join_source_adapter.md` | Route 5 closure: selected current-block join-source boundary validates indexed Route 5 identity; prepared scheduling, storage, wrappers, branch policy, final edge-copy records, and emitted output remain authoritative. |
| `ideas/closed/205_route6_call_use_source_adapter.md` | Route 6 closure: selected AArch64 scalar call argument source-producer adapter accepts Route 6 only under strict route/prepared agreement; prepared call plans remain authoritative for ABI, layout, helper protocol, policy, storage, movement, and emitted output. |
| `ideas/closed/206_route7_comparison_provenance_diagnostic_oracle.md` | Route 7 closure: materialized-condition provenance reader validates Route 7 and fails closed; prepared/AArch64 authority remains for branch targets, final branch spelling, suffix mapping, fused legality, hazards, emitted-register state, final order, and assembler rows. |
| `docs/bir_prealloc_fusion/phase_d_followup_pre_phase_e_readiness.md` | Phase D selected-consumer migrations are useful evidence but not true Phase E retirement work; draft 155 is not ready; residual fallback/oracle, target-policy, cross-target, and return-chain boundaries remain blockers. |
| `docs/bir_prealloc_fusion/prepared_function_lookups_ownership_readiness_map.md` | `PreparedFunctionLookups` remains aggregate compatibility/pass-context; selected Route 3-7 subfacts can move only one consumer at a time with prepared fallback and route/prepared agreement proof. |
| `docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md` | Prepared printer, CLI dump, route-debug, helper-oracle, AArch64 lookup threading, and target-wrapper surfaces remain retained diagnostic/oracle authorities until route-native equivalents prove positive, negative, ambiguous, mismatch, and fallback behavior. |
| `docs/bir_prealloc_fusion/residual_route_view_consumer_migration_map.md` | Residual Route 3-7 production, printer/debug, target-wrapper, oracle, fallback, and candidate next selected-migration surfaces are inventoried without claiming contraction readiness. |
| `ideas/closed/190_full_suite_baseline_99_percent_regression_attribution.md` | Baseline recovery proved Route 3 semantic memory/source identity cannot replace AArch64 prepared target-addressing policy; fallback and target-policy preservation are hard readiness rules. |
| `ideas/closed/199_full_suite_baseline_string_authority_timeout_attribution.md` | Accepted baseline and string-authority discipline must not be weakened; final accepted baseline was restored to 3428/3428, and string-authority failures require attribution before any baseline refresh. |

Missing required inputs: none found for Step 1.

## Step 1 Working Evidence List

| Candidate | Retained surface | C2 classification | Retained boundary for D2 | Source reference |
| --- | --- | --- | --- | --- |
| Route 3 memory/source | `memory_accesses`, `PreparedMemoryAccessLookups`, prepared memory/source helpers, load-local source helpers, memory/frame/stack oracles, target-addressing fallback. | Retained public fallback/oracle plus retained target/prepared policy. | Route 3 may answer selected memory/source identity only. Address formation, frame/global/TLS relocation, offsets, layout, addressing-mode legality, volatile/address-space lowering, materialization, final operands, source-home policy, and fallback stay prepared/target-owned. | C2 route table; Route 3 closure note; Phase B2 Route 3 row; idea 190 closure. |
| Route 3 diagnostic/oracle rows | Memory/frame/stack oracle rows and prepared lookup-helper statuses covering memory access, global-load, same-block global-load, load-local source, non-memory, mismatch, ambiguity, and fallback. | Retained public fallback/oracle and diagnostic/oracle authority. | No diagnostic row can contract from adapter greenness. Route-native replacement would need equivalent positive, absent, non-memory, alias/address ambiguity, mismatch, fallback, and string/baseline proof while preserving target-addressing fallback. | C2 diagnostic/oracle section; diagnostics checklist; Route 3 closure; idea 190 and idea 199 closures. |
| Route 4 publication identity | `edge_publications`, current/block-entry publication helpers, prepared publication lookup mechanics, wrappers, debug/output rows. | Retained target/prepared policy and retained public fallback/oracle. | Route 4 owns selected semantic publication identity only. Edge-copy emission, move/home/storage policy, stack-source extension, block-order emission, immediate payload spelling, wrapper formatting, emitted strings, duplicate/ambiguous/wrong-reference fallback, and prepared lookup authority remain retained. | C2 route table; Route 4 closure note; Phase B2 Route 4 row; residual map Route 4 row. |
| Route 4 diagnostic/wrapper rows | Prepared printer/debug publication rows, route-oracle tests, x86/riscv wrapper compatibility, emitted/debug string surfaces. | Retained public fallback/oracle and diagnostic/oracle replacement prerequisite. | Wrapper or diagnostic work must preserve emitted/debug output and prepared fallback. Production reader migration does not remove printer/debug, wrapper, or oracle authority. | C2 retained public fallback list; diagnostics checklist; Phase D readiness route coverage and fallback/oracle tables. |
| Route 5 edge/join-source | `PreparedEdgePublicationLookups`, `PreparedEdgePublicationSourceProducerLookups`, indexed edge-publication/source-producer helpers, current-block join-source helpers, move-bundle lookup surfaces, edge/join wrapper oracles. | Retained target/prepared policy and retained public fallback/oracle. | Route 5 owns selected edge/join-source identity only. Parallel-copy scheduling, source/destination homes, move bundles, branch spelling, scratch policy, cycle temporaries, execution-site placement, storage, wrappers, final edge-copy records, and emitted output remain prepared/target-owned. | C2 route table; Route 5 closure note; Phase B2 Route 5 row; residual map Route 5 row. |
| Route 5 diagnostic/oracle rows | Edge-publication, source-producer, current-block join, move-bundle, x86 joined-branch, prepared printer, and helper-oracle rows. | Retained public fallback/oracle and diagnostic/oracle authority. | Any next diagnostic/oracle row must prove no-source, memory-source, duplicate/conflict, absent/mismatch fallback, wrapper output stability, and adjacent branch/parallel-copy sanity without changing expected strings. | Diagnostics checklist; C2 retained public fallback list; Route 5 closure note. |
| Route 6 call-use source | `call_plans`, `PreparedCallPlanLookups`, call argument/result helper surfaces, prepared call-plan fallback, call-boundary oracles, x86 `ConsumedPlans` compatibility context. | Retained target/prepared policy and retained public fallback/oracle. | Route 6 owns selected call-use source identity only under route/prepared agreement. ABI placement, call wrapper kind, clobbers, outgoing stack sizing, byval lanes, variadic FPR counts, helper/carrier protocols, value homes, move bundles, publication routing, aggregate transport layout, final call records, storage, movement, and emitted output remain retained. | C2 route table; Route 6 closure note; PreparedFunctionLookups map `call_plans` row; Phase B2 Route 6 row. |
| Route 6 wrapper/debug rows | Prepared call printer/debug rows, call-boundary tests, prepared lookup-helper, x86 route-debug compatibility rows, `ConsumedPlans` fallback, direct-call/helper oracle messages. | Retained public fallback/oracle and target-wrapper compatibility. | x86 Route 6 reuse is one agreement-gated scalar source boundary, not route-wide x86 migration. Diagnostic/wrapper replacement must preserve absent, mismatched, ambiguous, ABI-bound, and fallback statuses plus unchanged AArch64/x86 output. | Phase D readiness cross-target section; residual map Route 6 row; diagnostics checklist; Route 6 closure note. |
| Route 7 comparison provenance | Route 7 materialized-condition/fused-compare provenance helpers, prepared fused-compare and materialized-condition fallbacks, branch-policy oracles, route-debug/printer/debug rows, expected-string authority. | Diagnostic/oracle replacement prerequisite plus retained target/prepared policy. | Route 7 owns selected comparison provenance only. Branch targets, final branch spelling, condition suffix mapping, fused legality, hazards, emitted-register state, final instruction order, final assembler rows, compare/branch instruction selection, and branch-policy oracle behavior remain prepared/AArch64-owned. | C2 route table; Route 7 closure note; Phase B2 Route 7 row; diagnostics checklist. |
| Route 7 diagnostic/oracle rows | Materialized-condition provenance reader, fused-compare helper oracles, route-debug/handoff rows, prepared printer/debug, branch-policy expected strings. | Diagnostic/oracle replacement prerequisite. | Route 7 diagnostic work is prerequisite evidence, not contraction. Replacement must cover fused, materialized, unfused fallback, absent-route, invalid-reference, duplicate, mismatch, expected-string proof, and prepared fallback without branch-policy cleanup. | C2 diagnostic/oracle section; Route 7 closure note; diagnostics checklist. |
| `PreparedFunctionLookups` aggregate | `call_plans`, `address_materializations`, `memory_accesses`, `move_bundles`, `value_homes`, `edge_publications`, and `edge_publication_source_producers` as one aggregate lookup/cache/pass-context bundle. | Retained aggregate compatibility/pass-context; blocked aggregate work. | No group-wide deletion, privatization, clone, or facade rename is supported. Later work must name one lookup group and one consumer or diagnostic row, exact route fact, retained prepared fallback/oracle, target/prepared policy exclusion, and proof cases. | C2 aggregate coupling section; PreparedFunctionLookups ownership map summary and contraction prerequisites; Phase A2 aggregate rows. |
| `PreparedBirModule` aggregate | Mixed target-neutral BIR module state plus target profile, names/control flow, value locations, stack/frame/dynamic-stack, addressing, liveness, regalloc, call, publication, variadic, storage, wide-carrier, atomic, intrinsic, inline-asm, runtime-helper, phase, and note state. | Retained target/prepared policy plus transient pass context and diagnostic/oracle compatibility; blocked aggregate retirement. | Draft 155 is not ready. Broad aggregate retirement requires a separate field-by-field owner map, fallback/oracle strategy, diagnostic/string proof, and public consumer strategy; selected Route 3-7 adapter greenness is not evidence for demotion or deletion. | C2 PreparedBirModule blockers; Phase D readiness conclusion and retirement blockers; Phase A2 `PreparedBirModule` row. |
| AArch64 lookup threading and pass context | `PreparedFunctionLookups` and prepared lookup groups threaded through AArch64 lowering context; narrow route views may be threaded beside prepared context later. | Transient pass-context/private construction cache plus retained target/prepared policy. | D2 may later identify one route fact delivery boundary, but Step 1 evidence says pass-context threading is not itself semantic ownership or contraction readiness. | Phase A2 AArch64 lookup threading row; C2 aggregate coupling; diagnostics checklist AArch64 lookup threading row. |
| Cross-target wrappers | x86 `ConsumedPlans` and route-debug compatibility, riscv prepared emission/wrapper behavior, target-wrapper tests. | Target-wrapper compatibility plus retained target/prepared policy. | Only one x86 Route 6 scalar source reuse point is proven. Other x86/riscv work must wait for an AArch64-proven route view or retained-owner boundary and must preserve frame/register/ABI/formatting/emission policy as target-local. | Phase D readiness cross-target section; residual map target-wrapper area; diagnostics checklist target-wrapper row. |
| Baseline and string authority | Accepted full-suite baseline, string-authority guard, expected output, helper names, supported-path status, c_testsuite contracts. | Cross-cutting proof guardrail and diagnostic/string authority. | D2 follow-ups cannot use baseline refreshes, expected-string rewrites, unsupported downgrades, helper renames, output relabeling, or timeout masking as evidence. Current accepted baseline evidence is non-regressive 3428/3428 after idea 199. | Ideas 190 and 199 closure notes; C2 explicit no-go decisions; Phase A2 baseline/string-authority row. |
| Return-chain boundary | Route 8 return-chain owner/schema line and historical prepared return-chain helper/oracle evidence. | Separate imported owner/schema line; no-route blocker for Routes 1-7. | Return-chain cannot be folded into Route 1 producer identity, Route 7 comparison provenance, predecessor rescans, name matching, generic route-index facades, or D2 retained-surface contraction. | Phase D readiness return-chain section; Phase B2 Route 8 row; residual map return-chain area. |

## Step 1 Closure Constraints

- Routes 3 through 7 selected adapters proved bounded route/prepared agreement
  or fail-closed validation for one selected reader or diagnostic surface; they
  did not remove public prepared helper groups, aggregate lookup fields,
  target-wrapper boundaries, printer/debug rows, oracle families, or target
  policy ownership.
- C2 accepted no bounded micro-contraction candidate. Later D2 work must start
  from retained public fallback/oracle, retained target/prepared policy,
  diagnostic/oracle replacement prerequisite, transient pass-context, or
  blocked aggregate classifications.
- Any future follow-up must be one prepared surface or one diagnostic/oracle
  row wide. It must name the route fact or retained-owner evidence, the
  fallback/oracle that remains, the target/prepared policy it refuses to move,
  and proof for positive, absent, invalid, duplicate/conflict, mismatch, and
  fallback cases as applicable.
- Draft 155, broad `PreparedFunctionLookups` retirement, broad
  `PreparedBirModule` retirement, aggregate clone/facade work, and prepared API
  deletion are not supported by this Step 1 evidence.
- Baseline, helper-oracle, diagnostic, wrapper, supported-path, and
  expected-string authority must remain non-regressive throughout D2.

## Step 1 Completion Check

- Required Phase A2, B2, C2, D, closure-note, residual-map,
  diagnostics/oracle, aggregate-readiness, and baseline-recovery inputs were
  read.
- Route 3 through Route 7 retained surfaces are listed with C2
  classification, retained boundary, and source references.
- Diagnostic/oracle rows, target/prepared policy boundaries, pass-context
  areas, blocked aggregate surfaces, return-chain constraints, and baseline
  authority are recorded as raw evidence for later D2 steps.

## Step 2 Current Consumer Source Anchors

Step 2 inspected current consumers without changing implementation or tests.
`c4c-clang-tool-ccdb` was used to confirm the AArch64 production anchors in
`traversal.cpp`, `memory.cpp`, `calls.cpp`, and `comparison.cpp`; `rg` was used
for printer/debug, target-wrapper, oracle/expected-string, pass-context, and
aggregate-coupling consumers.

Common anchors used by the dependency map:

- Production/prealloc construction: `BirPreAlloc::publish_contract_plans(...)`
  populates frame, call, store-source publication, variadic, storage, carrier,
  runtime-helper, intrinsic, inline-asm, and other prepared products before
  lowering (`src/backend/prealloc/prealloc.cpp:40`).
- `PreparedBirModule` remains the aggregate phase artifact with target profile,
  names, control flow, value locations, stack/frame, addressing, liveness,
  regalloc, call, publication, variadic, storage, carrier, helper, intrinsic,
  inline-asm, phase, and note state (`src/backend/prealloc/module.hpp:38`).
- `PreparedFunctionLookups` exposes `call_plans`, `address_materializations`,
  `memory_accesses`, `move_bundles`, `value_homes`, `edge_publications`, and
  `edge_publication_source_producers` as one aggregate lookup surface
  (`src/backend/prealloc/prepared_lookups.hpp:15`).
- AArch64 traversal builds `PreparedFunctionLookups`, `PreparedCallPlanLookups`,
  address-materialization, value-home, and move-bundle lookups per function, then
  stores their addresses in `FunctionLoweringContext`
  (`src/backend/mir/aarch64/codegen/traversal.cpp:74`,
  `src/backend/mir/aarch64/codegen/traversal.cpp:85`).
- AArch64 `FunctionLoweringContext` still publicly carries the prepared module
  and projected prepared lookup handles (`src/backend/mir/aarch64/module/module.hpp:73`).
- Prepared printer output prints the prepared module header plus BIR, summaries,
  control flow, value locations, call plans, store-source publications,
  select-chain materializations, carriers, runtime helpers, and addressing
  (`src/backend/prealloc/prepared_printer.cpp:11`).
- Prepared diagnostic replacement remains blocked while prepared printer, CLI
  dump, x86 route-debug, helper-oracle, AArch64 lookup-threading, and
  target-wrapper consumers exist
  (`docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md:29`).
- `PreparedFunctionLookups` ownership audit keeps only one-consumer/one-route
  follow-ups viable and rejects aggregate clone, rename-only facade, target-only
  wrapper migration, or deletion while current readers remain
  (`docs/bir_prealloc_fusion/prepared_function_lookups_ownership_readiness_map.md:42`).
- Expected-string and dump-route tests still assert prepared sections, route
  debug, required/forbidden snippets, and baseline string authority
  (`tests/backend/bir/CMakeLists.txt:439`,
  `tests/backend/cmake/run_backend_codegen_route_case.cmake:45`,
  `tests/backend/cmake/run_backend_codegen_route_case.cmake:59`).

## Step 2 Retained-Surface Consumer Dependency Map

| Candidate | Current consumers observed | Retained fallback, policy, or oracle authority | Adapter effect and Step 3 implication |
| --- | --- | --- | --- |
| Route 3 memory/source | Production: AArch64 globals, memory, calls, and ALU still read prepared memory/address surfaces through `find_prepared_memory_access(...)`, `memory_accesses`, `address_materialization_lookups`, and load-local source fallback (`src/backend/mir/aarch64/codegen/memory.cpp:913`, `src/backend/mir/aarch64/codegen/globals.cpp:157`, `src/backend/mir/aarch64/codegen/calls.cpp:8250`, `src/backend/mir/aarch64/codegen/alu.cpp:1039`). Printer/debug: prepared addressing emits access and address-materialization rows (`src/backend/prealloc/prepared_printer/addressing.cpp:49`). Target-wrapper: x86 local-slot/global guards still call prepared memory access helpers (`src/backend/mir/x86/module/module.cpp:5453`). Oracles: prepared lookup helper and memory operand tests assert memory-access and address-materialization behavior (`tests/backend/bir/backend_prepared_lookup_helper_test.cpp:4955`, `tests/backend/mir/backend_aarch64_prepared_memory_operand_records_test.cpp:2123`). | Prepared memory lookup, prepared load-local/global-source helpers, address-materialization lookups, and target-addressing policy remain authority for address formation, frame/global/TLS relocation, offsets, address spaces, base-plus-offset legality, materialization, final operands, and mismatch/fallback behavior. Idea 190 still makes target-addressing fallback non-negotiable. | Existing Route 3 adapters can answer selected source identity only. Step 3 should treat Route 3 as a possible one-consumer semantic read, not `memory_accesses` contraction or target-wrapper migration. |
| Route 3 diagnostic/oracle rows | Production/debug consumers include AArch64 memory diagnostics and address-materialization diagnostics (`src/backend/mir/aarch64/codegen/memory.cpp:246`, `src/backend/mir/aarch64/codegen/globals.cpp:55`). Printer/oracle consumers include prepared addressing printer rows and helper-oracle status tests (`src/backend/prealloc/prepared_printer/addressing.cpp:57`, `tests/backend/bir/backend_prepared_lookup_helper_test.cpp:5062`). Expected-string consumers include dump snippets that require prepared addressing output (`tests/backend/bir/CMakeLists.txt:497`). | Prepared memory/frame/stack oracle rows remain authoritative for positive, absent, non-memory, ambiguous, mismatch, fallback, and target-policy-sensitive outputs. | Step 3 should not open a diagnostic replacement unless it names one row and proves equivalent route-native success and fail-closed statuses without changing expected strings. |
| Route 4 publication identity | Production: AArch64 call-boundary source reads Route 4 first and then falls back to prepared current-block publication consumption through `edge_publication_source_producers` (`src/backend/mir/aarch64/codegen/calls.cpp:6852`, `src/backend/mir/aarch64/codegen/calls.cpp:6950`). Publication/edge consumers still use `edge_publications` and source-producer lookup groups through the aggregate (`src/backend/prealloc/prepared_lookups.hpp:21`). Printer/debug: value-location printer emits block-entry publication rows (`src/backend/prealloc/prepared_printer/value_locations.cpp:177`). Target-wrapper: x86/riscv prepared publication paths still consume prepared module or lookup-derived publication state (`src/backend/mir/riscv/codegen/emit.cpp:320`, `src/backend/mir/x86/prepared/dispatch.cpp:56`). | Prepared publication fallback remains authority for missing publication, duplicate/ambiguous rows, wrong-reference rejection, wrapper compatibility, move/home/storage policy, immediate payload spelling, and emitted/debug output. | The Route 4 adapter affects one current-block publication source reader. Step 3 can consider a single publication-source consumer only if it keeps prepared fallback and refuses publication mechanics or output migration. |
| Route 4 diagnostic/wrapper rows | Printer/debug consumers include prepared block-entry publication output (`src/backend/prealloc/prepared_printer/value_locations.cpp:202`). CLI/dump consumers require prepared control-flow/value-location/publication sections (`tests/backend/bir/CMakeLists.txt:453`). Target wrappers include x86 publication-plan reuse tests and riscv edge-publication emission (`tests/backend/mir/backend_x86_publication_plan_reuse_test.cpp:89`, `src/backend/mir/riscv/codegen/emit.cpp:308`). | Prepared printer/debug, target-wrapper, and expected-string authority remain. Route 4 does not own wrapper formatting, emitted strings, block-entry publication mechanics, or x86/riscv target policy. | Step 3 should classify wrapper/diagnostic work as replacement-prerequisite only; any follow-up must be one wrapper row or one diagnostic row wide. |
| Route 5 edge/join-source | Production: AArch64 current-block join routing validates Route 5 when available, then retains prepared edge-publication/value-home fallback (`src/backend/mir/aarch64/codegen/dispatch_producers.cpp:405`, `src/backend/mir/aarch64/codegen/dispatch_producers.cpp:446`). Direct query facts still use prepared edge-publication lookups (`src/backend/mir/aarch64/codegen/dispatch_producers.cpp:102`). Printer/debug: prepared control-flow prints join transfers and parallel-copy authority (`src/backend/prealloc/prepared_printer/control_flow.cpp:151`, `src/backend/prealloc/prepared_printer/control_flow.cpp:217`). Oracles: current-block join routing and prepared lookup helper tests still cover move/source statuses (`tests/backend/mir/backend_aarch64_current_block_join_routing_test.cpp:44`, `tests/backend/bir/backend_prepared_lookup_helper_test.cpp:3297`). | Prepared edge-publication, source-producer, value-home, and move-bundle surfaces remain authority for no-source, memory-source, duplicate/conflict, execution-site, cycle-temp, branch/parallel-copy sanity, final move records, and emitted output. | Route 5 proves one semantic join-source validation boundary. Step 3 should not treat it as edge-publication, source-producer, or move-bundle contraction. |
| Route 5 diagnostic/oracle rows | Prepared control-flow, value-location, and helper-oracle consumers still assert join-transfer, parallel-copy, move-bundle, edge-publication, and source-producer rows (`src/backend/prealloc/prepared_printer/control_flow.cpp:220`, `src/backend/prealloc/prepared_printer/value_locations.cpp:116`, `tests/backend/bir/backend_prepared_lookup_helper_test.cpp:1195`). x86 joined-branch expected strings still exercise prepared-control-flow ownership (`tests/backend/bir/backend_x86_handoff_boundary_joined_branch_test.cpp:11329`). | Prepared edge/join oracle authority remains for no-source, memory-source, unsupported move, duplicate/conflict, absent/mismatch fallback, wrapper output stability, and branch/parallel-copy sanity. | Step 3 can only propose a single helper-oracle or printer row if it preserves prepared expected strings and proves equivalent route-native statuses. |
| Route 6 call-use source | Production: AArch64 call lowering requires `PreparedCallPlan` and indexed call plan lookups (`src/backend/mir/aarch64/codegen/calls.cpp:83`, `src/backend/mir/aarch64/codegen/calls.cpp:111`). Call-boundary helpers still classify prepared moves and read call-plan indexes (`src/backend/mir/aarch64/codegen/calls.cpp:4103`, `src/backend/mir/aarch64/codegen/calls.cpp:4174`, `src/backend/mir/aarch64/codegen/calls.cpp:8999`). Target-wrapper: x86 `ConsumedPlans` stores call plans, prepared lookups, and a Route 6 index, then only accepts a scalar `i32` Route 6 source when it agrees with prepared call-plan source ids (`src/backend/mir/x86/x86.hpp:14`, `src/backend/mir/x86/x86.hpp:95`). Printer/debug: prepared summaries and call-plan sections print callsites, wrappers, args, variadic counts, aggregate transport, and late publications (`src/backend/prealloc/prepared_printer/functions.cpp:418`, `src/backend/prealloc/prepared_printer/calls.cpp:452`). | Prepared call plans remain authority for ABI placement, wrapper kind, clobbers, outgoing stack sizing, byval lanes, variadic FPR counts, helper/carrier protocols, value homes, move bundles, publication routing, aggregate transport, final call records, and emitted output. | Route 6 adapters affect selected source identity under strict route/prepared agreement. Step 3 can target one call argument/result source consumer only; whole `call_plans` and x86 wrapper migration stay retained. |
| Route 6 wrapper/debug rows | x86 route-debug consumes prepared call plans and prepared names for summary/trace rows (`src/backend/mir/x86/debug/debug.cpp:60`, `src/backend/mir/x86/debug/debug.cpp:428`). Prepared printer call rows and aggregate transport rows remain diagnostic authority (`src/backend/prealloc/prepared_printer/calls.cpp:372`, `src/backend/prealloc/prepared_printer/calls.cpp:457`). Route-debug tests compare exact expected summaries/traces (`tests/backend/bir/backend_x86_route_debug_test.cpp:1408`). | Prepared call printer/debug, x86 route-debug, direct-call/helper-oracle messages, `ConsumedPlans` fallback, and expected strings remain authoritative. | Step 3 should classify Route 6 wrapper/debug work as diagnostic/oracle replacement prerequisite unless one exact row proves route-native parity while preserving prepared output. |
| Route 7 comparison provenance | Production: AArch64 comparison still reads prepared fused-compare branch facts and prepared fused-operand producers (`src/backend/mir/aarch64/codegen/comparison.cpp:200`, `src/backend/mir/aarch64/codegen/comparison.cpp:239`). The selected materialized-condition reader validates Route 7 but falls back to generic BIR identity when validation fails (`src/backend/mir/aarch64/codegen/comparison.cpp:349`). Branch lowering still uses prepared branch facts for condition suffix, targets, stack-home compare, fused support, and final assembler lines (`src/backend/mir/aarch64/codegen/comparison.cpp:2106`, `src/backend/mir/aarch64/codegen/comparison.cpp:2444`). Printer/oracle consumers include machine-printer fused-compare tests and branch-control Route 7 tests (`tests/backend/mir/backend_aarch64_machine_printer_test.cpp:1633`, `tests/backend/mir/backend_aarch64_branch_control_lowering_test.cpp:1459`). | Prepared/AArch64 authority remains for branch targets, final branch spelling, condition suffix mapping, fused legality, hazards, emitted-register state, final instruction order, final assembler rows, and branch-policy oracle behavior. | Route 7 adapter effect is provenance validation for selected materialized-condition/fused-compare contexts. Step 3 should target at most one diagnostic/oracle row or one comparison consumer, not branch policy or printer/debug contraction. |
| Route 7 diagnostic/oracle rows | Route 7 helper-oracle tests assert fused and materialized-condition positive and fail-closed paths (`tests/backend/bir/backend_prepared_lookup_helper_test.cpp:9643`, `tests/backend/bir/backend_prepared_lookup_helper_test.cpp:9797`). Branch-control tests cover duplicate, absent, mismatch, and invalid Route 7 reference fallback (`tests/backend/mir/backend_aarch64_branch_control_lowering_test.cpp:1554`, `tests/backend/mir/backend_aarch64_branch_control_lowering_test.cpp:1641`). Prepared diagnostic checklist retains comparison helpers, scalar producer/select-chain fallback, fused-compare oracles, and target branch policy (`docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md:48`). | Prepared fused-compare/materialized-condition helpers and branch-policy expected strings remain oracle authority for positive, absent-route, invalid-reference, duplicate, mismatch, unfused fallback, and prepared fallback behavior. | Step 3 should keep Route 7 diagnostic work prerequisite-only unless it names one row and proves equivalent route-native oracle coverage without weakening branch-policy expectations. |
| `PreparedFunctionLookups` aggregate | Production/pass context: AArch64 traversal constructs the aggregate and projects lookup pointers into context (`src/backend/mir/aarch64/codegen/traversal.cpp:74`, `src/backend/mir/aarch64/module/module.hpp:83`). x86 `ConsumedPlans` builds and stores the aggregate as a compatibility wrapper (`src/backend/mir/x86/x86.hpp:65`, `src/backend/mir/x86/x86.hpp:134`). riscv prepared emission builds function lookups and consumes edge-publication-derived state (`src/backend/mir/riscv/codegen/emit.cpp:531`, `src/backend/mir/riscv/codegen/emit.cpp:537`). Oracles: ownership map lists current readers across call, memory, move, value-home, edge-publication, source-producer, printer/debug, target-wrapper, and helper-oracle groups (`docs/bir_prealloc_fusion/prepared_function_lookups_ownership_readiness_map.md:42`). | Aggregate compatibility/pass-context remains authority for delivery of prepared fallback to production, printer/debug, wrappers, and oracle tests. Individual fields mix target/prepared policy, retained public fallback, and selected route-native subfacts. | Step 3 should reject aggregate deletion, clone, facade rename, or broad privatization. A valid next idea must name one lookup group and one consumer or diagnostic row. |
| `PreparedBirModule` aggregate | Production: `PreparedBirModule` is the prealloc product and AArch64/x86/riscv prepared-module handoff input (`src/backend/prealloc/prealloc.cpp:29`, `src/backend/mir/aarch64/module/module.hpp:136`, `src/backend/mir/x86/api/api.hpp:24`, `src/backend/mir/riscv/codegen/emit.hpp:84`). Printer/debug: `prepare::print(...)` dumps the aggregate (`src/backend/prealloc/prepared_printer.cpp:11`). Tests/dumps require prepared-module sections for multiple targets (`tests/backend/bir/CMakeLists.txt:453`, `tests/backend/bir/backend_prepared_printer_test.cpp:4674`). | The aggregate remains target/prepared policy plus diagnostic/oracle compatibility and transient pass context. It owns durable prepared phase state and broad public handoff behavior that selected Route 3-7 adapters do not replace. | Step 3 should keep draft 155 and broad `PreparedBirModule` retirement out of scope; any future work needs a separate field-by-field owner map and proof strategy. |
| AArch64 lookup threading and pass context | AArch64 context explicitly carries `PreparedBirModule`, `PreparedFunctionLookups`, call-plan, address-materialization, move-bundle, and value-home lookup handles (`src/backend/mir/aarch64/module/module.hpp:73`). Traversal wires those handles per function (`src/backend/mir/aarch64/codegen/traversal.cpp:71`). Diagnostic checklist names lookup threading as a blocker needing independent route-fact delivery proof (`docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md:35`). | Pass-context threading remains retained until one route fact family can be delivered and consumed independently of prepared lookup handles while preserving prepared fallback and target policy. | Step 3 may identify one route fact delivery boundary, but should not classify context threading itself as semantic ownership or contraction readiness. |
| Cross-target wrappers | x86 `ConsumedPlans` retains prepared module, lookup aggregate, call plans, and Route 6 index side by side (`src/backend/mir/x86/x86.hpp:14`). x86 module lowering still validates prepared control-flow/value-location/addressing handoff and renders prepared memory/branch/call wrapper paths (`src/backend/mir/x86/module/module.cpp:719`, `src/backend/mir/x86/module/module.cpp:5453`). riscv prepared emission accepts `PreparedFunctionLookups` and prepared modules (`src/backend/mir/riscv/codegen/emit.hpp:72`, `src/backend/mir/riscv/codegen/emit.cpp:531`). Wrapper tests remain listed as blockers (`docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md:36`). | Target-wrapper compatibility remains authority for target-local frame/register/ABI/formatting/emission policy, with prepared fallback retained for mismatches and unsupported shapes. | Step 3 should not propose route-wide x86/riscv migration. Only a single already-proven route-view wrapper input can be considered. |
| Baseline and string authority | Expected-string consumers include backend route CMake snippets, prepared dump snippets, route-debug comparisons, and smoke/route runners (`tests/backend/bir/CMakeLists.txt:439`, `tests/backend/bir/CMakeLists.txt:572`, `tests/backend/bir/backend_x86_route_debug_test.cpp:1390`, `tests/backend/cmake/run_backend_codegen_route_case.cmake:45`). Diagnostic checklist rejects printer label rewrites, unsupported downgrades, and expected-output weakening as readiness evidence (`docs/bir_prealloc_fusion/prepared_diagnostics_oracle_replacement_plan.md:116`). | Accepted baseline and string authority remain proof guardrails. Expected strings, helper names, supported-path status, c_testsuite contracts, and timeout/baseline discipline cannot be weakened to make a surface look ready. | Step 3 must treat expected-string edits as non-progress unless separately justified by route-native semantic proof and non-regressive attribution. |
| Return-chain boundary | Current production uses Route 8 BIR records/index helpers in AArch64 ALU lowering, not the removed prepared helper API (`src/backend/mir/aarch64/codegen/alu.cpp:1422`, `src/backend/mir/aarch64/codegen/alu.cpp:1491`, `src/backend/mir/aarch64/codegen/alu.cpp:4405`). Route 8 helper/oracle tests cover fail-closed return-chain behavior (`tests/backend/bir/backend_prepared_lookup_helper_test.cpp:10271`). Import notes record that old public prepared return-chain helpers were removed after idea 180 and Route 8 owns only target-neutral same-block return-chain identity (`docs/bir_prealloc_fusion/return_chain_import_and_naming.md:72`, `docs/bir_prealloc_fusion/return_chain_import_and_naming.md:83`). | Return ABI, homes, registers, scratch policy, ALU records, final return emission order, and helper/oracle expected strings remain outside Route 8. Return-chain must not be folded into Routes 1-7 or a generic route-index facade. | Step 3 should treat return-chain as an imported separate owner/schema boundary, usually citation-only for D2, not a retained-surface contraction candidate for Routes 3-7. |

## Step 2 Completion Check

- A dependency-map row was recorded for every plausible Step 1 candidate.
- Current consumers were grouped as production, printer/debug, target-wrapper,
  oracle/expected-string, pass-context, and aggregate coupling where applicable.
- Each row records the retained fallback, policy, or oracle authority and the
  adapter effect for later Step 3 follow-up classification.
- No implementation files or tests were changed.

## Step 3 Classification Rules

Step 3 uses only the categories allowed by the source idea:

- semantic reader migration;
- diagnostic/oracle replacement;
- public fallback removal after replacement evidence;
- retained-owner adapter clarification;
- target/prepared policy ownership migration;
- no-action because the surface is aggregate, policy-owned, or still blocked.

An accepted follow-up below is not a contraction claim. It is only a viable
later implementation idea when it is exactly one prepared surface or one
diagnostic/oracle row wide, keeps the retained prepared fallback in scope, and
refuses aggregate migration, draft 155, `PreparedFunctionLookups` retirement,
`PreparedBirModule` retirement, baseline weakening, and expected-string
rewrites.

## Step 3 Follow-Up Viability Classification

| Candidate | Category | Decision | Rationale and retained boundary |
| --- | --- | --- | --- |
| Route 3 memory/source | Semantic reader migration | Accepted follow-up candidate: one AArch64 semantic reader that consumes selected Route 3 memory/source identity while retaining prepared memory/address fallback. | Step 2 found production readers still using `find_prepared_memory_access(...)`, `memory_accesses`, address-materialization lookups, and load-local source fallback. The viable scope is one consumer of source identity only; address formation, frame/global/TLS relocation, offsets, address spaces, materialization, final operands, target-addressing policy, and idea 190 fallback stay prepared/target-owned. |
| Route 3 diagnostic/oracle rows | Diagnostic/oracle replacement | Accepted follow-up candidate only for one named memory/source helper-oracle or prepared addressing printer row. | A row-wide replacement could be analysis-worthy if it proves route-native positive, absent, non-memory, ambiguous, mismatch, fallback, and unchanged string behavior. No whole memory oracle family, printer section, target-wrapper migration, or expected-string refresh is accepted. |
| Route 4 publication identity | Semantic reader migration | Accepted follow-up candidate: one publication-source consumer beside the existing current-block publication reader. | The selected Route 4 adapter affects one current-block publication source boundary. A follow-up may move one semantic source read, but prepared publication fallback remains authority for missing/ambiguous/wrong-reference cases, publication mechanics, move/home/storage policy, immediate spelling, wrapper compatibility, and emitted/debug output. |
| Route 4 diagnostic/wrapper rows | Diagnostic/oracle replacement | No action for broad wrapper or diagnostic migration; accepted only if Step 4 later names one exact wrapper row or diagnostic row. | Step 2 retained prepared printer/debug, x86/riscv wrapper, CLI dump, and expected-string consumers. Route 4 does not own wrapper formatting, emitted strings, block-entry publication mechanics, or target policy, so no route-wide wrapper/debug work is viable. |
| Route 5 edge/join-source | Semantic reader migration | Accepted follow-up candidate: one current-block join-source semantic reader. | Route 5 proves one join-source validation boundary. Prepared edge-publication, source-producer, value-home, and move-bundle surfaces remain authority for no-source, memory-source, duplicate/conflict, execution-site, cycle temporaries, branch/parallel-copy sanity, final move records, and emitted output. |
| Route 5 diagnostic/oracle rows | Diagnostic/oracle replacement | Accepted follow-up candidate only for one named helper-oracle or prepared printer row. | A row-wide replacement can be considered if it preserves no-source, memory-source, unsupported move, duplicate/conflict, absent/mismatch fallback, wrapper output stability, and branch/parallel-copy sanity. Whole edge-publication, source-producer, move-bundle, or x86 joined-branch migration is no-action. |
| Route 6 call-use source | Semantic reader migration | Accepted follow-up candidate: one call argument or result source consumer under route/prepared agreement. | Route 6 can identify selected call-use source identity, including the existing scalar x86 agreement point, but prepared call plans remain authority for ABI placement, wrapper kind, clobbers, outgoing stack sizing, byval lanes, variadic counts, helper/carrier protocol, value homes, move bundles, aggregate transport, final call records, storage, movement, and emitted output. |
| Route 6 wrapper/debug rows | Diagnostic/oracle replacement | No action for broad x86 route-debug, call printer, or wrapper migration; accepted only if later narrowed to one exact row. | Prepared call printer/debug, x86 route-debug, `ConsumedPlans` fallback, direct-call/helper-oracle messages, and expected strings are still authorities. A viable later row must prove route-native parity without changing the prepared output contract. |
| Route 7 comparison provenance | Semantic reader migration | Accepted follow-up candidate: one comparison provenance consumer, not branch policy. | The Route 7 materialized-condition/fused-compare provenance adapter can validate selected provenance, but prepared/AArch64 branch facts still own targets, suffix mapping, fused legality, hazards, emitted-register state, final order, final assembler rows, and branch-policy oracle behavior. |
| Route 7 diagnostic/oracle rows | Diagnostic/oracle replacement | Accepted follow-up candidate: one Route 7 fused-compare or materialized-condition helper-oracle row. | A row-wide diagnostic replacement is viable only if it preserves fused, materialized, unfused fallback, absent-route, invalid-reference, duplicate, mismatch, expected-string, and prepared fallback behavior. Branch-policy cleanup, printer/debug contraction, and expected-string weakening remain no-action. |
| `PreparedFunctionLookups` aggregate | No-action because aggregate/pass-context blocked | No action. | Step 2 confirmed the aggregate still delivers prepared fallback to AArch64 traversal, x86 `ConsumedPlans`, riscv emission, printer/debug, wrappers, and helper-oracles. Aggregate deletion, clone, rename-only facade, broad privatization, and broad retirement are rejected. Future work must name one lookup group and one consumer or diagnostic row. |
| `PreparedBirModule` aggregate | No-action because aggregate/policy blocked | No action. | The module remains the prepared phase artifact and public handoff for target policy, printer/debug, dumps, wrappers, and tests. Draft 155 and broad `PreparedBirModule` clone/facade/deletion/retirement are rejected until a separate field-by-field ownership map and proof strategy exist. |
| AArch64 lookup threading and pass context | No-action because pass-context/policy-owned | No action. | Threading prepared lookup handles through AArch64 context is delivery infrastructure, not semantic ownership. A later idea may name one route fact delivery boundary, but context threading itself is not contraction-ready and must keep prepared fallback plus target policy available. |
| Cross-target wrappers | No-action because target-wrapper/policy-owned | No action. | x86 and riscv wrappers remain target-local frame, register, ABI, formatting, and emission authorities. Only an already proven single route-view wrapper input could be considered later; route-wide x86/riscv migration is rejected. |
| Baseline and string authority | No-action because proof guardrail | No action. | Expected strings, helper names, supported-path status, c_testsuite contracts, baseline recovery, and timeout/string-authority rules are proof constraints. Edits that mainly rewrite expected output, relabel helpers, refresh baselines, or downgrade supported paths are non-progress. |
| Return-chain boundary | No-action because separate imported owner/schema line | No action. | Route 8 return-chain identity is outside Route 3-7 retained-surface contraction. It must not be folded into Route 1 producer identity, Route 7 comparison provenance, predecessor rescans, generic route-index facades, or this D2 follow-up set. |

## Step 3 Accepted Follow-Up Set

The accepted set is intentionally limited to one-surface or one-row candidates:

- Route 3: one AArch64 memory/source semantic reader, with prepared memory and
  target-addressing fallback retained.
- Route 3: one memory/source helper-oracle or prepared addressing printer row.
- Route 4: one publication-source semantic reader, with publication mechanics
  and output policy retained.
- Route 4: one exact publication wrapper or diagnostic row, only if Step 4 can
  name the row and proof shape.
- Route 5: one current-block join-source semantic reader.
- Route 5: one edge/join helper-oracle or prepared printer row.
- Route 6: one call argument or result source consumer under route/prepared
  agreement.
- Route 6: one exact call wrapper/debug/oracle row, only if Step 4 can name the
  row and proof shape.
- Route 7: one comparison provenance consumer.
- Route 7: one fused-compare or materialized-condition helper-oracle row.

None of these accepted candidates supports public fallback removal yet. Public
fallback removal remains a later category that is blocked until a named
one-surface replacement has positive, negative, mismatch, fallback,
printer/debug or wrapper, and expected-string proof.

## Step 3 Explicit No-Action Decisions

- `PreparedFunctionLookups` aggregate work is no-action: aggregate deletion,
  broad privatization, clone/facade work, and retirement are still blocked.
- `PreparedBirModule` aggregate work is no-action: draft 155 and broad module
  retirement are not ready from Route 3-7 selected adapter evidence.
- AArch64 lookup threading/pass-context work is no-action as a contraction
  target: it may only supply a future one-route-fact delivery boundary.
- Cross-target wrapper work is no-action unless narrowed to one already proven
  route-view wrapper input and one target-local row.
- Baseline and string authority are no-action as implementation targets:
  expected-output rewrites, baseline refreshes, helper renames, unsupported
  downgrades, and timeout masking are rejected as readiness evidence.
- Return-chain work is no-action for D2 retained-surface contraction because
  it belongs to the separate Route 8 owner/schema line.

## Step 3 Retirement Blockers

`PreparedFunctionLookups` retirement remains blocked by active production,
pass-context, target-wrapper, printer/debug, and oracle consumers across
`call_plans`, `address_materializations`, `memory_accesses`, `move_bundles`,
`value_homes`, `edge_publications`, and
`edge_publication_source_producers`. The current evidence supports only
single lookup-group plus single consumer or single diagnostic-row follow-ups;
it does not support aggregate deletion, broad privatization, facade-only
renaming, or compatibility-wrapper removal.

`PreparedBirModule` retirement remains blocked by its role as the prepared
phase artifact and public handoff for target profile, names/control flow,
value locations, stack/frame/dynamic-stack, addressing, liveness, regalloc,
call, publication, variadic, storage, wide-carrier, atomic, intrinsic,
inline-asm, runtime-helper, phase, note, printer/debug, dump, wrapper, and
test authority. Broad retirement needs a separate field-by-field owner map,
fallback/oracle replacement plan, diagnostic/string proof plan, and public
consumer strategy before draft 155 or module contraction can be reopened.

## Step 3 Completion Check

- Every Step 2 candidate has an accepted or no-action decision.
- Accepted candidates are limited to one prepared surface or one
  diagnostic/oracle row and preserve retained prepared fallback.
- Aggregate, policy-owned, pass-context, baseline/string, return-chain, and
  blocked retirement surfaces have explicit no-action decisions.
- Draft 155, broad `PreparedFunctionLookups` retirement, broad
  `PreparedBirModule` retirement, broad migration/API contraction, and
  expectation weakening remain rejected.
- Remaining blockers to `PreparedFunctionLookups` and `PreparedBirModule`
  retirement are recorded for Step 4/Step 5 proof planning.

## Step 4 Proof Shape Rules

Step 4 attaches proof expectations only to the accepted Step 3 follow-up set.
These proof shapes are prerequisites for later idea drafting, not contraction
claims. No later public fallback removal, aggregate contraction, wrapper
contraction, prepared-printer contraction, or expected-string change is valid
unless the exact one-surface proof shape below has been satisfied first.

Each later implementation idea must stay inside the named one-surface scope,
retain the listed prepared fallback or policy boundary, cite the evidence
source, and prove the applicable positive, absent, invalid,
duplicate/conflict, mismatch, fallback, printer/debug, wrapper, and
expected-string cases without weakening supported-path status.

## Step 4 Accepted Follow-Up Proof Shapes

| Accepted follow-up | One-surface scope | Retained fallback or policy boundary | Evidence source | Required proof shape |
| --- | --- | --- | --- | --- |
| Route 3 memory/source semantic reader | One AArch64 memory/source semantic reader that consumes selected Route 3 source identity before prepared lookup fallback. | Prepared memory lookup, load-local/global-source helpers, address-materialization lookups, and target-addressing policy still own address formation, frame/global/TLS relocation, offsets, address spaces, base-plus-offset legality, materialization, final operands, and mismatch/fallback behavior. | Step 2 Route 3 memory/source row: `memory.cpp:913`, `globals.cpp:157`, `calls.cpp:8250`, `alu.cpp:1039`; idea 190 target-addressing fallback rule. | Positive: route/prepared agreement selects the same source identity for the named reader. Absent: missing Route 3 fact keeps prepared lookup behavior. Invalid: wrong or unusable Route 3 reference fails closed to prepared lookup. Mismatch: route/prepared disagreement preserves prepared result. Fallback: target-addressing and address-materialization paths remain prepared-owned. Printer/debug, wrapper, and expected-string cases: existing prepared addressing output and x86 wrapper behavior must remain unchanged; no expected string may change. |
| Route 3 memory/source helper-oracle or prepared addressing printer row | One named memory/source helper-oracle status row or one prepared addressing printer row. | Prepared memory/frame/stack oracle rows remain authority for positive, absent, non-memory, ambiguity, mismatch, fallback, and target-policy-sensitive output. | Step 2 Route 3 diagnostic/oracle row: `memory.cpp:246`, `globals.cpp:55`, `prepared_printer/addressing.cpp:57`, `backend_prepared_lookup_helper_test.cpp:5062`, `CMakeLists.txt:497`. | Positive: route-native row matches the current prepared success status for the named row. Absent: no Route 3 fact reports the same fail-closed status as prepared. Invalid: invalid reference reports the same diagnostic status. Duplicate/conflict: ambiguous or alias/address conflict reports the same status. Mismatch: route/prepared disagreement preserves prepared status. Fallback: target-addressing fallback stays visible. Printer/debug and expected-string cases: the named printer or oracle string is byte-stable unless separately justified by semantic proof; no baseline refresh or helper rename counts as proof. |
| Route 4 publication-source semantic reader | One publication-source semantic reader beside the existing current-block publication reader. | Prepared publication fallback still owns missing publication, duplicate/ambiguous rows, wrong-reference rejection, publication mechanics, move/home/storage policy, immediate payload spelling, wrapper compatibility, and emitted/debug output. | Step 2 Route 4 publication identity row: `calls.cpp:6852`, `calls.cpp:6950`, `prepared_lookups.hpp:21`, `prepared_printer/value_locations.cpp:177`, `riscv emit.cpp:320`, `x86/prepared/dispatch.cpp:56`. | Positive: route/prepared agreement supplies the same publication source identity to the named reader. Absent: missing Route 4 fact uses prepared publication fallback. Invalid: wrong-reference publication fails closed. Duplicate/conflict: ambiguous publication rows keep prepared behavior. Mismatch: route/prepared disagreement keeps prepared source. Fallback: publication mechanics and immediate/output policy remain prepared/target-owned. Wrapper and expected-string cases: x86/riscv wrapper output and prepared debug strings stay unchanged. |
| Route 4 block-entry publication printer/debug row | One block-entry publication printer/debug row from the prepared value-location/publication output, not a wrapper family or CLI dump section. | Prepared printer/debug, target-wrapper, and expected-string authority remain retained; Route 4 does not own wrapper formatting, emitted strings, block-entry publication mechanics, or target policy. | Step 2 Route 4 diagnostic/wrapper row: `prepared_printer/value_locations.cpp:202`, prepared dump snippets in `tests/backend/bir/CMakeLists.txt:453`, x86 publication-plan reuse tests, and riscv edge-publication emission evidence. | Positive: the named publication row can be reproduced from route-native evidence with the same prepared semantics. Absent: missing Route 4 publication keeps the current prepared row or absence status. Invalid: wrong-reference publication keeps the current diagnostic behavior. Duplicate/conflict: duplicate or ambiguous rows keep the current prepared diagnostic. Mismatch: route/prepared disagreement refuses route-native replacement. Fallback: prepared printer/debug remains authoritative. Printer/debug and expected-string cases: the exact row text remains byte-stable. Wrapper cases: x86/riscv wrapper output is checked for no change, but wrapper migration is out of scope. |
| Route 5 current-block join-source semantic reader | One current-block join-source semantic reader. | Prepared edge-publication, source-producer, value-home, and move-bundle surfaces remain authority for no-source, memory-source, duplicate/conflict, execution-site placement, cycle temporaries, branch/parallel-copy sanity, final move records, and emitted output. | Step 2 Route 5 edge/join-source row and Route 5 diagnostic/oracle row: prepared control-flow/value-location rows, helper-oracle rows, and x86 joined-branch expected-string evidence. | Positive: route/prepared agreement provides the same current-block join source. Absent: no Route 5 fact keeps prepared no-source behavior. Invalid: invalid source reference fails closed to prepared behavior. Duplicate/conflict: duplicate or conflicting join-source rows keep prepared behavior. Mismatch: route/prepared disagreement keeps the prepared source. Fallback: move-bundle, branch, and parallel-copy policy remain prepared/target-owned. Wrapper and expected-string cases: joined-branch and prepared output strings remain unchanged. |
| Route 5 edge/join helper-oracle or prepared printer row | One named edge/join helper-oracle row or one prepared printer row covering join transfer, parallel copy, move bundle, edge publication, or source producer. | Prepared edge/join oracle authority remains for no-source, memory-source, unsupported move, duplicate/conflict, absent/mismatch fallback, wrapper output stability, and branch/parallel-copy sanity. | Step 2 Route 5 diagnostic/oracle row: `prepared_printer/control_flow.cpp:220`, `prepared_printer/value_locations.cpp:116`, `backend_prepared_lookup_helper_test.cpp:1195`, `backend_x86_handoff_boundary_joined_branch_test.cpp:11329`. | Positive: route-native evidence reproduces the named prepared success row. Absent: no edge/join fact reports the same no-source/absent status. Invalid: invalid source or publication reference preserves prepared status. Duplicate/conflict: duplicate or conflicting rows preserve prepared diagnostics. Mismatch: route/prepared disagreement preserves prepared row. Fallback: unsupported move and branch/parallel-copy paths remain prepared-owned. Printer/debug, wrapper, and expected-string cases: the named row and adjacent wrapper/joined-branch strings remain byte-stable. |
| Route 6 call argument or result source consumer | One call argument or result source consumer under route/prepared agreement. | Prepared call plans remain authority for ABI placement, wrapper kind, clobbers, outgoing stack sizing, byval lanes, variadic FPR counts, helper/carrier protocols, value homes, move bundles, publication routing, aggregate transport, final call records, storage, movement, and emitted output. | Step 2 Route 6 call-use source row: `calls.cpp:83`, `calls.cpp:111`, `calls.cpp:4103`, `calls.cpp:4174`, `calls.cpp:8999`, x86 `ConsumedPlans` Route 6 agreement evidence, prepared printer call rows. | Positive: route/prepared agreement supplies the same argument or result source for the named consumer. Absent: no Route 6 fact uses prepared call-plan source. Invalid: invalid call-use reference fails closed. Duplicate/conflict: conflicting route source facts do not override prepared call plans. Mismatch: route/prepared disagreement preserves prepared source. Fallback: ABI, wrapper kind, aggregate transport, and final call records remain prepared/target-owned. Printer/debug, wrapper, and expected-string cases: prepared call printer and x86 route-debug output remain unchanged. |
| Route 6 x86 scalar source agreement route-debug row | One x86 Route 6 scalar source agreement route-debug row, not broad x86 call wrapper migration. | Prepared call printer/debug, x86 route-debug, direct-call/helper-oracle messages, `ConsumedPlans` fallback, and expected strings remain authoritative. | Step 2 Route 6 wrapper/debug row: `x86/debug/debug.cpp:60`, `x86/debug/debug.cpp:428`, `prepared_printer/calls.cpp:372`, `prepared_printer/calls.cpp:457`, `backend_x86_route_debug_test.cpp:1408`. | Positive: the route-debug row reports the same scalar source agreement as the prepared-backed path. Absent: no Route 6 fact keeps the prepared debug row. Invalid: invalid call-use reference keeps current route-debug behavior. Duplicate/conflict: ambiguous route source facts refuse route-native replacement. Mismatch: route/prepared disagreement reports or falls back exactly as today. Fallback: `ConsumedPlans` remains the compatibility boundary. Printer/debug and expected-string cases: the exact route-debug text is byte-stable. Wrapper cases: x86 wrapper behavior is checked for no output change, but wrapper contraction is out of scope. |
| Route 7 comparison provenance consumer | One comparison provenance consumer, not branch policy, suffix mapping, fused legality, or final assembler rows. | Prepared/AArch64 branch facts still own targets, condition suffix mapping, fused legality, hazards, emitted-register state, final instruction order, final assembler rows, and branch-policy oracle behavior. | Step 2 Route 7 comparison provenance row: `comparison.cpp:200`, `comparison.cpp:239`, `comparison.cpp:349`, `comparison.cpp:2106`, `comparison.cpp:2444`; machine-printer and branch-control route tests. | Positive: route/prepared agreement supplies the same comparison provenance to the named consumer. Absent: missing Route 7 fact uses prepared/generic BIR fallback. Invalid: invalid reference fails closed. Duplicate/conflict: duplicate provenance facts keep prepared/AArch64 behavior. Mismatch: route/prepared disagreement preserves prepared provenance. Fallback: branch policy, fused legality, suffixes, targets, and final assembler remain prepared/AArch64-owned. Printer/debug and expected-string cases: machine-printer and branch-control expected strings remain unchanged. |
| Route 7 fused-compare or materialized-condition helper-oracle row | One fused-compare or materialized-condition helper-oracle row. | Prepared fused-compare/materialized-condition helpers and branch-policy expected strings remain oracle authority for positive, absent-route, invalid-reference, duplicate, mismatch, unfused fallback, and prepared fallback behavior. | Step 2 Route 7 diagnostic/oracle row: `backend_prepared_lookup_helper_test.cpp:9643`, `backend_prepared_lookup_helper_test.cpp:9797`, branch-control duplicate/absent/mismatch/invalid fallback tests, diagnostics checklist comparison-helper row. | Positive: route-native helper row matches the current fused or materialized-condition success row. Absent: absent Route 7 fact reports the same fail-closed status. Invalid: invalid reference reports the same status. Duplicate/conflict: duplicate or conflicting provenance rows preserve current diagnostic status. Mismatch: route/prepared disagreement preserves prepared oracle behavior. Fallback: unfused and prepared fallback behavior stays intact. Printer/debug and expected-string cases: helper-oracle and branch-control expected strings remain byte-stable. Wrapper cases: no wrapper contraction is implied. |

## Step 4 Proof Shape Completion Check

- Every accepted Step 3 follow-up has a one-surface or one-row scope.
- Each accepted follow-up names the retained prepared fallback, target-policy,
  wrapper, printer/debug, or oracle boundary that remains authoritative.
- Each accepted follow-up cites the Step 2 evidence source that must anchor any
  later implementation idea.
- Positive, absent, invalid, duplicate/conflict, mismatch, fallback,
  printer/debug, wrapper, and expected-string cases are recorded where
  applicable.
- No accepted proof shape allows public fallback removal, aggregate
  contraction, prepared-module contraction, broad wrapper migration, baseline
  refresh, helper rename, unsupported downgrade, or expected-string change
  without separate proof.

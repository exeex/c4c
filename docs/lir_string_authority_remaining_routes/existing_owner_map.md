# 812 existing open-owner map

Evidence revision: `d58b8d44c9b64d2005d2b3760a0592b1b47ebd03`.

This map preserves ownership; it does not declare any owner complete or move a return point. Step 2 must cite one of these exact records before using the `existing open owner` disposition.

| Owner | Exact current scope | Blocked/parked chain and return point |
| --- | --- | --- |
| 734 | Typed LIR-to-new-BIR receiver completeness; it cannot invent producer identity or recover it from text. | Its current call/body-parameter/other raw forms remain later packets; see `ideas/open/734...:90-151`. 829 is a producer prerequisite for its selected next parameter receipt. |
| 795 | One bounded body-parameter index handoff. | Its accepted bounded publication returns to already-open 810 Step 3; it is not a family-wide parameter receipt (`795:53-63`). |
| 796 | Bounded scalar cast and pointer-subtraction result authority. | Steps 4--5 return only to 801 Step 2 after a clean comparable baseline; nonselected cast producers remain fail-closed (`796:50-132`). |
| 797 | Final dispatcher/proof convergence after accepted producer handoffs and 734 receipts. | It owns no producer repair or receiver invention (`797:8-35`). |
| 829 | Direct non-variadic call argument-1 body-parameter authority tuple. | Blocked because `arg_type_refs` is empty and `structured_args[1].operand` is text-only; after 830 proves a native arg-1 relation, resume 829 Step 2 (`829:69-101`). |
| 830 | Native direct-call argument-1 structured identity/type prerequisite. | Parked at Step 3 by 831. After accepted comparable baseline repairs, resume 830 Step 3, then return 829 Step 2 (`830:71-101`). |
| 831 | Decomposition and comparable-baseline gate for aggregate-owner residuals. | Closed 832/833/834 are only accepted subroutes. Its Step 4 must receive 836's bounded return before a comparable gate can return 830 (`831:149-190`). |
| 836 | Remaining aggregate-owner rejection first-owner decomposition. | Parked at unchanged Step 1 by the 837 priority architecture switch; after fresh evidence and any bounded route, return 831 Step 4, never directly to 830/829 (`836:69-99`). |
| 821/822 | Manual and lowering-produced switch selector authority. | Separate active/resumed selector routes; do not absorb into call/type text classification. |
| 838 | Canonical module-owned aggregate ref/store convergence. | Explicitly does not close parked 836/831; its aggregate owner/store work must not be credited to their gates. |
| 839 | Nominal function signatures and call composition. | Retains `signature_text`, `args_str`, and parsed-call construction until named migration gates; it does not own 829/830 value identity. |
| 840 | Nominal vector store/schema migration. | Does not reopen accepted 754/811/814/815; later schema migration is distinct from those bounded credits. |
| 841 | Compact scalar and ABI-leaf migration. | Opaque policy remains evidence-needed; no scalar inference from text. |
| 842 | Restricted first-class value boundary unions. | Only named boundaries; no universal replacement or signature/body-use ownership. |
| 843 | Direct HIR family construction and array composition. | Covers bounded HIR producers/recursive arrays; excludes global/extern mirrors and collectors. |
| 844 | Global/extern/initializer family facts. | Explicitly retains initializer-text redesign and scanner deletion for named later routes. |
| 845 | Typed reference carriers and collector migration. | Migrates one named source at a time; no rendered-text reconstruction. |
| 846 | Family-overloaded verifier/dispatch/printer. | Does not own producer/store construction or generic API deletion. |
| 847 | Universal model/string escape-hatch deletion. | Deletion waits for all named M1--M15 consumer/adaptor gates and does not own 797 completion. |

The “newer” records above were present at evidence capture and are relevant to the same authority surfaces. They prevent 812 from routing already-owned work to 813; their scope text, rather than this summary, is authoritative.

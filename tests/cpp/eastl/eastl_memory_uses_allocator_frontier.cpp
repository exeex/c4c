// Reduced parse-only timeout frontier for the EASTL memory bring-up path.
// The active cost now reproduces without the full runtime-shaped
// eastl_memory_simple.cpp driver.

#include <EASTL/internal/memory_uses_allocator.h>

int main() {
    return 0;
}

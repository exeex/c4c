interface:
  display_name: "Phoenix Rebuild"
  short_description: "Scripted staged rebuild driver"
  default_prompt: "Use $phoenix-rebuild to drive a scripted staged rebuild: glob legacy .cpp/.hpp files into markdown extraction artifacts with parallel agents, keep one .hpp index per directory, then proceed through review, replacement drafts, and only later real implementation."
